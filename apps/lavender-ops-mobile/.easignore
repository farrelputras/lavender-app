# Native directories — regenerated by expo prebuild on EAS (Linux paths)
# EAS doesn't respect nested .gitignore files; these are gitignored in
# apps/lavender-ops-mobile/.gitignore but must also be listed here.
android/
ios/

# JS build output
dist/
web-build/

# Expo local cache
.expo/

# OS
.DS_Store
Thumbs.db
