{
  "name": "lavender-monorepo",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "mobile": "npm run start --workspace=apps/mobile"
  },
  "engines": {
    "node": ">=22.0.0"
  }
}
