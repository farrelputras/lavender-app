# v1.0.4 — Release report

- **Release plan:** `docs/releases/v1-0-4.md`
- **Requirements:** `docs/prd/PRD-4-system-nav-bar-covers-controls.md` · `docs/prd/PRD-5-readable-at-moms-font-size.md`
- **Implementation plan:** *not yet written* — see **D-0** below; Lead proposes writing it **after** the
  discovery pass rather than before, and says why.
- **Lead session:** 2026-07-22 · **Opus 4.8, xhigh** (Fable not selected for this session — sanctioned
  fallback per `docs/agents/lead.md`)
- **Status:** ▶ **Phase 2 — plan approved by Farrel 2026-07-22 `[by-Farrel]`; discovery dispatched.
  A second gate follows discovery, before any code is written.**

---

## Pre-execution state

Verified this session, 2026-07-22, by running the commands — not quoted from v1.0.3.

| Fact | Value |
|---|---|
| Branch | `master`, working tree **clean** |
| HEAD | `193697f docs: renumber PDDIKTI release to unblock v1.0.4 (PRD-4 + PRD-5)` |
| Test baseline | **32 suites / 174 tests, all passing** (28.3s), run from `apps/lavender-ops-mobile` |
| **Lint baseline (debt #6 obligation — discharged)** | **159 real (non-`prettier`) errors** on `master` |
| Lint total | 27,544 errors, of which **27,385** are phantom `prettier/prettier` CRLF noise |
| Migrations | **none opened by this release** — last applied stays as-is |
| Subagent models | `developer-frontend` · `tester` pinned `sonnet` (available); `developer-backend` **not needed** |

### The lint baseline, recorded properly

Debt #6 revision 2 obligates *the next release that runs this gate* to replace the stale `~157` with a
measured number and record the command. Done:

```powershell
# from apps/lavender-ops-mobile — note: NOT `pnpm run lint`, which auto-fixes
$j = npx eslint . --format json | ConvertFrom-Json
$msgs = $j | ForEach-Object { $_.messages } | Where-Object { $_.severity -eq 2 }
$msgs | Where-Object { $_.ruleId -ne 'prettier/prettier' } | Measure-Object
```

**Baseline: 159.** Breakdown, because it is load-bearing for this release:

| Count | Rule |
|---|---|
| 88 | `react-native/no-inline-styles` |
| 37 | `no-restricted-imports` (raw `Text` / `TextInput`) |
| 15 | `react-native/no-color-literals` |
| 12 | `react-native/sort-styles` |
| 3 | `react-native/no-unused-styles` |
| 3 | `@typescript-eslint/no-unused-vars` |
| 1 | `import/no-unresolved` |

> ⚠️ **Two of these rows are hazards specific to v1.0.4, not background noise.**
>
> **`no-inline-styles` (88).** An inset-driven layout fix *is* a dynamic style. The obvious spelling —
> `style={{ paddingBottom: insets.bottom }}` — is a new violation on **every** screen this release
> touches. The release plan predicted this ("an app-wide layout sweep invites `no-inline-styles`"); the
> measured count turns it from a worry into an arithmetic certainty unless the briefs forbid the
> spelling. They do: **hoist dynamic styles into a `useMemo`'d object and reference it**
> (`style={[styles.bar, barStyle]}`), which the rule does not flag. This is a lint-gate requirement, not
> a style preference.
>
> **`no-restricted-imports` (37).** 37 sites import raw `Text`/`TextInput` instead of the wrapped
> components. **This is the single biggest threat to PRD-5's BR-8**: a clamp applied inside the wrapped
> `Text` primitive does not reach 37 sites. Whatever mechanism is chosen must either reach raw `Text`
> too, or those sites must be enumerated and handled. **This is the first question the discovery pass
> must answer.**

### Audit surface — enumerated now, so nobody samples later

PRD-4 AC-2/AC-3 and PRD-5 AC-5 all say *enumerated, not sampled*. The enumeration is fixed here so the
tester inherits it rather than inventing it:

**15 screens. 14 in the audit surface.**

`BerandaScreen` · `DetailSewaScreen` · `HutangDetailScreen` · `HutangFormScreen` · `HutangScreen` ·
`LoginScreen` · `PengembalianScreen` · `PilihKendaraanScreen` · `PilihUserScreen` · `RentalDetailScreen` ·
`RentalScreen` · `UserDetailScreen` · `UserFormScreen` · `UserScreen`

- **`KendaraanScreen` — excluded**, unreachable dead code (debt #3, release-plan guard 5). Must be
  *listed as excluded* in the test report, not omitted.
- **`ErrorScreen/ErrorBoundary` + `ErrorDetails` — in surface but low priority.** Reachable only on a JS
  crash, and `ErrorDetails` is the one file that already declares `safeAreaEdges`. Verify by scaffolding
  inheritance (AC-7), not by a hand pass.

**Bottom-bar inventory** (from the release plan, re-confirmed by file listing — the *contents* are the
discovery pass's job):

| Construct | Where | Treatment |
|---|---|---|
| Shared `components/form/BottomActionBar.tsx` | `HutangFormScreen`, `UserFormScreen` | **Fix properly** — BR-4/AC-7 binds here |
| Shared `components/form/EditActionBar.tsx` | `RentalDetailScreen` (v1.0.3) | **Fix properly** — same |
| Local bar | `DetailSewaScreen` (**holds *Simpan Rental* — AC-1**) | Local inset patch only (waiver) |
| Local bar | `PengembalianScreen` | Local inset patch only (waiver) |
| Local bar ×2 | `RentalDetailScreen` | Local inset patch only (waiver) |

**Delivery shape:** OTA only. No migration, no Edge Function, no native dep, `app.json` `version` stays
`1.0.0`. Rollback is a single `eas update:list --branch preview` republish. This is the first release
since v1.0.1 where that sentence is true without qualification.

---

## Decisions owed before dispatch

The release plan left four things to Lead. Resolving them here so the briefs are unambiguous. **All four
want Farrel's call; each is marked with what I recommend and why.**

### D-0 · There is no implementation plan, and I recommend not writing one yet `[by-agent]`

`docs/releases/v1-0-4.md` says the implementation plan is *"not yet written — owned by Lead."* v1.0.3 had
one before dispatch. **I am proposing we deliberately do not have one at dispatch time**, for a specific
reason rather than to save effort:

The central mechanism question of PRD-5 — *where does the text-scale clamp live so that it is genuinely
one decision (BR-8)?* — cannot be answered from the docs, and the obvious answers are likely wrong:

- **`Text.defaultProps.maxFontSizeMultiplier` is probably dead.** RN 0.83 / React 19 removed
  `defaultProps` support for function components, and RN's `Text` is a `forwardRef` function component.
  The most-cited global-clamp recipe on the internet would fail **silently** — no error, no clamp.
- **A clamp inside the wrapped `Text` component misses 37 raw-`Text` sites** (measured above).
- **Inline `fontSize` numbers** are plausible inside those 88 inline styles, which would defeat a
  token-level clamp too.

Planning a mechanism from that position is planning from belief. The project has an explicit lesson about
exactly this (v1.0.3, the `new Error(...)` mock: *"the test proves your belief about the library rather
than its behaviour"*).

**So: step ① is a read-only discovery pass, and Lead returns to Farrel with the mechanism before any code
is written.** The implementation plan gets written then, with facts in it. If Farrel would rather have a
plan document up front, say so and I will write one — but it will be conditional in exactly these places.

### D-1 · PRD-5 OQ-1 — the maximum supported text scale → **recommend `1.5`** `[by-agent, needs Farrel]`

The release plan resolved the *policy* (Mom is on MIUI "XL"; **cap high**, because under-capping shrinks
her text and reads as the app fighting her) but not the *number*.

**Recommendation: `MAX_FONT_SCALE = 1.5`.** Reasoning:

- MIUI's ladder is a labelled slider, not a number, and its mapping differs from stock Android's. I am
  not able to look up "XL" and get a trustworthy multiplier — and the release plan explicitly warns
  against doing exactly that.
- `1.5` sits above every value MIUI's slider is reported to reach, so **the clamp cannot be the thing
  that shrinks Mom's text.** That is the failure mode Farrel named as the expensive one.
- The cost is real and should be stated plainly: **the layouts must then survive 1.5×, not 1.3×.** That
  is more reflow work on 14 screens. I am recommending we pay it, because the alternative failure is
  discovered *on Mom's phone after publish*, and this release's only route to her device is the OTA
  itself.
- `1.3` was PRD-5's working assumption. If Farrel prefers it, that is a defensible call — but it should
  be made knowing it may sit *below* her actual setting.

**Companion recommendation — make this measurable instead of guessed** `[by-agent]`:
Beranda already renders a version footer (`app/config/release.ts`). Add **one line** to it showing the
device's reported `fontScale` and bottom inset. Cost: a few lines, zero risk, ships in the same OTA.
Benefit: Mom's next screenshot *tells us her real number* and simultaneously proves the inset is being
read — instead of us inferring both from whether the layout "looks fine". Remove it in the next release.

**Farrel's call on both.** If the footer line is unwanted, the cap stands on the cap-high argument alone.

### D-2 · The doubled-offset trap, named before it is built `[by-agent]`

PRD-4's constraints warn against "a doubled offset where a tab bar is also present" without saying where
it comes from. It comes from here, and both developers need it stated:

- Screens **inside the bottom tab navigator** (Beranda, Rental, Hutang, User) sit above a tab bar that —
  per PRD-4's Product-verified scoping note — **already accounts for the system inset.** Their scroll
  content must pad by **tab-bar height**, and adding `insets.bottom` on top of that double-counts.
- Screens **outside the tabs** (the *Sewa Baru* stack, the form screens, the detail screens) have no tab
  bar. They pad by the **raw `insets.bottom`**.

These are two different numbers and using one for both is the defect, pointed either way (dead band under
the tab bar, or *Simpan Rental* still covered). **BR-4 requires this be resolved once, in a shared
primitive — not decided per screen.** The mechanism (`useBottomTabBarHeight()` vs. a custom hook vs.
something the discovery pass finds already present) is the developer's call; **that it is one shared
decision is not.**

### D-3 · Who performs the enumerated visual audit, and on what `[by-agent, needs Farrel]`

This is the part of v1.0.4 I am least able to plan around, and I would rather say so than paper over it.

Almost every acceptance criterion in both PRDs is **visual and device-conditional**. Jest can prove a
handful of things honestly (below), and nothing else:

| Provable in Jest | Not provable in Jest |
|---|---|
| The shared hook returns 0 padding at `{bottom: 0}` and N at `{bottom: 48}` — **this is AC-5 / BR-5, the no-regression guard** | That a button is *visibly* clear of the nav bar |
| The clamp is applied by the shared primitive, at the chosen value | That a licence plate is not truncated |
| Screens render at a mocked `fontScale` without throwing | That a title and subtitle do not collide |
| Structural audit: no fixed-height container wrapping scalable text | Anything about how it *looks* |

So there must be a human visual pass over 14 screens × 2 conditions, and it must happen **before**
publish — because Mom's phone is only reachable *after* publish. Three options:

1. **Android emulator driven by `adb`** — recommended. Both conditions are settable:
   `adb shell settings put system font_scale 1.5` and forcing 3-button navigation via the
   `com.android.internal.systemui.navbar.threebutton` overlay. Reproduces Mom's configuration on a
   machine, repeatably, with screenshots attachable to the test report.
2. **Farrel's own device**, temporarily set to XL text + 3-button nav.
3. **Skip to Mom's phone.** Not acceptable — it makes her the first tester of 14 screens, and the release
   plan already commits to *"do not close the release on an emulator screenshot"*, which presumes there
   *was* one.

**Question for Farrel: is there a working Android emulator on this machine, and do you want the tester
briefed to drive it?** If not, the visual pass is yours and the plan should say so explicitly rather than
leaving it to be discovered at the gate. This is the single biggest schedule unknown in the release.

### D-4 · The lint budget ceiling is still owed by Farrel `[needs Farrel]`

Debt #6 revision 2 point 3: *"The budget needs a ceiling (⚠️ number still owed by Farrel)."* Cumulative
accepted increase is currently **+1** (v1.0.3's `RentalDetailScreen` raw `TextInput`).

This release is the natural moment to set it, because it is the release most likely to test it.
**Recommendation: ceiling `+10` cumulative** (i.e. 169 real errors), at which point the deliberate
`.gitattributes` renormalization + real-error cleanup gets scheduled as its own task.
**And for v1.0.4 specifically I am briefing for `+0`** — the `useMemo` style-hoisting requirement in D-0's
sibling note makes zero achievable, so any increase here should be argued for, not absorbed.

---

## Execution plan (awaiting approval)

### What is *not* in this release

- **No `developer-backend`.** Zero server surface: no migration, no RPC, no Edge Function, no connector
  signature change. This is the first LAVENDER release with no backend leg.
- **No connector signature to broker** in the usual sense. The contract this release must broker is a
  **layout contract between the two frontend dispatches** — see "The contract I am brokering".
- **No `connector-contract-reviewer`.** Its rules (async, camelCase, no row shapes, no signature change)
  have no surface here; both PRDs are presentation-only by constraint. Cheaper and more direct: the
  tester proves it with `git diff --stat` — **zero changes under `app/services/`, zero under
  `supabase/`**. Recorded as a deliberate omission, not an oversight.

### Sequencing

```
  ①  developer-frontend #1   DISCOVERY — read-only, changes nothing
        │                    answers the clamp mechanism + inset mechanism questions
        ▼
     ⏸  RE-GATE — Lead reports findings to Farrel; mechanism + contract locked;
                  implementation plan written (D-0). NO CODE BEFORE THIS.
        │
        ▼
  ②  developer-frontend #1   PRD-4 — space. Shared primitives + 3 local bars + scroll padding.
        │                    (same agent, context intact, via SendMessage)
        ▼
  ③  rental-math-reviewer    Fence check on DetailSewaScreen + PengembalianScreen ONLY:
        │                    did anything but spacing move?
        ▼
  ④  developer-frontend #2   PRD-5 — text. Clamp + reflow sweep, against ②'s settled space budget.
        │                    (fresh agent, briefed with ②'s handoff note)
        ▼
  ⑤  tester                  Automatable ACs + enumerated visual audit + full suite.
        │
        ▼
     ⏸  PUBLISH GATE — Farrel. Then OTA → Mom relaunches → Mom confirms (AC-8 both PRDs).
```

**Why PRD-4 first** — from the release plan, not re-derived: PRD-4 changes how much vertical room every
screen has. Reflowing text against a budget that is about to move is work done twice.

**Why a fresh agent for ④, when ② already knows the files** `[by-agent]`: `DetailSewaScreen` and
`PengembalianScreen` are 48KB and 50KB. One agent carrying PRD-4's sweep across 14 screens *and* PRD-5's
sweep across the same 14 is a context-exhaustion risk at the point where the work is most detailed. The
handoff cost is small and writable (see the contract below); the cost of an agent degrading mid-sweep is
a silently incomplete audit — the exact failure both PRDs' AC-5/AC-2 ("enumerated, not sampled") exist to
prevent. **② must therefore leave a written handoff note; it is a deliverable, not a courtesy.**

**Branch** `[by-agent]`: `v1.0.4-fits-moms-phone`, cut from `master`. Not `master` directly.

### The contract I am brokering

Provisional — **confirmed or amended at the re-gate**, once discovery has told us what actually exists.
Locked from that point: **④ codes to it; ② implements it; neither changes it without coming back to me.**

```ts
// ② creates. ④ consumes and must not redefine, move, or duplicate.

/** Bottom space a screen must reserve. Resolves tab-bar vs. raw-inset itself (D-2). */
export function useBottomSpace(): number

/** The one clamp value for the whole app (PRD-5 BR-8). One definition, one import site. */
export const MAX_FONT_SCALE: number
```

Two invariants ride on this and are worth more than the names:

1. **`useBottomSpace()` returns `0` when the device reports no inset and there is no tab bar.** This is
   PRD-4 AC-5 / BR-5 expressed as a unit test — the guard against fixing Mom's phone by degrading
   everyone else's.
2. **`MAX_FONT_SCALE` has exactly one definition site.** If ④ finds itself writing the number twice,
   BR-8 has failed and it must report rather than proceed.

### ① `developer-frontend` #1 — DISCOVERY — model `sonnet`

**Read-only. Change no file. Produce findings, not code.** Answer, with file:line evidence:

1. **The clamp mechanism.** Does `app/components/Text.tsx` (or equivalent wrapped primitive) exist, and
   does it forward `maxFontSizeMultiplier`? Confirm whether `Text.defaultProps` is viable on this
   RN/React version **by checking, not by assuming**. Enumerate the **37 `no-restricted-imports` sites**
   (`npx eslint . --format json`, filter `ruleId`) — those are raw `Text`/`TextInput` a wrapped-component
   clamp cannot reach. Report how many are inside the 14 audit screens. **Propose 2–3 mechanisms with
   their honest tradeoffs. Do not pick one — Lead and Farrel pick.**
2. **Inline `fontSize`.** How many of the 88 inline-style sites set `fontSize` directly, bypassing theme
   tokens? This decides whether a token-level approach is even available.
3. **The inset mechanism.** What does `Screen.tsx` do with `safeAreaEdges` today; how do the 4 tabbed
   screens obtain (or fail to obtain) tab-bar height; is `useBottomTabBarHeight` available from the
   installed `@react-navigation/bottom-tabs`; and which screens use a bare `FlatList`/`ScrollView` that
   bypasses `Screen` entirely (those need per-screen `contentContainerStyle` padding for AC-3).
4. **Keyboard interaction (AC-6).** Which form screens use `KeyboardAvoidingView`, and would adding
   bottom inset padding stack on top of it.
5. **Confirm the bottom-bar inventory above** — 2 shared + 3 local — or correct it.
6. **Fixed-height containers wrapping text** — a first-pass list, since PRD-5 names these as the
   mechanism behind the clipping.

**Standing instruction (Farrel's, carried from v1.0.3 and binding on every brief below):**
> **Verify against the codebase, not the plan.** These docs may be stale. **Read the actual file before
> acting on a claim about it.** Where reality diverges, **report the divergence** — do not silently
> follow either one.

**Reports back inline.** Then **STOP.** Lead re-gates with Farrel.

### ② `developer-frontend` #1 (continued) — PRD-4 — model `sonnet`

Dispatched only after the re-gate. Brief:

1. **Build the shared primitives** — `useBottomSpace()` per the locked contract, and apply it in
   `BottomActionBar.tsx` + `EditActionBar.tsx` and the shared screen scaffolding. **Replace the
   platform branch** (`iOS` vs. everything else) with inset-driven logic — PRD-4 names that branch as
   *the defect* (BR-3/BR-4). Preserve iOS home-indicator behavior (BR-6).
2. **Patch the 3 local bars in place** — `DetailSewaScreen`, `PengembalianScreen`, `RentalDetailScreen`
   (both bars). **Apply the same inset locally. Do NOT migrate them onto `BottomActionBar`** — that is
   debt #4's refactor and it is fenced (release-plan waiver, Farrel 2026-07-22). Spacing only.
3. **Scroll padding across all 14 screens** for AC-3, respecting D-2's tab-bar-vs-raw-inset split.
4. **TDD.** Mock `useSafeAreaInsets` at `{bottom: 0}` and `{bottom: 48}`. The zero case is AC-5 and is
   **not optional** — it is the no-regression guard, and it is the one AC of the two PRDs that Jest can
   prove outright.
5. **Lint discipline (gate-critical).** Baseline is **159**; brief is **+0**. Hoist every dynamic style
   into a `useMemo`'d object — never `style={{ ... }}` inline. **Do not run `eslint --fix` or
   `prettier --write` on anything** (release-plan guard 3, debt #6a): it would renormalize CRLF across
   the repo and make an app-wide layout diff unreviewable. Re-count with the command in Pre-execution
   state and report the delta.
6. **Hard fences.** No change to any calculation, connector, value, string, or control label. No new
   native dependency — **if the approach reaches for one, STOP and escalate** (it breaks AC-9/AC-10 and
   becomes an APK release, a different plan).
7. **Deliverable: a written handoff note for ④** — the final `useBottomSpace()` API, the per-screen
   bottom-space budget, which screens have which bottom construct, and anything ④ must not disturb.

### ③ `rental-math-reviewer` — fence check (read-only)

Narrow brief, and narrow on purpose: **`DetailSewaScreen` and `PengembalianScreen` only.** Confirm that
②'s diff on those two files is **spacing and nothing else** — no tariff composition, no fuel adjustment,
no auto-debt path, no value, no connector call moved. These are the two files debt #4 fences and the two
files `docs/02` §6 says must not be wrong. A finding here sends work back to ② before ④ starts.

*Not asked to review rental math generally* — nothing in this release intends to touch it, which is
precisely why an independent pair of eyes should confirm nothing did.

### ④ `developer-frontend` #2 — PRD-5 — model `sonnet`

Fresh agent. Briefed with the PRD, the locked contract, and ②'s handoff note. Brief:

1. **Apply the clamp** at the single point chosen at the re-gate. `MAX_FONT_SCALE` per **D-1**. One
   definition site (BR-8) — **if you find yourself writing the number twice, stop and report.**
2. **Fix the four reported defects first** (they are the PRD's flagship ACs): the fused
   `22 JuliSisa Rp 50.000` row (AC-1/AC-9 — **no rupiah amount may ever touch adjacent text**), the
   clipped *Sewa Baru* / *User Baru* quick-action labels (AC-2), truncated licence plates in the vehicle
   picker (AC-3 — **the plate must get room; shrinking it to fit is explicitly not acceptable**), and the
   crowded step header (AC-4).
3. **Then sweep the remaining screens** against BR-1/BR-2/BR-3 at `MAX_FONT_SCALE`, and **record the
   result screen by screen** — 14 screens, `KendaraanScreen` excluded and *named* as excluded. Sampling
   is a failed audit (AC-5).
4. **OQ-3 — the date/*Sisa* row: stack or reflow.** Your call, and the release plan says either satisfies
   BR-2 *"as long as the two values never touch."* Report which you chose and why; Farrel may want a
   different answer in front of Mom.
5. **OQ-2 is out of scope** — do **not** reduce any headline size at default scale. It would breach
   BR-5/AC-6 ("default size unchanged") and the question gets re-asked of Mom *after* she has this
   release.
6. **AC-6 / BR-5 is the ceiling on all of this:** at scale 1.0 nothing may change visually anywhere.
7. Same lint discipline, same hard fences, same standing instruction as ②. **Presentation only — do not
   touch rental math, connectors, or any value.**
8. Set `app/config/release.ts` `RELEASE` to `"1.0.4"`. **Do not touch `app.json` `version`** — it stays
   `1.0.0` (see CLAUDE.md: bumping it would target OTA at a runtime Mom's installed APK does not report,
   and she would silently stop receiving updates).
9. If Farrel approved the **D-1 diagnostic footer line**, add it here.

### ⑤ `tester` — model `sonnet`

**Derive "correct" from PRD-4 AC-1…AC-9 and PRD-5 AC-1…AC-10 — not from the implementation. If the code
contradicts a PRD, report a failure; do not adjust the test.**

1. **Automated, and be honest about what each proves:** the zero-inset no-regression case (PRD-4 AC-5 /
   PRD-5 AC-6 — the highest-value automated test in the release); the clamp applied at the chosen value
   with no further growth beyond it (AC-7); every screen renders without throwing at `fontScale =
   MAX_FONT_SCALE`; a **structural audit** for fixed-height containers wrapping scalable text.
2. **Prove the fences by diff, not assertion:** `git diff --stat` shows **zero** changes under
   `app/services/` and `supabase/`, and no change to `app.json` `version`.
3. **Author the enumerated visual-audit checklist for Farrel to walk** (D-3, decided: he runs it on his
   own device at XL text + 3-button nav). 14 screens × {default, `1.5`} × {zero inset, 3-button nav},
   `KendaraanScreen` listed as excluded. **This enumeration is the test report's main artifact.** Each
   row must name the *specific* thing to look at on that screen — which control, which label, which row —
   never "check that Beranda looks fine". Do **not** attempt to drive an emulator.
4. **Full suite from `apps/lavender-ops-mobile`** — never from the monorepo root. A root-level run
   resolves a different config and sweeps in a stale `.worktrees/` tree, reporting a wall of failures in
   files nobody touched. It cost v1.0.3 a detour immediately before a production push.
5. Re-count lint with the recorded command; report the delta against **159**.
6. Report coverage gaps against the PRDs plainly. **A criterion that can only be checked on Mom's phone
   should be reported as PENDING, never as passed.**

### Model routing

| Agent | Primary | Status |
|---|---|---|
| Lead (this session) | Fable high → **Opus xhigh** | on sanctioned fallback |
| `developer-frontend` ×2 | `sonnet` | pinned, available |
| `tester` | `sonnet` | pinned, available |
| `rental-math-reviewer` | inherits | — |
| `developer-backend` | — | **not dispatched** (no server surface) |

Per `docs/agents/lead.md`: if a developer's Sonnet becomes unavailable I **stop and hand to Farrel**. I
will not substitute a larger model for a developer — including for the discovery pass in ①, which is why
① is briefed to *report options* and leave the mechanism choice to Lead and Farrel.

---

## Risks I want on the record before approval

1. **The verification story is inverted from v1.0.3, and it is the release's defining constraint.** v1.0.3
   verified against production *before* the OTA. Here **every** acceptance criterion that matters is
   visual and device-specific, and the only route onto Mom's device is the OTA itself. The honest
   sequence is **publish → Mom relaunches → confirm**, and "shipped" is not claimable until she has
   confirmed. **D-3 exists to make sure that is not the first visual check anyone performs.**
2. **PRD-4 BR-4 is knowingly only partly met.** Three local bars get a local patch instead of migrating
   onto the shared primitive (Farrel, 2026-07-22). This **grows debt #4** — the local bars now carry
   inset logic too. **Record it as an addendum to #4 on ship**, per the release plan. Accepted, not
   overlooked.
3. **`no-inline-styles` is the likeliest gate breach**, for structural reasons, on a release that already
   owes debt #6 a re-counted baseline. Mitigated by the `useMemo` requirement in ② and ④'s briefs, and by
   a +0 brief rather than a budget.
4. **PRD-5's sweep has no natural stopping point.** "Every screen at maximum size" is 14 screens of
   judgment. The enumeration above is the fence; the risk is an agent that sweeps 9 and reports 14. The
   tester's independent enumeration is the control, which is why ④ and ⑤ are different agents.
5. **A cap of 1.5 costs more layout work than 1.3** (D-1). I am recommending we pay it. The failure it
   buys out of is discovered on Mom's phone, after publish, with no route to fix it except another OTA.

---

## Open items for Farrel — the plan is blocked on these

| # | Item | Lead recommends |
|---|---|---|
| # | Item | Lead recommended | **Farrel's call, 2026-07-22** |
|---|---|---|---|
| **D-0** | Discovery pass first, implementation plan written after it, re-gate before any code | Yes | ✅ **Approved as recommended** `[by-Farrel]` |
| **D-1a** | `MAX_FONT_SCALE` value | `1.5` | ✅ **`1.5`** `[by-Farrel]` |
| **D-1b** | `fontScale` + inset diagnostic line in the Beranda footer | Yes | ✅ **Yes — ships in this OTA, removed next release** `[by-Farrel]` |
| **D-3** | Who runs the 14-screen visual audit | Emulator via `adb`, by the tester | ⚠️ **Overruled — Farrel's own device, manually** `[by-Farrel]` (see below) |
| **D-4** | Lint budget ceiling (owed by debt #6 since 2026-07-21) | `+10` cumulative (169) | ✅ **`+10` cumulative = 169.** v1.0.4 briefed at **+0** `[by-Farrel]` |
| — | Branch `v1.0.4-fits-moms-phone` off `master` | Yes | ✅ |

### D-3 as decided — the visual audit is Farrel's, and the briefs change accordingly `[by-Farrel]`

Farrel takes the pre-publish visual pass on his own device, set to XL text + 3-button navigation. Two
consequences that must not be lost:

1. **The tester is no longer briefed to drive an emulator.** Its job narrows to the automatable ACs, the
   fence diffs, and — the important part — **producing the enumerated 14-screen × 2-condition checklist
   as a document Farrel walks**, with `KendaraanScreen` listed as excluded. The enumeration is still the
   test report's main artifact; only the hands change.
2. **The checklist must be written so it can be walked by a human in one sitting**, screen by screen with
   the specific thing to look at on each (which control, which label, which row) — not "check Beranda
   looks fine". A vague checklist is how a 14-screen audit silently becomes a 6-screen audit.

**Approval recorded. Discovery (①) dispatched 2026-07-22. No code will be written before the re-gate.**

---

## ① Discovery pass — findings (read-only, nothing changed)

Returned 2026-07-22. It did the job the gate existed for: **four of my Phase-1 premises were wrong**, and
one of them would have had a developer told a falsehood as the rationale for a real instruction.

### Corrections to my own plan `[by-agent]`

**C-1 · My lint rationale was wrong. Recording it rather than quietly fixing it.**
I wrote that `style={{ paddingBottom: insets.bottom }}` would be "a new violation on every screen this
release touches" and called it "an arithmetic certainty". It is neither.
`react-native/no-inline-styles` flags a style property only when its **value is a `Literal`**
(`node_modules/eslint-plugin-react-native/lib/util/stylesheet.js:394-421`). `insets.bottom` is a
`MemberExpression`; the rule's `invalid` flag never flips. **Proven against this codebase, not just the
rule source:** `DetailSewaScreen.tsx:993` and `PengembalianScreen.tsx:973` already contain
`<View style={{ height: spacing.xxxl + 64 }} />` — a pure inline object with a binary-expression value —
and **neither appears among the 88 flagged sites.**

I asserted a belief about a lint rule as a measured fact. That is the `new Error(...)` mock lesson from
v1.0.3 pointed at ESLint instead of Supabase, and it is worth writing down in exactly those terms.

> **The `useMemo` hoisting instruction stays** — it avoids the real trap (mixing a literal into the same
> object, e.g. `{ paddingBottom: insets.bottom, marginTop: 8 }`, *is* flagged), and it avoids a fresh
> object every render. **But it is briefed as hygiene, not as "otherwise it's a lint error."** The lint
> risk to the +0 budget is materially lower than I claimed.

**C-2 · "Fix the shared screen scaffolding" has no vehicle.** The release plan's *"no screen declares
`safeAreaEdges`"* is true but misleading: **none of the 14 audit screens imports `app/components/Screen.tsx`
at all.** Only `ErrorScreen/ErrorDetails.tsx` does. Every one of the 14 hand-rolls its own
`SafeAreaView edges={["top"]}` (bottom deliberately excluded) — `DetailSewaScreen.tsx:397`,
`PengembalianScreen.tsx:362`, `RentalScreen.tsx:114`, `RentalDetailScreen.tsx:297`, `BerandaScreen.tsx:78`,
`HutangScreen.tsx:94`, … `LoginScreen.tsx:42` is the lone exception — no `edges` prop, which defaults to
all four, so **Login already reserves the bottom inset today, by accident.**
**Consequence for PRD-4 BR-4:** "fix `Screen.tsx` and screens inherit" is false. The `useBottomSpace()`
hook *is* the shared vehicle; there is no scaffolding half to also fix.

**C-3 · `EditActionBar` does not branch on platform — it has no bottom handling at all**
(`EditActionBar.tsx:65-69`), and that is **correct**: it renders *inline inside scrolling content* for the
v1.0.3 inline-edit pattern (`RentalDetailScreen.tsx:535,830`), never pinned to a screen's bottom edge.
**Decision `[by-agent]`: leave it alone.** Adding inset padding to a component that floats mid-scroll
would inject a gap in the middle of a form — a regression, not a fix. My Phase-1 brief was wrong to list
it as "fix properly". The BR-4/AC-7 guarantee rides on `BottomActionBar` + `useBottomSpace()`.

**C-4 · The 37 `no-restricted-imports` sites undercount the render surface ~9×.** 37 counts *import
statements*. Actual JSX tags: **343 `<Text>` + 38 `<TextInput>`** app-wide. Per screen: `RentalDetailScreen`
60, `DetailSewaScreen` 53, `PengembalianScreen` 52, `BerandaScreen` 22 … This is the number that sizes
PRD-5, and I anchored on the wrong one. **See the mechanism decision below — it is the whole question.**

### Findings that stand on their own

| # | Finding | Consequence |
|---|---|---|
| **F-1** | **`Text.defaultProps` is definitively dead.** Verified two independent ways: the automatic JSX runtime (`babel-preset-expo`) has **zero** `defaultProps` handling (`react-jsx-runtime.development.js`, no matches), **and** `forwardRef` explicitly rejects it (`react.development.js:1080-1084`). React 19.2.0 / RN 0.83.6. | The internet's standard global-clamp recipe would have failed **silently**. This is exactly what D-0's discovery gate was bought for. |
| **F-2** | **The Ignite UI layer is dead code.** `components/Text.tsx`, `TextField.tsx`, `Header.tsx`, `Button.tsx`, `Card.tsx` have **no production callers** outside `ErrorDetails.tsx`. The app is raw RN primitives + `theme/tokens.ts` + `components/form/*`. | "Put the clamp in the wrapped `Text`" reaches **one file**. Also: `TextField` is *not* a drop-in for the 38 bespoke `TextInput`s — swapping it would be a BR-5 visual regression. |
| **F-3** | **Two parallel theme systems.** `theme/tokens.ts` (what screens use) and `theme/typography.ts` (Ignite side) define **the same 8-entry M3 scale with the same numbers**, independently. | Pre-existing. Any "one shared decision" has to pick a lane. **Not this release's to resolve** — log it. |
| **F-4** | **Token discipline is good.** Only **1 of 88** inline styles sets `fontSize` (`PilihKendaraanScreen.tsx:67`). Three screens hand-inline `fontFamily`+`fontSize: 40` instead of spreading `textStyles.displayLg` (`HutangScreen.tsx:158`, `UserScreen.tsx:235`, `RentalScreen.tsx:182`). | Text *sizing* goes through tokens. It is the *import path* that is undisciplined, not the values. |
| **F-5** | **All bottom padding is hardcoded and mutually inconsistent**, none inset-derived: `RentalScreen` 80, `HutangScreen` 120, `UserScreen` 120, `PilihUser`/`PilihKendaraan` 48, `HutangForm`/`UserForm` 160, `HutangDetail`/`UserDetail` 32, `Beranda` **8**, `RentalDetail` 48, `DetailSewa`/`Pengembalian` a trailing filler `<View style={{height: spacing.xxxl + 64}}/>`. | Two structurally identical tabbed lists differ by 40px *at zero inset*. AC-3's sweep surface, now enumerated with real numbers. |
| **F-6** | **The platform branch is one pattern in exactly four places**, byte-identical: `paddingBottom: Platform.OS === "ios" ? spacing.xl : spacing.base` — `BottomActionBar.tsx:54`, `DetailSewaScreen.tsx:1406`, `PengembalianScreen.tsx:1359`, `RentalDetailScreen.tsx:974`. | Clean substitution target. Inventory otherwise confirmed exactly, including *Simpan Rental* at `DetailSewaScreen.tsx:1018`. |
| **F-7** | **`KeyboardAvoidingView` is a no-op on Android.** Both `DetailSewaScreen.tsx:423` and `PengembalianScreen.tsx:365` set `behavior={Platform.OS === "ios" ? "padding" : undefined}` — and LAVENDER ships Android-only. Current keyboard behavior comes entirely from Android's native window resize. | **AC-6 cannot be settled from source.** Whether adding inset padding produces a floating gap depends on whether `useSafeAreaInsets()` changes while the IME is up — device/OS dependent. **Explicit visual-audit item.** |
| **F-8** | **`app.json:26` sets `edgeToEdgeEnabled: true`** — Expo SDK 55's standard Android posture, and almost certainly the root enabler of the entire PRD-4 defect class. Neither PRD mentions it. | Context, not a decision. Turning it off would be the wrong fix and is not proposed. |
| **F-9** | **`useBottomTabBarHeight` is available** (`@react-navigation/bottom-tabs@7.16.1`). Tab-bar height is computed in exactly one place — `MainNavigator.tsx:19,30`, `height: bottom + 70` — which already folds in the inset, confirming PRD-4's scoping note. | ② should call the hook rather than re-deriving the `+70`. D-2's tabbed/non-tabbed split confirmed structurally via `AppNavigator.tsx:27-52`. |
| **F-10** | **`SearchField.tsx:60-67` has a fixed `height: 48`** wrapping a `TextInput`, and is used by `RentalScreen`, `PilihUserScreen`, `PilihKendaraanScreen`, `UserScreen`. | One fix helps four screens; one miss repeats on four screens. Highest-leverage single target in PRD-5. |
| **F-11** | **~70 fixed-height text containers** across the 14 screens (`height: 40/44/48/52/56` wrapping single-line labels) — the dominant clipping class, well beyond PRD-5's four named defects. `StatusPill` is safe (padding-based). | **This is the real size of PRD-5's sweep.** See the scope question below. |
| **F-12** | **No existing jest pattern mocks `useSafeAreaInsets()` at varying values.** | ②'s AC-5 zero-inset test is new infrastructure, not a reuse. Small, but real. |

### All three flagship PRD-5 defects now have confirmed causal chains

Not "probably" — mechanically traced, which means the fixes can be verified rather than guessed at:

- **Truncated plates** (`PilihKendaraanScreen.tsx:76-78,386-394,413-416`): `flexBasis: "48%"` pins card
  width, `overflow: "hidden"` clips, `numberOfLines={1}` on the plate forces ellipsis once glyph width
  exceeds the column. Complete explanation for `N 2314 A…`.
- **`22 JuliSisa Rp 50.000`** (`RentalScreen.tsx:51-68`, styles `:258-266`): `justifyContent: "space-between"`
  with **no `gap`** and **no `flexShrink`** on either child — and **RN's default `flexShrink` is `0`, unlike
  web CSS's `1`.** At default scale `space-between` distributes leftover slack as a visible gap; when the
  date text grows, leftover slack reaches zero and the two `Text` nodes butt together. This is *precisely*
  the "relies on leftover horizontal slack" anti-pattern PRD-5's constraints name as unacceptable.
- **Clipped `Sewa Baru`** (`BerandaScreen.tsx:316-324`, JSX `:100-115`): `actionBtn: { height: 80 }`
  hard-pinned, label is `headlineSm` (20pt/28 → 30pt/42 at 1.5×), no `numberOfLines`, no `flexShrink`.
- **Headers:** **three-plus separately hand-rolled implementations** with *different* overflow strategies —
  `DetailSewaScreen.tsx:405-414` and `PilihKendaraanScreen.tsx:149-163` wrap (no pinned height), while
  `PengembalianScreen.tsx:369-388` sets `numberOfLines={1}` on **both** title and subtitle (fails by
  truncation instead). **BR-6 has no single fix point** — it is a genuine per-screen sweep.

**Nothing found needs a new native dependency.** `useBottomTabBarHeight`, `useSafeAreaInsets`, and
`maxFontSizeMultiplier` are all already available. OTA-only survives every option below.

---

## ⏸ RE-GATE — the clamp mechanism (PRD-5 BR-8)

Discovery returned three options and correctly declined to pick. **I am adding a fourth**, because the
three on the table all price in a cost I do not think this release has to pay.

### The cost driver, restated

Discovery's headline number is **~343 `<Text>` + 38 `<TextInput>` JSX tags**, and it concluded all three
options cost "~380 call sites". **That is true of Options 1 and 2. It is not intrinsic to the problem.**

It is true of **Option 1** (add `maxFontSizeMultiplier={MAX_FONT_SCALE}` to every tag) because the prop
goes on the tag. It is true of **Option 2** (route through Ignite's `components/Text.tsx`) because that
component has a *different API* — `preset` / `tx` / `text` / `weight` / `size` — so every tag has to be
rewritten to it, and its `TextField` sibling is not a drop-in for the 38 bespoke `TextInput`s at all
(F-2). Both numbers are real.

But the cost is a property of *those two designs*, not of the requirement.

### Option 4 — an API-identical passthrough, imported under the name `Text` `[by-agent, recommended]`

```ts
// app/components/AppText.tsx  — adds one default prop and nothing else
export const Text = forwardRef<RNText, TextProps>(
  ({ maxFontSizeMultiplier = MAX_FONT_SCALE, ...rest }, ref) =>
    <RNText ref={ref} maxFontSizeMultiplier={maxFontSizeMultiplier} {...rest} />
)
```

Because the component keeps **RN's exact props API and RN's exact name**, adopting it in a screen is an
**import-line change**, not a tag change:

```diff
- import { View, Text, StyleSheet, TextInput } from "react-native"
+ import { View, StyleSheet } from "react-native"
+ import { Text, TextInput } from "@/components/AppText"
```

**All 343 `<Text>` tags stay exactly as they are.** The migration surface is **~26 files, ~2 lines each** —
not 380 tags. Same for `TextInput` via a parallel thin passthrough (explicitly **not** Ignite's
`TextField`, which would be a BR-5 visual regression).

| Criterion | How Option 4 scores |
|---|---|
| Reaches every raw site? | **Yes** — the 22 screen-level and 14 form-component import sites all redirect |
| Reaches inline `fontSize`? | **Yes** — `maxFontSizeMultiplier` is orthogonal to styling. And per F-4 there is exactly **1** such site anyway |
| BR-8 durability | **Strong** — one definition, one file. The existing `no-restricted-imports` rule already bans raw `Text`/`TextInput` from `"react-native"`, so a new screen that does the wrong thing **fails lint today**, with no new rule to write |
| BR-5 (default size unchanged) | **Clean** — at scale 1.0 the prop does nothing. No style, no layout, no visual delta |
| Native dep | None |
| **Lint budget** | **Likely negative.** ~22 of the 159 real errors are `no-restricted-imports` in the 14 audit screens. Redirecting those imports **stops the rule firing** — this mechanism plausibly *pays down* debt #6 rather than drawing on it |

**Verify before committing — the three things that would kill it** (briefed to ④ as stop-and-report):

1. Any `<Text>`/`<TextInput>` reached by an aliased or namespace import (`import * as RN`), which an
   import-line redirect would miss.
2. Any site passing RN's `Text` as a *value* rather than rendering it — `Animated.createAnimatedComponent(Text)`,
   a `ref` typed as RN's `Text` instance, `Text.propTypes` — where a wrapper changes the type.
3. Whether nesting a wrapped `<Text>` inside a wrapped `<Text>` still inherits style as RN's does.

If any of those bites broadly, we fall back to Option 1 and the cost question reopens.

### Option 5, considered and rejected `[by-agent]`

Counter-scale the token font sizes: read `PixelRatio.getFontScale()` once and multiply every
`textStyles.*` size by `min(1, MAX/scale)`, so RN's own scaling multiplies back to the cap. It would clamp
**without touching any import**, and F-4 says token discipline is good enough to make it nearly work.

**Rejected.** It fights the platform instead of using the built-in prop; it misses the three screens that
hand-inline `fontSize: 40` (F-4) and any `<Text>` with no explicit size; a module-level `getFontScale()`
read goes stale if Mom changes her text size while the app runs (PRD-5 edge behavior); and it makes the
*stored token values* lie about what they mean, which the next reader has no way to discover. Clever, and
the wrong kind of clever for a file every future screen imports.

### Decisions I am taking without escalating `[by-agent]`

- **`EditActionBar` is left alone** — C-3. It floats mid-scroll; inset padding there would be a regression.
- **The `theme/tokens.ts` ↔ `theme/typography.ts` duplication (F-3) is not this release's to fix.** It is
  pre-existing, it is not a defect, and resolving "which theme system is canonical" inside a P0 layout fix
  is debt #4's mistake wearing new clothes. **Log it as a new debt item on ship.**
- **② calls `useBottomTabBarHeight()`** rather than re-deriving `MainNavigator`'s `bottom + 70` (F-9).
- **AC-6 becomes an explicit visual-audit line item**, not a code claim — F-7 makes it unresolvable from
  source.
- **The `useMemo` hoisting instruction stays, rebriefed as hygiene**, not as a lint requirement (C-1).

### The scope reframe ④ will be briefed on `[by-agent]`

F-11's "~70 fixed-height text containers" reads alarming and mostly is not. **PRD-5 AC-5 requires that
nothing *clips* at 1.5× — not that every fixed height is removed.** A `height: 48` wrapping a 14pt
`labelMd` still holds it at 1.5× (21pt); a `height: 80` wrapping a 20pt `headlineSm` does not (30pt/42
line-height, two lines). So the work is bounded by **what actually clips**, which is discoverable by
audit, not by refactoring 70 containers.

**Guard I am adding:** ④ audits **first**, produces the list of screens that actually fail at 1.5×, and
**reports that list to me before fixing** if it exceeds 8 of the 14. That is the checkpoint that keeps
this from becoming an open-ended refactor without anyone noticing.

### Re-gate resolved — Farrel, 2026-07-22 `[by-Farrel]`

| Decision | Call |
|---|---|
| **D-5 · Clamp mechanism** | ✅ **Option 4 — API-identical passthrough wrapper**, adopted by import-line redirect. Fall back to Option 1 only if one of the three kill-criteria bites broadly, and **come back to Lead** rather than switching unilaterally. |
| **D-6 · Ship shape** | ✅ **PRD-4 and PRD-5 together, as the release plan wrote it.** Considered splitting — discovery showed PRD-4 is far smaller than PRD-5 and Mom's *Simpan Rental* is blocked *now* — and **declined**: one verification event, Mom's attention spent once, and PRD-5 reflowing against a space budget PRD-4 has already settled. The release plan's reasoning survived new information, which is the strongest form of it holding. |

**② dispatched 2026-07-22** — same agent as discovery, context intact.

---

## ② `developer-frontend` #1 — PRD-4 (space) — delivered, then killed on the follow-up

**Delivered green.** Numbers **independently re-verified by Lead**, not quoted from the agent — the
v1.0.3 lesson about a subagent dying mid-task applies, and it applied again here:

| Gate | Result |
|---|---|
| `pnpm test` | **34 suites / 184 tests green** (from 32/174; +2 suites, +10 tests, all new) |
| `pnpm run compile` | clean |
| **Lint** | **159 → 159. +0 exactly** — every rule count identical to baseline, row by row |
| `git diff --stat` | 19 modified + 6 new, **+230 / −39**; **zero** under `app/services/` or `supabase/`; `app.json` untouched |
| `app/config/release.ts` | correctly left for ④ |

**Then it hit an API session limit** (resets 18:40 Asia/Jakarta) on my follow-up and was terminated. It
therefore **did not commit** and **did not answer the Beranda question** (below). Work is intact and
uncommitted on `v1.0.4-fits-moms-phone`.

### The deviation it made, and why I adopted it `[by-agent, ratified by Lead]`

My brief said tabbed screens pad by tab-bar height. **It refused, and was right.**
`@react-navigation/bottom-tabs`' `BottomTabView.tsx:329` already sizes each tab screen's viewport to
*exclude* the tab bar's inset-inclusive height — the bar does not overlay content unless
`tabBarStyle` is `position: 'absolute'`, which it is not here. Padding by `useBottomTabBarHeight()`
would therefore have added **~70px of dead whitespace on every zero-inset device** — a direct
violation of PRD-4 **G4** ("no screen gains dead whitespace on devices that don't need it"), and
arguably of AC-5.

It shipped an **additive delta** instead: `useBottomSpace()` returns
`useBottomTabBarHeight() − TAB_BAR_BASE_HEIGHT` for tabbed screens, which — since `MainNavigator`
sizes the bar as `insets.bottom + 70` — reduces to plain `insets.bottom`, identical to the non-tabbed
branch. Every screen becomes `EXISTING_BASELINE + useBottomSpace()`.

It kept the branch rather than always returning `insets.bottom`, so the hook stays correct if
`MainNavigator`'s formula ever stops being `bottom + constant`. **That is the difference between
following an instruction and understanding what it was for**, and it is the second time this agent
corrected me with evidence.

### What it built

- `app/utils/useBottomSpace.ts` — the shared vehicle. Reads `BottomTabBarHeightContext` directly (a
  plain React context read, so the tabbed branch is testable by wrapping in the real `Provider`, with
  nothing to mock). Returns **`0` at zero inset, tab bar or not** — AC-5/BR-5, unit-tested 5 ways.
- `app/utils/useBottomBarPadding.ts` — `Platform.select({ios: spacing.xl, default: spacing.base}) +
  useBottomSpace()`. Deliberately **separate** from `useBottomSpace` so the platform floor has one
  home, one test, and four trivial call sites.
- `app/navigators/tabBarMetrics.ts` — `TAB_BAR_BASE_HEIGHT = 70` extracted out of `MainNavigator.tsx`
  **specifically to avoid a circular import**: `useBottomSpace` importing `MainNavigator` would pull in
  every tabbed screen, and those screens now import `useBottomSpace`. Good instinct, unprompted.
- `test/mockSafeAreaInsets.ts` — `ZERO_INSETS`, `THREE_BUTTON_NAV_INSETS` (`{bottom: 48}`),
  `mockInsets()`. The reusable helper ④ and ⑤ both inherit.
- Four platform-branch sites replaced; **`EditActionBar` untouched** per C-3; all 14 screens' scroll
  clearance made inset-derived; `LoginScreen`'s accidental all-edges `SafeAreaView` made explicit
  without changing its rendered spacing.

### Its own correction to its own discovery report

While implementing Beranda it found **F-5's "Beranda: 8" was wrong** — it had misattributed a header
block's `paddingBottom: spacing.sm` as the scroll-end value. The real mechanism is a dedicated spacer
`<View style={styles.bottomPadding} />` at **height 32**, after `<VersionFooter />`. It then
**re-verified Rental/Hutang/User against their actual JSX** before trusting 80/120/120 (all three
confirmed).

> That is the second time this release a *checked* number differed from a *remembered* one — mine on
> the lint rule (C-1), its own on Beranda. Both were caught by going back to the source. It is worth
> naming as the release's most reliable habit.

### The jest trap it paid for, recorded so nobody pays twice

**`Platform.select()` cannot be influenced by reassigning `Platform.OS` in a test.** jest-expo's preset
(`haste.defaultPlatform: 'ios'`) loads `Platform.ios.js`, whose `select` is hardcoded to prefer
`spec.ios` and **never reads `.OS` at all** — file-identity dispatch, not runtime-value dispatch. The
only working fix mocks the platform-suffixed submodule by name:

```js
jest.mock("react-native/Libraries/Utilities/Platform.ios", () => ({
  __esModule: true,
  default: { OS: "android", select: (spec) => spec.android ?? spec.default },
}))
```

It also confirmed **by real failure** that mocking the whole `"react-native"` package via
`{...jest.requireActual("react-native"), Platform: …}` **crashes** — spreading forces every lazy getter
on RN's `index.js` to evaluate eagerly, and `DevMenu` throws under jest. No prior test depended on
`Platform.select` output, so this was invisible until now. **Debt-register item.**

---

## ③ `rental-math-reviewer` — fence check: **PASS**, and Lead closed its residual gap

**Verdict: PASS.** No blocking finding. Work did not go back to ②.

### It reported its own methodology limit instead of hiding it

The reviewer subagent has **no Bash tool**, so it could not run `git diff`. Rather than issue a
confident PASS it could not back, it **reconstructed** the diff from an independent pre-change record —
the `file:line` landmarks ① wrote into this report *before any code existed* — cross-checked math
content against the months-old copies in `.worktrees/feat-v1-0-1-phase-3/`, and then **stated exactly
what its method could not exclude**: an equal-line-count, content-only edit in an unlandmarked region,
naming `PengembalianScreen.tsx:321-334`'s `CloseRentalInput` literal as the specific place (it
post-dates the worktree copy, since `tujuan` arrived in v1.0.3).

It then asked Lead to close that with one command.

### Lead closed it `[by-agent]`

`git diff -U0 master --` on both files. **The complete change to both fenced files:**

```
+import { useBottomBarPadding } / { useBottomSpace }     (2 lines)
+  // PRD-4 comment                                      (3 lines)
+  const barPadding = useBottomBarPadding()
+  const bottomSpace = useBottomSpace()                   (2 lines + 1 blank)
-  <View style={{ height: spacing.xxxl + 64 }} />
+  <View style={{ height: spacing.xxxl + 64 + bottomSpace }} />
-  <View style={styles.bottomBar}>
+  <View style={[styles.bottomBar, { paddingBottom: barPadding }]}>
-    paddingBottom: Platform.OS === "ios" ? spacing.xl : spacing.base,
```

**8 added, 1 removed, 2 modified, per file. Nothing else.** No calculation, no value, no clamp, no
connector call, no argument order, no string, no dependency array, no early return. The residual
uncertainty is now closed to certainty, and the reviewer's reconstruction was accurate in every
particular.

> **This is the pattern worth keeping.** The reviewer did not pretend to a capability it lacked; it
> reported its method, quantified what the method proved, named the exact gap, and handed the gap to
> the one role that could close it. Compare v1.0.3's §①c, where a verification script *claimed* to
> prove something it did not. The difference is entirely in whether the limit is stated.

### Findings it added

- **Claim 2 was verified as *better* than reported.** ② said the filler became `112 + useBottomSpace()`.
  It did **not** substitute a literal — it kept `spacing.xxxl + 64` and appended `+ bottomSpace`. My
  arithmetic concern (does `112` really equal `spacing.xxxl + 64`?) is moot because no literal was
  introduced. `spacing.xxxl` is 48. **The code is safer than the report describing it.**
- **Hook placement clean.** Both hooks are unconditional at the top of the component, above every
  `if (loading)` early return. No rules-of-hooks hazard — a real risk with new hook call sites in a
  large component, and the reason I briefed for placement rather than presence.
- **KAV asymmetry confirmed *unchanged*, not merely "left alone".** Those are different claims and only
  the second is verifiable; the reviewer verified the second.
- **Neither fenced screen has any unit test.** `DetailSewa*.test.tsx` and `Pengembalian*.test.tsx` do
  not exist. **This review was the only gate those two files have** — worth stating plainly, since it
  makes the visual audit and this review load-bearing rather than ceremonial.

### 🔎 Two pre-existing rental-math ↔ `docs/02` §6 divergences — NOT this release's, but real

Byte-identical to the v1.0.1-era copy, so outside this release's blame. Surfacing because `docs/02` §6
is the document that says this math **must be correct**, and a doc that disagrees with shipped code is
the condition under which someone eventually "fixes" the wrong one.

1. **`PengembalianScreen.tsx:306-313`** — `applyFuelSuggestion()` appends an extra fee line item
   `{description: "Bensin"}` rather than modifying Subtotal Sewa. §6 says explicitly
   *"satu arah ke subtotal, **bukan baris terpisah**"* (`docs/02:124`) and *"Menekan tombol mengubah
   nilai Subtotal Sewa"* (`:130`). **Arithmetically equivalent** — `computeReturnTotal` sums extras —
   so there is **no money impact**. But it is not what the spec describes.
2. **`PengembalianScreen.tsx:663-669`** (styles `:1229,1237-1238`) — the fuel suggestion row renders
   `warningContainer`/`onWarningContainer` (amber) **unconditionally**. §6 (`:127-128`) calls for green
   when fuel is returned in excess (reduce) and amber only when short.

**Neither is taken into v1.0.4** `[by-agent]` — the freeze binds, both are cosmetic-or-equivalent, and
neither is live harm. **Both go to the debt register on ship**, with the question stated as *"which is
wrong, the code or §6?"* rather than assuming the code is.

---

## 🔴 Open blocker — the Beranda AC-3 discrepancy

②'s `BottomTabView` finding has a tail it was killed before following, and it lands on a flagship
criterion.

**If the tab bar does not overlay content** — ②'s own verified premise — then a tabbed screen's content
already ends above the tab bar, which already clears the system nav. **In that case the tabbed screens
never had a PRD-4 overlap defect at all.** That contradicts the reported symptom:

> PRD-4, problem statement, third bullet: `docs/mom-ss/sewa-baru-text-too-big.jpeg` — *"Beranda. The
> last summary card's line ('0 pelanggan') is clipped by the tab bar; the list cannot scroll far enough
> to reveal it."*

That is **AC-3 and user flow 3**, from one of only three screenshots Mom actually sent.

| | Reading | Consequence |
|---|---|---|
| **(a)** | The tab bar genuinely doesn't overlay | The clipping has a **different cause**, and ②'s `+insets.bottom` **does not fix it.** AC-3's flagship case ships broken and we learn that on Mom's phone, after publish |
| **(b)** | It does overlay in this configuration | A delta of `insets.bottom` alone is **insufficient**; Beranda needs the tab bar's full height cleared |

**Lead's hypothesis, explicitly unverified** `[by-agent]`: this may be a **PRD-5 defect misfiled as
PRD-4** — at Mom's enlarged text size the summary card's content outgrows the card's own fixed height
and clips its last line *inside the card*, nothing to do with the tab bar. Product read the screenshot
and attributed it to the nearest visible cause. If so, ④ closes it and the release still delivers
AC-3 — but **routing it deliberately matters**, which is why ② was told to diagnose and *not* fix.

**Nobody in this chain has looked at the actual pixels.** ①, ②, ③ and Lead have all worked from my
prose and the PRD's. That is exactly the condition under which a misattribution survives a whole
release.

### ✅ RESOLVED — and the answer is that **PRD-4's third bullet is not a defect** `[by-agent]`

A narrow read-only diagnostic was dispatched with one instruction: **look at the pixels first.** It did,
zooming 3× on the cut line. The answer is **(a)**, and it goes further than (a).

**What the screenshot actually shows.** `sewa-baru-text-too-big.jpeg` is captured at **scroll offset ≈ 0**
— the entire non-sticky header ("Halo!", the date, both quick-action buttons, the whole "Harus Kembali
Hari Ini" section) is rendered, which is only possible if the ScrollView **has not been scrolled**. The
descenders of the two "g"s in "0 pelanggan" are sliced exactly where the card's white background meets
the tab bar's pale gray-blue — **no card border anywhere near the cut**, meaning the card's real bottom
edge is simply further down, off-screen. Below the cut: the tab bar's icons, fully legible and
unobstructed; a hairline divider; then the system nav icons, also unobstructed.

**That is a ScrollView viewport edge. It is a fold, not a clip.**

**And the comparison is what makes it conclusive.** The other two screenshots show what an overlay
actually looks like: in `android-nav-blocking-on-sewa-baru-screen.jpeg` the recents and back icons are
drawn *literally on top of* the "Simpan Rental" glyphs; in `…-pilih-kendaraan-screen.jpeg` the nav row
sits flush against the bottom card row with no reserved gap. **Neither pattern appears in the Beranda
image.** The defect is real and urgent on those two screens. It was never present on this one.

**It corrected ② on the evidence, and me on the hypothesis:**

- **②'s citation was not load-bearing.** `BottomTabView.tsx:329` only sets a *context value* for
  `useBottomTabBarHeight()` consumers. The structural proof is upstream: the navigator root is
  `flexDirection: 'column'` (`:242-249`), the screens container is `{flex: 1, overflow: 'hidden'}`
  (`:254-259, 362-365`), and the tab bar renders as the **next sibling** in normal flex flow
  (`:354-357`) — not absolute, not a portal, not z-stacked. `MainNavigator.tsx:31`'s `$tabBar` carries
  no `position: "absolute"`. **Non-overlap is a flexbox guarantee, independent of what the height
  computes to.** ②'s conclusion was right; its evidence was one layer off.
- **My "fixed-height card" hypothesis was wrong**, and it checked rather than confirmed. `statCard`
  (`BerandaScreen.tsx:385-392`) uses **`minHeight: 128`, not `height`**, has no `overflow: 'hidden'`,
  and the `"{count} pelanggan"` Text (`:224-226`) has **no `numberOfLines`**. Nothing there can clip.
  The pixels agreed — no card border at the cut.
- **`edgeToEdgeEnabled: true` is relevant, but to the *other two* screenshots.** Those are non-tab
  screens with no sibling reserving `insets.bottom`, so Android paints the system bars over full-bleed
  content. Beranda's tab bar already folds `insets.bottom` into its own height and is not subject to
  that failure mode. This is the cleanest statement anyone has produced of *why PRD-4 is real* — and it
  arrived while disproving one of its three examples.

### Consequences `[by-agent]`

1. **②'s Beranda change is not wrong, and is not reverted.** It extends the trailing spacer *after*
   `<VersionFooter />`, so the true last item clears the tab bar when scrolled to the end. That is a
   real, separate problem (BR-2/AC-3), correctly solved. It is simply **orthogonal** to the symptom
   Product attributed to it — "0 pelanggan" is in the 2nd of 4 stat cards, nowhere near the trailing
   spacer.
2. **No fix is owed by ② or ④.** The routing question that blocked ④ is answered: **neither.** This is
   why ② was told to diagnose and not fix.
3. **Product owes PRD-4 an amendment.** The third problem-statement bullet ("clipped by the tab bar; the
   list cannot scroll far enough to reveal it") is a **misattribution**, and its "cannot scroll far
   enough" half is not demonstrated by a frame in which scrolling was never attempted. Same shape as
   v1.0.3's PRD-1 BR-5/AC-5 amendment: **the PRD must be corrected, not quietly worked around.** Logged
   as a follow-up, not a blocker.
4. **AC-3 narrows but does not weaken.** It still binds on every non-tabbed screen, where the overlap is
   real and photographed. What changes is that Beranda cannot be used as evidence for or against it.
5. **One line is added to the visual-audit checklist** — the honest residual, which the diagnostic
   flagged itself rather than papering over: *on Mom's device at her font scale, scroll Beranda to the
   very bottom and confirm all four stat cards plus the footer are reachable.* The code says they are.
   A static image cannot prove it, and **saying so was the right answer.**

> **The lesson, and it is the release's sharpest.** Four agents and Lead reasoned about this defect for
> an entire release from **prose descriptions of an image nobody had opened.** The description was
> Product's good-faith reading, it was wrong, and it propagated unchallenged through discovery, planning,
> implementation, and review — because every downstream step took it as given. **The fix cost one
> instruction: "look at the pixels first."** Mom's larger font size does plausibly push the stats grid
> further down and make her scroll more — so her *report* was real. What was wrong was our diagnosis of
> it, and no amount of source reading would have caught that.

---

## Post-execution state

*(filled after execution)*

## What happened during execution

*(filled after execution)*

## Decisions taken

*(filled after execution)*

## Bottlenecks

*(filled after execution)*

## Tech debt

*(filled after execution)*

## Best practices applied

*(filled after execution)*

## Agent / workflow improvements

*(filled after execution)*
