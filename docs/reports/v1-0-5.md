# v1.0.5 — Release report

- **Release plan:** `docs/releases/v1-0-5.md`
- **Requirements:** `docs/prd/PRD-8-editable-vs-readonly.md` (BR-1…BR-12, AC-1…AC-12), read together with
  the release plan's **AC-4 split** (AC-4a / AC-4b / AC-4c) — see **D-5** below.
- **Implementation plan:** *not yet written* — see **D-0**. Lead proposes writing it **after** a discovery
  pass, and says why.
- **Lead sessions:** **Opus 5, xhigh** throughout (Fable not selected — sanctioned fallback per
  `docs/agents/lead.md`; flagged to Farrel at the start of each session).
  **S1** (2026-07-25) — Phase 1, gate, ①, re-gate. **S2** (2026-07-25, resumed from `AI_Continuation_Document-25Jul2026-1808.md`) — ② / ②b / ③ / ②c, then the halt.
  **S3** (2026-07-26, resumed from `AI_Continuation_Document-26Jul2026-1424.md`) — halt lifted; ④ onward.
- **Status:** ▶ **Phase 2 — running.** Plan approved `[by-Farrel]`; ① / ② / ②b / ③ / ②c all returned
  green. **AC-9's "before" state is final at `49fae63`** (52 suites / 283 tests, lint 122 `+0`, `tsc`
  clean). **④ dispatched 2026-07-26** — the first dispatch that touches `app/`.
  See the **Phase 2 — execution log** at the foot of this report for the running record.

---

## Pre-execution state

Measured this session, 2026-07-25, by running the commands — not quoted from the release plan.

| Fact | Value |
|---|---|
| Branch | `master` |
| Working tree | **not clean** — 2 modified + 4 untracked, **all docs** (see below) |
| HEAD | `0c1293c docs(v1.0.4): close the release paperwork — debt register, PRD-4 A-1, status truth` |
| Test baseline | **50 suites / 237 tests, all passing** (25.6s), run from `apps/lavender-ops-mobile` |
| **Lint baseline (debt #6 obligation — discharged)** | **122 real (non-`prettier`) errors** on `master` |
| Lint total | 29,679 errors, of which **29,557** are phantom `prettier/prettier` CRLF noise |
| Migrations | **none opened by this release** (guard 5, BR-9, AC-11) |
| Subagent models | `developer-frontend` · `tester` pinned `sonnet` (available) · `rental-math-reviewer` inherits · `developer-backend` **not needed** |

### The working tree is dirty, and it is dirty with this release's own requirements

```
 M docs/known-technical-debt.md          M docs/prd/PRD-1-edit-active-rental.md
?? docs/prd/PRD-6-…  ?? docs/prd/PRD-7-…  ?? docs/prd/PRD-8-…  ?? docs/releases/v1-0-5.md
```

**PRD-8 and the release plan — the two documents this entire release is accountable to — are untracked.**
Debt **#13** says `git diff` is blind to untracked files and the release plan flags it as applying
*acutely* here. It applies to the requirements themselves before it applies to a single test file.

**Recommendation `[by-agent]`: one docs commit on `master` before branching**, covering all six paths
(PRD-6, PRD-7, PRD-8, the release plan, the debt-register edits, the PRD-1 edits). Then cut the branch from
a clean tree. Otherwise every `git diff` scope proof in this release is measured against a baseline that
already contains uncommitted work, and the AC-9 "before" state has no commit to point at.

### The lint baseline, recorded properly — and it moved in our favour

```powershell
# from apps/lavender-ops-mobile — note: NOT `pnpm run lint`, which auto-fixes
$j = npx eslint . --format json | ConvertFrom-Json
$msgs = $j | ForEach-Object { $_.messages } | Where-Object { $_.severity -eq 2 }
$msgs | Where-Object { $_.ruleId -ne 'prettier/prettier' } | Measure-Object
```

**Baseline: 122.** Breakdown, against v1.0.4's measured 159:

| Count | Rule | v1.0.4 | Δ |
|---|---|---|---|
| 88 | `react-native/no-inline-styles` | 88 | — |
| 15 | `react-native/no-color-literals` | 15 | — |
| 12 | `react-native/sort-styles` | 12 | — |
| 3 | `react-native/no-unused-styles` | 3 | — |
| 3 | `@typescript-eslint/no-unused-vars` | 3 | — |
| 1 | `import/no-unresolved` | 1 | — |
| **0** | **`no-restricted-imports`** | **37** | **−37** |

**Two findings fall straight out of this, and both matter to the briefs.**

**1 · v1.0.3's breach ledger row is repaid, incidentally.** Debt #6's ledger carries one row — v1.0.4-era
status *"Repaid? No — open"* — for a raw `TextInput` import in `RentalDetailScreen`
(`no-restricted-imports`, **+1**). That rule now measures **0 app-wide**, so no file imports raw
`Text`/`TextInput` from `"react-native"` anywhere: v1.0.4's `AppText` passthrough (its D-5, Option 4) paid
the row off as a side effect nobody claimed at the time. **Cumulative accepted increase is now 0.**
Recommend recording that in the ledger at ship `[by-agent]`.

**2 · The hazard rows have rotated, and the new one is sharper.** v1.0.4's threat was
`no-restricted-imports` at 37. This release's threats are the three rules a **new shared primitive** trips
by default:

- **`no-color-literals` (15)** — a field box whose border or fill is written `"#7a7580"` / `"#f6faff"`
  instead of `colors.outline` / `colors.surface` is a new error **and** a BR-3/BR-6 violation. Same defect,
  caught twice.
- **`sort-styles` (12)** — new `StyleSheet.create` blocks must be alphabetically ordered or they add errors.
- **`no-restricted-imports` (now 0)** — **this is the trap.** At 37 it was background noise; at 0 it is a
  tripwire. Any `TextInput` imported from `"react-native"` in this release is a **brand-new** error against
  a **+0** budget. Everything must come from `@/components/AppText`. Both developer briefs carry this.

### Audit surface — enumerated now, so nobody samples later

**~~Five screens~~ SIX FILES are touched — amended at the re-gate, RG-1 `[by-Farrel]`. Nothing else is.**

| File | Treatment | ACs |
|---|---|---|
| `PengembalianScreen` | **apply + D-5 migration** (input-bearing primitives only) | AC-1, AC-2, AC-9, AC-10 |
| `UserFormScreen` | apply — treatment #8 → the box | AC-3 |
| `HutangFormScreen` | apply — treatment #8 → the box | AC-3 |
| `RentalDetailScreen` | strip + 2 inputs boxed | AC-4a, AC-4c, AC-11 |
| `UserDetailScreen` | audit only — must contain no box | AC-5 |
| **`PembayaranSheet` (component)** | **apply — 3 Fields → the box (RG-1)** | **BR-7**, AC-7 |

> **`PembayaranSheet` is a shared component, not a screen.** It is consumed by `RentalDetailScreen` and
> `PengembalianScreen` (in scope) **and** by `HutangDetailScreen` and `DetailSewaScreen` (out of scope).
> Those two out-of-scope screens **change appearance inside the sheet while their source changes by zero
> lines.** That is not a fence breach — D-6 fences `DetailSewaScreen`'s own primitives and tariff math — but
> it *is* the reason the visual checklist must cover the sheet as opened from `HutangDetailScreen` too.

**Out, and named as out** (so the AC-7 allow-list can be checked against a fixed list rather than a
judgment): `DetailSewaScreen` (D-6) · `LoginScreen` (D-7) · `HutangDetailScreen`, `RentalScreen`,
`HutangScreen`, `UserScreen`, `PilihUserScreen`, `PilihKendaraanScreen`, `BerandaScreen` (read-only +
`SearchField`, a Control) · `KendaraanScreen` (dead code, debt #3 — renders no `TextInput`, so it is
outside AC-7's surface by construction, per the release plan's triage row 3).

**Delivery shape:** OTA only. No migration, no RPC, no RLS, no Edge Function, no native dep; `app.json`
`version` stays `1.0.0`; `app/config/release.ts` `RELEASE` → `"1.0.5"`. Rollback is a single
`eas update:list --branch preview` republish.

---

## Decisions owed before dispatch

The release plan and PRD-8 leave six things to Lead and two to Farrel. Resolving them here so the briefs
are unambiguous. **Each is marked with who I think owns it.**

### D-0 · No implementation plan yet — discovery first, and here is the specific reason `[by-agent]`

`docs/releases/v1-0-5.md` says the implementation plan is *"not yet written — owned by Lead."* I am
proposing we deliberately not have one at dispatch time, exactly as v1.0.4 did, and for a sharper reason
than v1.0.4 had.

**The release's largest line item is a test suite whose feasibility is unverified.** BR-12 requires
characterisation tests pinning `PengembalianScreen`'s current fuel-adjustment, return-total and auto-debt
behaviour, green **before anything moves**. But that screen has **zero** automated coverage (debt #15) and
is a 50KB component. So the suite is one of two completely different shapes:

| Shape | What it costs | What it requires |
|---|---|---|
| **Pure-function tests** — if the math lives in importable functions | Small, fast, precise | The math is *already* extracted |
| **Render-level tests** — mount the screen, drive inputs, assert displayed numbers + the payload handed to the connector | Large; needs connector, navigation, safe-area, image-picker and date-picker mocks | Nothing — but it may not be *possible* |

**And there is a circularity nobody has named yet.** If the math is inline in the component, making it
unit-testable means *extracting* it — which is product-code change on a fenced rental-math screen,
performed **before** the characterisation tests that are supposed to protect that screen exist. That is
the exact ordering BR-12 forbids. If render-level mounting also turns out infeasible, **the release as
scoped has no legal first step**, and that is a fact worth discovering in a read-only dispatch rather than
three dispatches deep.

So: **① is a read-only discovery pass that proves mountability by trying it** — a throwaway spike that
renders the screen and asserts one visible string. Evidence, not belief. This project's most reliable
habit is going back to the source (v1.0.4 C-1, and the `new Error(...)` mock lesson from v1.0.3); its most
expensive habit is reasoning about an artefact nobody opened (PRD-4 A-1). The implementation plan gets
written at the re-gate, with facts in it.

### D-1 · PRD-8 OQ-1 — the field box's fill → **`surface` `#f6faff`** `[by-agent]`

The plan hands this to Lead. Taking `#f6faff` (via the token, never the literal):

- It measures **4.28:1** with `outline` — clears BR-3's 3:1 with margin. Either candidate passes, so this
  cannot break the convention (PRD-8 §Contrast measurement).
- It is what treatments 2, 3 and 4 **already use**, so the diff is "correct the border token", not
  "restyle the field". Smaller diff, smaller review surface, less to go wrong on a fenced screen.
- White-on-white would make the box's presence depend **entirely** on one 1px border. `#f6faff` gives the
  signal two independent carriers, which is the right bias for a 50-year-old primary user at arm's length.

### D-2 · PRD-8 OQ-2 — the *Kembali* row: **box it, and keep the `inlineEditBtn`** `[by-agent]`

It is a Field under BR-4, so it wears the box. On the button, I am keeping both, deliberately:

1. **The box says "changeable"; it does not say "how".** A date/time is not typed — it opens a picker. The
   box makes the row look editable without telling Mom that tapping it does something. The explicit
   `inlineEditBtn` (16px icon + `labelLg` "Edit") is the only thing on the row that currently does.
2. **Debt #16 lives on this exact row.** It already renders a confident wrong default that has produced one
   wrong persisted record, and *reducing* that row's affordance while the value defect is unfixed is
   exactly the wrong direction. Debt #16's own note is blunt about it: *"a prettier wrapper around a
   confident wrong answer is not progress on this item."*
3. **Making the box itself tappable is a behaviour change**, not styling, and guard 1 draws the line at
   styling. So: box the row, keep the button, change no interaction.

> ⚠️ **Guard 1 goes into ⑤'s brief verbatim.** `returnedAt`'s `new Date()` seed is **not touched**. This is
> the release plan's own nomination for "the single most likely misread available here."

### D-3 · PRD-8 OQ-3 — `extraFeeDesc`: **box it**, and AC-1 already decided this `[by-agent]`

The plan calls this open-but-not-blocking. It is closed by AC-1's own wording: *"every extra-fee
description and amount"* renders in the field box. Keeping the underline would fail the flagship criterion.
The vertical-space concern at `fontScale` 1.5 is real and is handled by BR-8's `minHeight` + PRD-5 growth —
which is AC-10's job to re-verify. Recording it as decided rather than leaving a Field with a "maybe".

### D-4 · PRD-8 OQ-4 — characterisation depth: floor, shape, and who writes it `[by-agent]`

**Who: the `tester`, not a developer.** BR-12's suite exists to constrain the migration; an agent that
writes the constraint and then satisfies it has pinned its own output. OQ-4 names *"Lead / Tester"* as the
owner, which agrees. **The migrating developer may run the suite and may not edit it** — that instruction is
in ⑤'s brief in those words.

**Floor (BR-12's, not negotiable):** fuel adjustment in **both directions across zero**, return total,
`Sisa`, auto-debt creation at close.

**Shape I am adding `[by-agent]` — pin the payload, not just the pixels.** The suite must assert the
**arguments handed to the connector at close**, not only the numbers rendered on screen. v1.0.3's
`deleted_at` regression is the precedent: *the screen showed the correct `sisa` and the server wrote the
wrong hutang.* A display-only characterisation suite would have been green through it.

**And it must pin the two known divergences as-is.** Debt #12's two `docs/02` §6 mismatches both live in
this screen — the fuel suggestion writes an extra-fee line instead of adjusting Subtotal, and the
suggestion row renders amber unconditionally. Guard 3 forbids fixing either. **AC-9 demands identical
before/after, so the suite pins current behaviour warts and all.** Both ② and ③ are briefed that these two
are known, declined, and must be *preserved*, or ③ will return a finding guard 3 forbids anyone acting on.

### D-5 · The AC-4 amendment needs Product to write it `[needs Farrel]`

The release plan splits AC-4 into 4a/4b/4c and says plainly: *"This belongs in PRD-8 as an amendment, the
way PRD-4 A-1 was recorded. Product's call to write it; the release does not get to amend a PRD by
itself."* I agree, and there is an operational reason beyond bookkeeping:

**The tester derives "correct" from the PRD, not from the implementation** — that instruction is in every
tester brief this project writes. A tester reading AC-4 as currently written will see `kmEditInput` and
`notesInput` wearing boxes and **correctly report a failure**. The amendment is what stops a true test from
producing a false verdict.

**Recommendation:** a short `/product` session writes **PRD-8 A-1** (the AC-4 split, verbatim from the
release plan's table) at any point before ⑦ dispatches. It blocks nothing earlier — Lead's briefs carry the
operative table meanwhile. **Farrel's call on when.**

### D-6 · The lint ceiling's arithmetic is now ambiguous, and the ambiguity is worth 47 errors `[needs Farrel]`

v1.0.4 **D-4** set the ceiling as *"+10 cumulative (i.e. 169 real errors)"* against a baseline of 159. The
baseline is now **122**. Read as an absolute **169**, the ceiling silently grants **+47** of headroom that
nobody voted for — on the release most likely to add color literals and unsorted styles.

**Recommendation `[by-agent]`: restate the ceiling as "lowest measured baseline + 10", i.e. 132 today, and
let it ratchet down as the baseline improves.** That is what "+10" meant. **This release is briefed at +0
regardless**, so the restatement costs nothing now and prevents a slow leak with a paper trail later —
which is debt #6's own stated failure mode. **Farrel's call**; it is his number.

### D-7 · AC-7 needs *two* lists, not one — and the gate as written can't accommodate the second `[by-agent]`

A specification gap, found while planning, worth catching before the tester hits it:

- **AC-7** audits that no `<TextInput` under `app/` renders outside a field box, *"except for an explicit
  allow-list"* starting as `LoginScreen` + `DetailSewaScreen`.
- **The release gate** says: *"AC-7's allow-list is exactly `LoginScreen` + `DetailSewaScreen`. Any third
  entry is a scope breach, not a test detail."*
- **But `SearchField.tsx` renders a `<TextInput`** and is a **Control** under BR-4 — explicitly *exempt*
  from the box and explicitly *unchanged* by this PRD. It is in neither list, and adding it to the
  allow-list would trip the gate as a "scope breach" when it is nothing of the kind. `Stepper` may be in
  the same position; ① enumerates.

**Resolution `[by-agent]`: the audit carries two lists with different meanings, and they are not
interchangeable.**

| List | Meaning | Contents | Gate |
|---|---|---|---|
| **Control exemption (BR-4)** | Not a Field. Its shape already declares interactivity; the box does not apply | `SearchField`, plus whatever ① finds | Fixed by BR-4. Each entry names *why it is a Control* |
| **Allow-list (AC-7)** | **Is** a Field, deliberately unboxed this release | `LoginScreen` (D-7), `DetailSewaScreen` (D-6) | **Exactly two.** A third is a scope breach, as the gate says |

The gate's sentence stands unweakened — it just applies to the list it was written about. **① fixes both
lists before ⑦ writes the audit**, so neither is assembled by judgment mid-test.

### D-8 · The hardest task in the release runs on the smallest model `[needs Farrel — information, not a recommendation]`

Writing a render-level characterisation suite for a 50KB uncovered rental-math screen is the highest-skill
task here, and `tester` is pinned `sonnet` in `.claude/agents/tester.md`. I am **not** recommending a
substitution — the ladder in `docs/agents/lead.md` is explicit that a bigger model is not a fix for a
developer-tier agent, and the pin is deliberate.

My mitigations, which I think are adequate: ① enumerates the seams **before** ② writes anything, so the
suite is written to a map rather than discovered; ③ independently reviews the suite against `docs/02` §6
before it is trusted; and **Lead re-runs the suite and reports the numbers rather than quoting the agent**
(the v1.0.3/v1.0.4 lesson about subagents dying mid-task).

**Stating it because it is the release's biggest quality unknown and it is Farrel's to overrule.** Changing
the pin means editing `.claude/agents/tester.md` **and restarting the session** (new agent files register
only on restart), which would cost this plan.

---

## Execution plan (awaiting approval)

### What is *not* in this release

- **No `developer-backend`.** Zero server surface — BR-9, AC-11, guard 5. Second release in a row with no
  backend leg.
- **No connector signature to broker.** No connector changes. What I *am* brokering is a **component
  contract** between the two frontend dispatches — see below. (Per `docs/agents/lead.md`'s new-contract
  bootstrap note: the primitive must exist before its consumers can typecheck, so ④ → ⑤ is sequential by
  necessity, not by preference.)
- **No `connector-contract-reviewer`.** Its rules have no surface here; nothing under `app/services/` is
  touched. Cheaper and more direct: proven by `git diff --stat` showing **zero** changes under
  `app/services/` and `supabase/`, plus ⑥'s explicit "no connector call moved" check. Recorded as a
  deliberate omission, as v1.0.4 did.
- **No PRD-6, no `DetailSewaScreen`, no `returnedAt` fix, no debt #12 "fix", no repo-wide lint fix.**
  Guards 1, 3, 6, 7, 8 — carried verbatim into the briefs that could plausibly reach them.

### Sequencing

```
  ①  tester                DISCOVERY — read-only on product code. Mountability spike.
        │                  Is the BR-12 suite pure-function or render-level? Is it possible at all?
        ▼
     ⏸  RE-GATE — Lead reports to Farrel; suite shape locked; implementation plan written (D-0).
        │           NO PRODUCT CODE BEFORE THIS. If ① says "not mountable", this is where we choose
        │           between the release plan's short option and a reviewer-gated extraction.
        ▼
  ②  tester (cont.)        The BR-12 characterisation suite, on UNTOUCHED master. Green.
        │                  → committed as its own commit before any product code moves (AC-9's "before")
        ▼
  ③  rental-math-reviewer  Reviews THE SUITE against docs/02 §6: does it pin the right things?
        │                  A suite that pins the wrong behaviour makes AC-9 vacuous.
        ▼
  ④  developer-frontend #1 Ride-along (footer + RELEASE) → the field-box primitive
        │                  → UserFormScreen + HutangFormScreen (AC-3). Low risk, high signal.
        ▼
  ④b developer-frontend #1 The strips: RentalDetailScreen AC-4a + AC-4c, UserDetailScreen AC-5.
        │                  (same agent, context intact, via SendMessage)
        ▼
  ⑤  developer-frontend #2 The PengembalianScreen D-5 migration (AC-1, AC-2). Fresh agent.
        │                  Runs ②'s suite before AND after. Any delta = STOP, do not edit the test.
        ▼
  ⑥  rental-math-reviewer  AC-9 post-migration pass + fence check on ⑤'s diff.
        ▼
  ⑦  tester (fresh)        Durability layer AC-6/AC-7/AC-8 (mutation-verified), acceptance ACs,
        │                  full suite, lint re-count, v1.0.5 visual-audit checklist.
        ▼
     ⏸  PUBLISH GATE — Farrel. Then OTA → Mom relaunches → combined visit (OQ-5): AC-12 **and**
                       v1.0.4's outstanding AC-8 rows, recorded as two separate verdicts.
```

**Why this order** — it is the release plan's binding order, with two additions I am accountable for:

- **③ is mine `[by-agent]`.** The plan requires a `rental-math-reviewer` pass *after* the migration. I am
  adding one *before*, reading the suite rather than the code. Cost: one read-only dispatch. Buys: AC-9
  cannot be satisfied by a suite that pins the wrong thing, and the two debt-#12 divergences get confirmed
  as *deliberately preserved* rather than discovered as findings at ⑥ when it is too late to be cheap.
- **④b is a continuation, not a new dispatch.** The strips are small and subtractive, and they benefit from
  an agent that just built the primitive. Splitting them off would re-derive context for no gain.

**Why a fresh agent for ⑤ `[by-agent]`:** `PengembalianScreen` is 50KB and it is the release's only
dangerous file. It gets an agent's full context budget and nothing else. This is v1.0.4's ②/④ split
applied for the same reason, and there it was the right call.

**Why a fresh tester for ⑦ `[by-agent]`:** ①/② wrote the characterisation suite. ⑦ audits the
*developers'* work against the PRD, including AC-1/AC-2 on the screen ②'s suite protects. Different
question, and it should not be answered by the agent whose own output is part of the evidence.

**Branch `[by-agent]`:** `v1.0.5-which-fields-you-can-change`, cut from `master` **after** the docs commit.

### The contract I am brokering

**Provisional — confirmed or amended at the re-gate.** Locked from that point: **④ creates it; ④b and ⑤
consume it; nobody redeclares it without coming back to me.**

```ts
// app/components/form/FieldBox.tsx — the ONE definition site (BR-6, AC-6)

/**
 * The editable-field container. Wraps any input or picker row.
 * Boxed = Mom can change this. Plain = she can't. Two states only (BR-10/D-8).
 */
export function FieldBox(props: { children: ReactNode; style?: StyleProp<ViewStyle> }): JSX.Element
```

Five invariants, worth more than the name:

1. **Exactly one declaration** of the style set (border colour + fill + radius + `minHeight`) under `app/`.
   AC-6 is a source audit that fails if a second appears. **The trap is `RupiahInput`** — it declares
   treatment #2 today, and after this release it must **consume** the box, not carry its own copy. Same for
   every local style in `PengembalianScreen`. A consumer that needs a variant *composes*; it does not
   redeclare.
2. **`minHeight: 52`, never `height`.** BR-8, AC-2, and it closes the two live PRD-5 BR-1 violations at
   `PengembalianScreen:1203` / `:1221` — one of which is a **sub-48dp tap target on the money field**.
3. **Tokens only — no colour literal anywhere.** `colors.outline` for the border (retiring
   `outlineVariant` from field borders per BR-3), `colors.surface` for the fill (D-1),
   `borderRadius.default`. This is simultaneously BR-3/BR-6 compliance and the `no-color-literals` lint gate.
4. **It is a container, not an input.** It must wrap a `TextInput`, a date/time row, or a rupiah row without
   imposing an API on any of them — that is what keeps `Stepper` / `PhotoRow` / `SearchField` (Controls,
   BR-4) genuinely untouched instead of "refactored while we were in there".
5. **It carries its own doc comment** stating the convention, BR-4's three categories, the retired
   `outlineVariant`, the demoted tint, and the two known exceptions. BR-11 requires this **in the primitive
   itself** as well as in `docs/` — an undocumented convention decays back into ten treatments.

### ① `tester` — DISCOVERY — model `sonnet`

**Read-only on product code. Change no file under `app/`.** You may write **one throwaway spike test**; say
so explicitly and we will decide whether it stays. Answer with `file:line` evidence:

1. **The decisive question: can `PengembalianScreen` be mounted in jest at all?** *Prove it, do not reason
   about it.* Write a spike that renders it and asserts one visible string. Report: does the repo have
   `@testing-library/react-native`? What had to be mocked (connectors, navigation params, safe-area,
   date-picker, image-picker)? Did it need real navigation? How long does one render take?
2. **Where does the math actually live?** For each of: fuel adjustment (`applyFuelSuggestion`, ~`:306-313`),
   return total (`computeReturnTotal`), `Sisa`, and auto-debt creation at close — is it an importable
   function, a local closure inside the component, or inline JSX? **This decides pure-function vs
   render-level (D-0), and it is the answer the re-gate is waiting on.**
3. **The close payload.** Which connector does the screen call to close a rental, with what argument shape?
   D-4 requires the suite to pin the arguments, not just the display.
4. **Enumerate every `<TextInput` under `app/`** (v1.0.4 discovery counted **38** tags — confirm or
   correct), and classify each as **Field** / **Control (BR-4)** / **allow-listed (LoginScreen,
   DetailSewaScreen)**. This fixes D-7's two lists. Note that `TextInput` now comes from
   `@/components/AppText`, so match the **tag**, not the import.
5. **Confirm PRD-8's inventory rows for the five in-scope screens** — the line numbers in the 10-treatment
   table and the `height: 48` / `height: 40` violations. PRD-8 verified these on 2026-07-25; confirm they
   are still true, and **report any divergence rather than following either document**.
6. **`RupiahInput`'s consumers.** Who imports it? Correcting its border token (BR-3) changes appearance on
   every consumer, including the two fenced screens — I want that blast radius named before ④ moves.
7. **Existing test infrastructure to reuse**: `test/mockSafeAreaInsets.ts`, the `AppText.clampAudit`
   source-audit pattern (AC-6/AC-7's model), and the `closeRentalBodyInvariance` mutation-verification
   pattern (the model for making AC-6/7/8 non-vacuous).

**Standing instruction (Farrel's, carried from v1.0.3 and binding on every brief below):**
> **Verify against the codebase, not the plan.** These docs may be stale. **Read the actual file before
> acting on a claim about it.** Where reality diverges, **report the divergence** — do not silently follow
> either one.

**Run jest from `apps/lavender-ops-mobile`, never from the monorepo root** — a root run resolves a
different config and sweeps in a stale `.worktrees/` tree, reporting a wall of failures in files nobody
touched.

**Reports back inline. Then STOP.** Lead re-gates with Farrel.

### ② `tester` (continued, via `SendMessage`) — the BR-12 characterisation suite — model `sonnet`

Dispatched only after the re-gate, at the shape locked there.

1. **Write it against untouched `master` behaviour and get it green.** It pins **what the code does today**,
   not what any document says it should do.
2. **Floor (D-4):** fuel adjustment in **both directions across zero**, return total, `Sisa`, auto-debt
   creation at close. Deeper is allowed; shallower is not.
3. **Pin the payload handed to the connector at close**, not only rendered text. A display-only suite would
   have been green through v1.0.3's `deleted_at` regression.
4. **Pin the two debt #12 divergences exactly as they behave** — the fuel suggestion appending an extra-fee
   line rather than adjusting Subtotal, and the amber suggestion row. **These are known, declined
   (guard 3), and preserving them is the requirement.** Do not "correct" toward `docs/02` §6.
5. **Touch no product code.** If a test cannot be written without changing `app/`, **stop and report** —
   that is the D-0 circularity and it is Lead's to escalate, not yours to work around.
6. Report the exact suite/test count delta so Lead can verify the baseline arithmetic (50 / 237) rather
   than quote you.

**Deliverable beyond the code: the "before" snapshot.** Lead commits this suite as its own commit before
any product code moves, so AC-9's *before* state is a git object and not a memory (debt #13).

### ③ `rental-math-reviewer` — review the suite, not the code (read-only)

Narrow, and narrow on purpose. **You are reviewing `test/` files, not `app/`.**

Does ②'s suite actually pin the four things `docs/02` §6 says must not be wrong — tariff-derived return
total, fuel adjustment in both directions, `Sisa`, and auto-debt creation — **including the arguments sent
to the server at close**? Name anything it leaves unpinned that a component swap could plausibly break.

**Two things you must NOT do:**
- **Do not report debt #12's two divergences as defects.** They are known, Product owns the open question
  of which side is wrong (code or §6), and guard 3 forbids touching either this release. Confirm the suite
  **preserves** them; that is the correct finding.
- **Do not propose math improvements.** Nothing in this release intends to change math. AC-9 exists to
  prove none did.

A finding here goes back to ② before ④ starts.

### ④ `developer-frontend` #1 — the primitive + the two form screens — model `sonnet`

1. **First, the ride-along, so it cannot be forgotten** (it survived all of v1.0.4 by being forgettable):
   remove the `fontScale` diagnostic footer from `app/components/VersionFooter.tsx` and update its two
   tests. **Its job is fully done** — Mom's real values were measured on 2026-07-23 and are recorded in
   `docs/reports/v1-0-4-visual-audit.md` row H1 (`fontScale` ≈ 1.4, inset 47.27px), so removing it loses
   nothing. Set `app/config/release.ts` `RELEASE` to `"1.0.5"` in the same pass. **Do not touch `app.json`
   `version`** — it stays `1.0.0`; bumping it would target OTA at a runtime Mom's installed APK does not
   report and she would silently stop receiving updates.
2. **Build `FieldBox` to the locked contract**, all five invariants. **TDD it.**
3. **`RupiahInput` consumes the box** and stops declaring its own border/fill/radius/`minHeight` (AC-6's
   real trap, and BR-3's `outlineVariant` → `outline` correction). ~~Check ①'s consumer list before you
   move it — the two fenced screens consume it and must not change beyond the intended appearance.~~
   **Corrected after ① — see C-1.** Its **entire** blast radius is `HutangFormScreen` (`:16`, `:132`), the
   only production importer, and this release is already modifying that screen under AC-3. **No fenced
   screen consumes it.** What `DetailSewaScreen` and `PengembalianScreen` each carry is a *local
   `parseRupiahInput` function* — a parsing helper, not the component. Do not confuse the two.
4. **`UserFormScreen` + `HutangFormScreen` (AC-3):** every `TextInput` sits in a box; treatment #8
   (bare text) is **gone from both files**; no read-only-looking row is editable. Real JSX sites are
   `UserFormScreen.tsx:193` (first of 6) and `HutangFormScreen.tsx:138` — **not** the line numbers PRD-8
   cites, which point at style declarations (see C-3).
4b. **`PembayaranSheet.tsx` — added at the re-gate (RG-1, `[by-Farrel]`).** Its three Fields get the box:
   *Jumlah* (`:203`), the "Lainnya" method description (`:248`), *Notes* (`:294`). **This is what makes BR-7
   true** — *Jumlah* is where Mom types a payment amount, and it is currently bare treatment #8.
   **Two things to hold in mind while you do it:**
   - It is consumed by **four** screens. `HutangDetailScreen` and `DetailSewaScreen` are **out of scope** and
     will change appearance inside the sheet. **Do not edit either file.** Zero source lines in them.
     If something tempts you to, that is guard 6 and the answer is no.
   - **No payment math, no parsing change.** The sheet's amount parsing, its method selection, and the
     payload it produces stay byte-identical in behaviour. You are wrapping three inputs, nothing more.
5. **Confirm, don't migrate:** `SearchField`, `Stepper`, `PhotoRow` are **Controls** (BR-4) and are
   **unchanged**. `FieldCard` is a container and is unchanged. If one of them looks like a twenty-minute
   win, that is guard 2 and the answer is no.
6. **Write the rule down (BR-11)** — the convention, BR-4's three categories, the retired `outlineVariant`,
   the demoted tint (after this release **the tint means nothing about editability in either direction**),
   and the two known exceptions. In the primitive's doc comment **and** in `docs/`.
7. **Lint discipline (gate-critical).** Baseline **122**; brief is **+0**. Tokens only, never colour
   literals. Alphabetise new `StyleSheet.create` blocks. `TextInput` from `@/components/AppText`, **never**
   from `"react-native"` — that rule is at **0** and any new violation is visible immediately. **Do not run
   `eslint --fix` or `prettier --write` on anything** (guard 8, debt #6a).
8. **Hard fences.** No math. No connector. No value, string or control label changed. No server surface. No
   native dep — if the approach reaches for one, **STOP and escalate**: it breaks the OTA shape and becomes
   a different release.
9. **Deliverable: a written handoff note for ⑤** — `FieldBox`'s final API, how `RupiahInput` consumes it,
   which style names it replaced, and anything ⑤ must not disturb.

### ④b `developer-frontend` #1 (continued, via `SendMessage`) — the strips

1. **`RentalDetailScreen` AC-4a:** remove the `surfaceContainerLow` single-value blocks — *Tujuan*
   (`:795`) and read-only *Catatan* (`:846`). BR-5. This deletes one side of the `insetBlock` ↔ `notesInput`
   collision rather than arbitrating it.
2. **`RentalDetailScreen` AC-4c:** `kmEditInput` (`:1100`) and `notesInput` (`:1112`) **do** get the box —
   they are live editable Fields today under BR-4. **This is the release plan's AC-4 split (D-5); AC-4b
   ("nothing on this screen is boxed") is explicitly OUT and belongs to the release carrying PRD-6.** Do
   not implement AC-4 as literally written in PRD-8.
3. **`UserDetailScreen` AC-5:** audit only. Nothing renders in a box; every value row is the plain
   read-only rendering. If it already complies, **say so and change nothing** — an audit that finds nothing
   is a pass, not a failure to contribute.
4. **AC-11:** `editLogic.ts`'s `canEditKondisiKeluar` / `canEditNotes` are **consumed unmodified**. Prove
   it: `git diff` shows zero change to that file. No grant, no revoke (BR-9).
5. Same lint discipline, same hard fences, same standing instruction.

### ⑤ `developer-frontend` #2 — the `PengembalianScreen` D-5 migration — model `sonnet`

Fresh agent. Briefed with PRD-8, the locked contract, ④'s handoff note, and the guards **verbatim**. This
is the release's one dangerous dispatch.

1. **Before you change anything: run ②'s characterisation suite and confirm it is green.** Record the
   output. That is AC-9's "before".
2. **Migrate the input-bearing primitives only** (D-5): local `inputRow`, `amountInputRow`, and the bare
   *Tujuan* input move onto `FieldBox`; the local `FieldCard` goes.
   **AC-1 — every Field gets the box:** *Kembali* time, *Tujuan*, *Harga bensin / kotak*, *KM Kembali*,
   *Subtotal Sewa*, and **every** extra-fee description (D-3: boxed, not underlined) and amount.
   **Every read-only row stays plain:** *Mulai*, *Durasi*, *Total Tagihan*, *Sudah dibayar*, *Sisa*, and
   the `Saat keluar: …` captions.
   **AC-2:** no field box declares a fixed `height`; every one is `minHeight ≥ 52` — closing `:1203` and
   `:1221`.
   **D-2:** the *Kembali* row gets the box **and keeps its `inlineEditBtn`**. No interaction change.
3. **After: run the suite again. It must pass identically.** If any test fails, **that is your defect, not
   the test's.** You may not modify, skip, or relax any file ② wrote. **Stop and report.**
4. **The guards, and they are the point of this brief:**
   - **Guard 1 — do not change the `returnedAt` default.** You are styling the row debt #16 is about. Its
     `new Date()` seed at `:175` stays exactly as it is. **The release plan calls this the single most
     likely misread available here.** Styling the row is safe; re-seeding it is not, and fixing it properly
     needs an RPC this release is forbidden to open.
   - **Guard 2 — do not widen the migration.** Local `SectionLabel`, `FuelGauge`, `Stepper` and the local
     bottom action bar **stay put**. The two `FuelGauge` copies have already diverged on `max` — migrating
     that one is a *behavioural* change on a money path wearing a refactor's clothes.
   - **Guard 3 — do not "fix" debt #12.** Both divergences are under your cursor: the fuel suggestion
     writing an extra-fee line instead of adjusting Subtotal, and the unconditionally-amber suggestion row.
     **Which side is wrong — the code or `docs/02` §6 — is an open Product question.** Touching either turns
     a presentation release into a math change. ②'s suite pins them; leave them pinned.
   - **Guard 4 — no math moves.** This is a component swap. AC-9 cannot prove the calculations came through
     identical if the calculations were edited.
   - **Guard 6 — do not fold in `DetailSewaScreen`.** Once `FieldBox` exists it will look like a
     twenty-minute win. It is tariff composition behind the same fence, and opening two rental-math screens
     in one release is how a fenced migration stops being fenced.
5. **AC-10 — re-verify, do not re-derive.** Every field box grows rather than clips at `fontScale` 1.5
   (PRD-5), and this screen still respects the system-nav inset via `useBottomBarPadding()` /
   `useBottomSpace()` (PRD-4). **The D-5 migration is the most likely place in the app for a v1.0.4 fix to
   be silently undone**, and you are the dispatch that would do it.
6. **No server surface, no connector change, no new native dep.** `git diff --stat` must show zero under
   `app/services/` and `supabase/`. Same lint discipline (+0), same standing instruction.

### ⑥ `rental-math-reviewer` — AC-9 post-migration pass + fence check (read-only)

**`PengembalianScreen` only.** Confirm ⑤'s diff is a **component swap and nothing else**: no fuel
adjustment, no return total, no `Sisa`, no auto-debt path, no value, no connector call moved, no
`returnedAt` seed change. This is the pass AC-9 and the release gate both require, and it is the
independent read behind a release whose failure mode is *invisible on Mom's screen*.

If you cannot run `git diff` (no Bash tool — v1.0.4's reviewer hit exactly this and handled it well), say
so and reconstruct from an independent record rather than issuing a PASS you cannot back. Lead will supply
whatever diff you need.

### ⑦ `tester` (fresh) — the durability layer + acceptance — model `sonnet`

**Derive "correct" from PRD-8's BR-1…BR-12 and AC-1…AC-12 as amended by the release plan's AC-4 split —
not from the implementation. If the code contradicts a criterion, report a failure; do not adjust the
test.**

1. **AC-6 · AC-7 · AC-8 — the durability layer, and it must be non-vacuous.** Modelled on
   `AppText.clampAudit.acceptance.test.ts`. **Verify each by mutation, the way debt #9's body-invariance
   guard was:** introduce a second style declaration, an un-boxed `TextInput`, and a failing contrast pair,
   and **confirm each test goes red**. Report the mutation results — a durability test nobody tried to
   break is decoration.
   - **AC-7 is a TWO-LAYER audit (RG-5)**, not a single regex pass: **layer 1** enumerates every
     `<TextInput` tag app-wide and asserts the containing-file set equals a fixed manifest; **layer 2**
     mounts each in-scope screen and asserts every rendered `TextInput` has a `FieldBox` **ancestor**.
     Layer 2 is what answers the nesting question; layer 1 is what catches a *new* file. Both must be
     mutation-verified independently.
   - **AC-7 uses D-7's two lists, and the manifest is fixed — ① measured it, do not re-derive it.**
     **33** production `<TextInput` tags (not 38 — see C-2), distributed as:
     | Category | Files |
     |---|---|
     | **Boxed** (must have a `FieldBox` ancestor) | `PengembalianScreen` (8) · `UserFormScreen` (6) · **`PembayaranSheet` (3 — RG-1)** · `RentalDetailScreen` (2) · `HutangFormScreen` (1) · `RupiahInput` (1, the primitive's own) |
     | **Control exemption (BR-4)** | `SearchField` (1) — *its shape already declares interactivity; it filters, it does not record.* `Stepper` contains **no** `TextInput` |
     | **Allow-list (AC-7) — exactly two** | `DetailSewaScreen` (8, D-6: fenced tariff composition) · `LoginScreen` (2, D-7: not an operational data-entry surface) |
     | **Excluded by name, with reason (RG-6)** | `TextField.tsx` (1) — **dead Ignite code, zero consumers app-wide**, but it *does* render a `TextInput`, so an audit that ignores it produces a false failure. This is the mirror of `KendaraanScreen`, which is excluded because it renders none |
     `AppText.tsx` contributes zero matches — it renders `<RNTextInput` under an aliased import, so it is
     invisible to a source grep by construction. **Do not "fix" the regex to catch it.**
   - **AC-8** computes contrast **from the tokens**, not from hard-coded hex, so a future palette edit
     cannot silently drop the border back to 1.63:1.
2. **AC-1 · AC-2 · AC-3 · AC-4a · AC-4c · AC-5** — verify per screen. Enumerate; do not sample.
3. **Prove the fences by diff:** zero changes under `app/services/` and `supabase/`; `editLogic.ts`
   untouched (AC-11); `app.json` `version` still `1.0.0`; `RELEASE` is `"1.0.5"`; the `fontScale`
   diagnostic footer is **gone**. **Run `git status --short` before trusting any `git diff` as a scope
   proof** — this release creates a lot of new test files and `git diff` is blind to untracked ones
   (debt #13).
4. **Full suite from `apps/lavender-ops-mobile`.** Never the monorepo root. Report the delta against
   **50 suites / 237 tests**, and account for the footer tests ④ removed rather than letting the number
   drift unexplained.
5. **Re-count lint** with the command in Pre-execution state; report the delta against **122**.
6. **Author the v1.0.5 visual-audit checklist** — the six touched files, each row naming the *specific*
   control, label or row to look at, at default scale **and** at `fontScale` 1.5. **Where a row overlaps an
   outstanding v1.0.4 row, say which one** (see the open item on entanglement) so one walk can close both
   and be recorded twice.
   **`PembayaranSheet` needs rows for all four screens that open it** — including `HutangDetailScreen` and
   `DetailSewaScreen`, which are **out of scope yet change appearance** (RG-1 consequence 4). An
   out-of-scope screen whose look changes is exactly the row a checklist author skips.
   **Try to write it to `docs/reports/v1-0-5-visual-audit.md`.** Debt #14 claims your role cannot write
   `.md` — your tool list says otherwise, so **try it and report which is true**; if it fails, return the
   checklist inline and Lead persists it. Either way it must not exist only inside a returned message,
   which is what nearly happened in v1.0.4.
7. **Report coverage gaps plainly.** A criterion that can only be checked on Mom's phone is **PENDING**,
   never passed. AC-12 is structurally pending until she has the OTA.

### Model routing

| Agent | Primary | Status |
|---|---|---|
| Lead (this session) | Fable high → **Opus 5 xhigh** | on sanctioned fallback; flagged to Farrel |
| `tester` ×2 | `sonnet` | pinned, available — see **D-8** |
| `developer-frontend` ×2 | `sonnet` | pinned, available |
| `rental-math-reviewer` ×2 | inherits | — |
| `developer-backend` | — | **not dispatched** (no server surface) |
| `connector-contract-reviewer` | — | **not dispatched** (deliberate; proven by diff instead) |

Per `docs/agents/lead.md`: if a developer's Sonnet becomes unavailable I **stop and hand to Farrel**. I will
not substitute a larger model for a developer-tier agent — including for ①, which is briefed to *report and
prove*, leaving every judgment call to Lead and Farrel.

---

## Risks I want on the record before approval

1. ✅ **DISSOLVED by ① — did not materialize.** `PengembalianScreen` mounts in jest, and the math was
   **already extracted** into `app/utils/rentalMath.ts`. Both suite shapes are available; no extraction, no
   circularity, no blocked first step. The gate did its job by returning a "yes" rather than a "no", which
   is the outcome that costs the least. *Original text kept below, unedited, because the reasoning was
   sound and the escape routes may be needed by a future fenced migration:*
   ~~**The release has one feasibility risk and ① exists to find it.**~~ If `PengembalianScreen` cannot be
   mounted in jest and the math is inline, BR-12's suite has no legal first step (D-0's circularity). The
   two exits, both **Farrel's call at the re-gate**: (a) fall back to the release plan's own **short
   option** — style-only on `PengembalianScreen`, no component swap, delivering AC-1/AC-2/BR-7/BR-8 with
   **no fence opened**; or (b) permit a **minimal pure-function extraction** as its own commit with its own
   `rental-math-reviewer` pass, then pin. I am not choosing between these before ① reports, because the
   choice depends entirely on what it finds.
2. **This release's failure mode is silent.** v1.0.4's worst case looked wrong on Mom's screen. A math
   regression here would look like nothing at all, and the money screen it lives on is the one she taps at
   every handover. That is what AC-9 is for, and it must be satisfied **before** publish, not alongside her
   sign-off.
3. **The hardest task runs on the smallest model** (D-8). Mitigated by ①'s map, ③'s independent review, and
   Lead re-running rather than quoting. Stated because it is Farrel's to overrule.
4. **`no-color-literals` is the likeliest gate breach**, for structural reasons: a new shared primitive
   whose entire job is a border colour and a fill, on a +0 budget. Mitigated by briefing tokens-only in
   both developer briefs, and by the fact that a literal there is *also* a BR-3/BR-6 failure — the lint
   rule and the requirement point the same way.
5. **AC-6/AC-7/AC-8 are the release's durability layer and are the easiest thing here to fake.** Three
   green tests that assert nothing would sail through. The gate already demands mutation verification; ⑦'s
   brief demands the mutation *results* be reported.
6. **Guard 1 is the misread I most expect.** ⑤ is styling the exact row debt #16 is about, with a
   `new Date()` seed sitting three lines from its cursor and an obvious "while I'm here" fix available. It
   is in the brief three times for that reason.
7. **Debt #4 is *partially* opened and must be recorded that way on ship.** `PengembalianScreen`'s
   input-bearing primitives close; `DetailSewaScreen` entirely, plus that screen's local `SectionLabel` /
   `FuelGauge` / `Stepper` / bottom bar, stay open. Same for debt #15: `PengembalianScreen` gets its
   first-ever coverage, **`DetailSewaScreen` stays at zero**, so #15 is *partially* closed and **not
   struck**. A future reader must not be able to mistake either for closed.

---

## Open items for Farrel — the plan is blocked on these

| # | Item | Lead recommends |
|---|---|---|
| **D-0** | Discovery pass (①) first, implementation plan written after it, re-gate before any product code | **Yes** — the BR-12 suite's shape, and its feasibility, are unknown |
| **D-1** | PRD-8 OQ-1 — the box's fill | **`surface` `#f6faff`** via token (4.28:1, smallest diff, two carriers) |
| **D-2** | PRD-8 OQ-2 — the *Kembali* row | **Box it, keep the `inlineEditBtn`, no interaction change** |
| **D-3** | PRD-8 OQ-3 — `extraFeeDesc` | **Box it** — AC-1's own wording already requires it |
| **D-4** | PRD-8 OQ-4 — characterisation depth + owner | **Tester writes it; pin the close payload, not just the display; preserve debt #12's two divergences** |
| **D-5** | PRD-8 **A-1** — the AC-4 split needs Product to write it | **Yes, before ⑦ dispatches.** Blocks nothing earlier; a tester reading AC-4 as written will correctly report a false failure |
| **D-6** | The lint ceiling: absolute **169**, or **baseline + 10 = 132**? | **132**, ratcheting down with the baseline. The absolute reading silently grants +47 |
| **D-7** | AC-7 needs a Control-exemption list separate from its two-entry allow-list | **Yes** — two lists, different meanings; the gate's "exactly two" stands for the allow-list |
| **D-8** | `tester` is pinned `sonnet` for the release's hardest task | **No change** — mitigations named. Overruling costs a session restart |
| — | Commit the six untracked/modified **docs** on `master` before branching | **Yes** — PRD-8 and the release plan are currently untracked (debt #13) |
| — | Branch `v1.0.5-which-fields-you-can-change` off `master` | Yes |
| — | Record debt #6's ledger row as **repaid** (`no-restricted-imports` now 0; cumulative **0**) | Yes, at ship |

### One more, and it is a sequencing question rather than a decision

**v1.0.4's per-screen visual rows are still un-walked, and this release restyles four of the screens they
cover.** `docs/reports/v1-0-4-visual-audit.md` rows **B3/C3/F2** (`PengembalianScreen`), **I6/J2/F3**
(`UserFormScreen`, `HutangFormScreen`), **B4/D1/I8** (`RentalDetailScreen`) and **C5/I7**
(`UserDetailScreen`) all describe screens v1.0.5 changes. Walk them *after* v1.0.5 publishes and a failure
can no longer be attributed to a release — v1.0.4's claim and v1.0.5's are entangled in the same pixels.

**Recommendation `[by-agent]`: walk those overlapping rows on the current published build before v1.0.5
publishes** — Farrel's own device, and only the rows on those four screens, not the whole checklist. It is
cheap, it can happen in parallel with any dispatch, and it keeps two release ledgers honest instead of one.
The rest of the checklist and Mom's AC-8 stay with OQ-5's combined visit, recorded as two separate verdicts.

---

## Farrel's approval — 2026-07-25 `[by-Farrel]`

**"Agree with your recommendation, continue."** Every row of the table above approved as recommended:

| # | Decision | Recorded |
|---|---|---|
| **D-0** | Discovery pass ① first; implementation plan written at the re-gate; **no product code before it** | ✅ `[by-Farrel]` |
| **D-1** | Field-box fill = `surface` `#f6faff`, via token | ✅ `[by-Farrel]` |
| **D-2** | *Kembali* row: box it, **keep the `inlineEditBtn`**, no interaction change | ✅ `[by-Farrel]` |
| **D-3** | `extraFeeDesc`: **boxed**, not underlined (AC-1's own wording) | ✅ `[by-Farrel]` |
| **D-4** | Tester owns the BR-12 suite; **pin the close payload**, not just the display; **preserve** debt #12's two divergences | ✅ `[by-Farrel]` |
| **D-5** | Product writes **PRD-8 A-1** (the AC-4 split) before ⑦ dispatches | ✅ `[by-Farrel]` |
| **D-6** | **Lint ceiling restated: "lowest measured baseline + 10" = 132 today**, ratcheting down with the baseline. This release briefed at **+0** | ✅ `[by-Farrel]` |
| **D-7** | AC-7 carries **two lists** — BR-4 Control exemption, separate from the exactly-two allow-list | ✅ `[by-Farrel]` |
| **D-8** | `tester` stays pinned `sonnet`; mitigations as briefed | ✅ `[by-Farrel]` |
| — | Commit the six requirement docs on `master`, **then** branch | ✅ `[by-Farrel]` |
| — | Branch `v1.0.5-which-fields-you-can-change` off `master` | ✅ `[by-Farrel]` |
| — | Debt #6 ledger row recorded as **repaid** at ship (`no-restricted-imports` 37 → 0; cumulative **0**) | ✅ `[by-Farrel]` |
| — | **Walk v1.0.4's overlapping visual rows before v1.0.5 publishes** (B3/C3/F2, I6/J2/F3, B4/D1/I8, C5/I7) — Farrel's device, parallel to any dispatch | ✅ `[by-Farrel]` — **outstanding action, not yet done** |
| — | Lead stays on **Opus 5 xhigh** (Fable not selected) | ✅ |

**Approval recorded. Phase 2 open. ① dispatched 2026-07-25 — read-only; no product code will be written
before the re-gate.**

---

## ① Discovery pass — findings (read-only; one spike file written, see RG-7)

Returned 2026-07-25. **The gate's own question came back "yes"**, two of my Phase-1 premises were wrong, and
it found a file that four separate documents had missed.

### The decisive answer: `PengembalianScreen` mounts, and there is no D-0 circularity

Proven by a spike that runs green, not argued:

| Question | Answer |
|---|---|
| Mountable in jest? | **Yes.** `@testing-library/react-native@^13.2.0` is present, and `RentalDetailScreen.test.tsx` (409 lines) is a direct precedent — it mounts a comparable screen, drives inputs, and asserts connector payloads |
| Mocks needed | `useSafeAreaInsets` (via existing `test/mockSafeAreaInsets.ts`), `Platform.ios`→android (debt #11's trick, needed because the screen reads `useBottomBarPadding()`), `@/services/rentals`, `@/services/photos/capture`, `@/utils/showToast` |
| Mocks **not** needed | `@react-native-community/datetimepicker` and `PembayaranSheet` both render fine untouched. **No `NavigationContainer`** — the screen takes `{navigation, route}` as plain props, so a stub object suffices |
| Cost | **~210–230 ms per test.** A 25–40 case suite runs in under 10s |
| Can it pin the **close payload** (D-4's requirement)? | **Yes — demonstrated live.** The spike drove *Subtotal Sewa*, pressed save, and asserted `closeRental`'s exact arguments, full object |

**And the math was already extracted.** This is the finding that dissolves the risk:

| Concern | Where | Kind |
|---|---|---|
| Fuel adjustment | `computeFuelAdjustment()` — `app/utils/rentalMath.ts:78-87` | **Importable pure function.** `applyFuelSuggestion()` (`PengembalianScreen.tsx:305-312`) is a thin closure that calls it and pushes an extra-fee line |
| Return total | `computeReturnTotal()` — `rentalMath.ts:89-96` | **Importable pure function**, called at `PengembalianScreen.tsx:238` |
| `Sisa` | **Inline**, `PengembalianScreen.tsx:242` — `Math.max(0, totalTagihan - totalPaid)` | One line. *Not* the module's own `sisa()` (`:49-51`), because `pendingPayments` are `Omit<Payment,"id">[]`, not persisted `Payment[]`. Not a defect — a shape mismatch |
| Auto-debt creation | **Server-side**, inside `rpc_close_rental` | **Invisible to jest.** The client's only testable contribution is *the payload it sends* — which is exactly what the spike proved pinnable |

> **A dividend nobody planned for.** `app/utils/rentalMath.ts` has **zero test coverage of its own** today,
> and it is imported by **both** rental screens. So the pure-function half of ②'s suite lands on shared math
> that `DetailSewaScreen` also depends on. **This does not close debt #15's other half** — that screen's own
> tariff-composition code and *Simpan Rental* stay uncovered — but it is more than the release bargained for,
> and it should be recorded as such rather than overclaimed.

### Corrections to my own plan `[by-agent]`

**C-1 · I got `RupiahInput`'s blast radius wrong — in the safe direction, which is not an excuse.**
I briefed ④ that *"the two fenced screens consume it and must not change beyond the intended appearance."*
They do not consume it. **Exactly one production file imports the component**: `HutangFormScreen.tsx:16`,
used at `:132`. What `DetailSewaScreen` and `PengembalianScreen` each carry is a **local
`parseRupiahInput` *function*** (`:46` / `:45`) — a parsing helper, not the shared component. Debt #4's own
table says precisely this (*"`parseRupiahInput` — 2 local copies"* on a separate row from *`RupiahInput`*),
and I read one row as the other.

So BR-3's border-token correction has a blast radius of **one screen this release is already modifying**.
④'s brief is corrected in place. Recording the error rather than quietly fixing it, because the shape of it
matters: I inferred a consumer relationship from a *name collision* in a debt document instead of checking
the imports. That is the same failure as v1.0.4's C-1 (asserting a belief about a lint rule as a measured
fact) — and both were caught the same way, by an agent going to the source.

**C-2 · The `<TextInput` count is 33, not 38.** Verified file-by-file rather than by one grep (a naive
`<TextInput` sweep returns 47, inflated by test files and type-only `useRef<TextInput>` usages). v1.0.4's
report recorded 38 on 2026-07-22 and its methodology can't be reconstructed; **33 production tags is the
operative number.** Plausibly 33 + 5 test-file matches = 38, but I am not asserting that — the divergence is
benign either way, and the enumeration below is what AC-7 is built against.

**C-3 · Four of PRD-8's inventory line numbers point at the *style declaration*, not the JSX tag.** Same
systematic pattern, not behavioural, recorded so nobody navigates by them:

| PRD-8 cites | Actually there | The real JSX site |
|---|---|---|
| `UserFormScreen.tsx:340` | `input: {…}` style | `:193` (first of 6) |
| `HutangFormScreen.tsx:229` | `input: {…}` style | `:138` (Catatan) |
| `RentalDetailScreen.tsx:1100` `kmEditInput` | style block | `:520`, testID `kondisi-km-input` |
| `RentalDetailScreen.tsx:1112` `notesInput` | style block | `:827`, testID `notes-input` |

Everything else in PRD-8's inventory **confirmed accurate**, including the two live BR-1 violations
(`inputRow` `height: 48` at `:1203`, `amountInputRow` `height: 40` at `:1221`), the `extraFeeDesc`
underline (`:1268`), and both tinted single-value blocks (`:795`, `:846`). Debt #12's two divergences
confirmed still present and unmodified, exactly as described.

### The `<TextInput` enumeration — D-7's two lists, fixed

**33 production tags.** In-scope files in **bold**:

| File | Count | Category |
|---|---|---|
| **`PengembalianScreen.tsx`** | 8 | Field — AC-1 |
| `DetailSewaScreen.tsx` | 8 | Field — **allow-listed** (D-6) |
| **`UserFormScreen.tsx`** | 6 | Field — AC-3 |
| `PembayaranSheet.tsx` | 3 | Field — **unaccounted for. See F-1 / RG-1** |
| **`RentalDetailScreen.tsx`** | 2 | Field — AC-4c (`:520`, `:827`) |
| `LoginScreen.tsx` | 2 | Field — **allow-listed** (D-7) |
| **`HutangFormScreen.tsx`** | 1 | Field — AC-3 |
| `SearchField.tsx` | 1 | **Control** (BR-4) — exempt |
| `RupiahInput.tsx` | 1 | The primitive's own input |
| `TextField.tsx` | 1 | **Dead code that still renders one. See F-2** |

`Stepper` contains **no** `TextInput` — so the Control-exemption list is `SearchField` alone, plus
`RupiahInput`'s internal input once it composes the box. `AppText.tsx` contributes zero matches (it renders
`<RNTextInput` under an aliased import, so it is invisible to a source grep by construction, not by
convention — a detail AC-7's regex must not trip over).

### Findings that stand on their own

**F-1 · `PembayaranSheet.tsx` is missing from four documents at once, and one of its three fields is a
rupiah amount.** 3 bare treatment-#8 inputs — *Jumlah* (`:203`), the "Lainnya" method description (`:248`),
*Notes* (`:294`) — all Fields under BR-4. Imported by **four** screens: `RentalDetailScreen` and
`PengembalianScreen` (in scope) plus `HutangDetailScreen` and `DetailSewaScreen` (explicitly out). It
appears in **neither** PRD-8's inventory, **nor** the release plan's D-7 rollout table, **nor** the AC-7
allow-list, **nor** my Phase-1 audit surface. → **RG-1, escalated below.**

**F-2 · `TextField.tsx:214` is dead code that renders a `<TextInput`.** Zero consumers app-wide (confirmed;
v1.0.4's F-2 already found the whole Ignite UI layer dead). It is the mirror image of `KendaraanScreen`,
which my Phase-1 audit surface excluded *"because it renders no `TextInput`"* — this one is also dead but
does render one. Without an explicit exclusion, AC-7 produces a **false failure on a file nobody will ever
touch.**

**F-3 · AC-7 has a *design* problem, not just a listing problem.** Once `RupiahInput` composes `FieldBox`,
"is this `<TextInput>` inside a field box" becomes a **nesting** question. The
`AppText.clampAudit.acceptance.test.ts` model answers a different one — *"does this pattern appear anywhere
in this file"* — which is strong for AC-6's "exactly one definition" and weak for AC-7's "this specific tag
is wrapped by that specific component." → **RG-5.**

**F-4 · `PengembalianScreen` has zero `testID`s** (`RentalDetailScreen`, by contrast, has several). The
spike located *Subtotal Sewa* by `UNSAFE_getAllByType(TextInput)[3]`. Works, but fragile. → **RG-3.**

**F-5 · `returnedAt` is wall-clock-seeded**, so any assertion on it needs fake timers or a tolerant match.
→ **RG-4.** (The seed itself is debt #16 and stays untouched — guard 1.)

**F-6 · Nothing else resisted.** `KeyboardAvoidingView`, the local `FieldCard` / `SectionLabel` /
`FuelGauge` / `Stepper`, and the local bottom bar caused **no** mounting friction; the screen rendered
cleanly end-to-end on the first attempt. The D-5 migration has no hidden structural obstacle.

### Decisions I am taking at the re-gate without escalating `[by-agent]`

- **RG-2 · Suite shape: hybrid, both halves, because both are now proven.** (a) Pure-function tests directly
  against `rentalMath.ts` — `computeFuelAdjustment` in **both directions across zero**, `computeReturnTotal`,
  `sumPayments`, `hoursLate`. (b) Render-level tests mounting the screen for `Sisa`'s inline composition, the
  *Terapkan* fuel-suggestion interaction end-to-end, and **the exact `closeRental` payload** across
  scenarios: `sisa = 0`, `sisa > 0`, both fuel directions, extra fees, discount. The pure half satisfies
  BR-12's floor with no screen involved; the render half is what makes AC-9 mean something.
- **RG-3 · Lookup strategy: semantic first, index only as a documented fallback. No `testID`s added this
  release.** ② uses placeholder text and label proximity; where nothing else exists it may fall back to
  type-index and **must say so per lookup**. Rationale: wrapping a tag in `FieldBox` does not reorder the
  tree, so index fallbacks survive the migration; and ② cannot touch product code anyway. If ② finds a field
  with no usable handle *and* an ambiguous index, it reports rather than improvises.
- **RG-4 · `returnedAt` in assertions: fake timers or a tolerant match.** Never by changing the default.
- **RG-5 · AC-7 becomes a two-layer audit**, which is stronger than the PRD imagined and is only affordable
  because ① proved mountability:
  | Layer | What it does | Mutation that must turn it red |
  |---|---|---|
  | **1 — source, app-wide** | Enumerate every `<TextInput` tag; assert the set of containing files equals a fixed manifest (boxed files + Control list + allow-list + the F-2 exclusion) | Add a `TextInput` to an unlisted file |
  | **2 — render, in-scope screens** | Mount each in-scope screen; assert every rendered `TextInput` has a `FieldBox` **ancestor** | Un-box a single input |
  Layer 1 gives durability against new files; layer 2 answers F-3's nesting question honestly instead of
  approximating it with a regex.
- **RG-6 · `TextField.tsx` is excluded from AC-7 by name, with a written reason — not deleted.** v1.0.4's
  F-2 established the whole Ignite UI layer is dead; removing it is its own decision and not this release's.
- **RG-7 · The spike is absorbed, not kept.** Its payload test is the seed of ②'s suite. ② folds it in and
  **deletes** `test/spike.pengembalian.discovery.test.tsx`. No throwaway file survives in `test/`.
- **RG-8 · No separate implementation-plan document, and I am recording the judgment rather than silently
  dropping the deliverable.** D-0 promised one written *"at the re-gate, with facts in it."* The facts turned
  out to be: both suite shapes are viable, nothing is blocked, and no mechanism had to be chosen. A separate
  plan document would restate this report's sequencing and briefs with no new information. **The briefs are
  the plan.** If Farrel wants a standalone document anyway, say so and I will write it.

---

## ⏸ RE-GATE — one question, and it is a scope question

Everything else from ① I have absorbed. **This one is Farrel's**, because it changes what the release
contains and because the release gate has an opinion about it.

### RG-1 · `PembayaranSheet` — in scope, or a third allow-list entry? `[needs Farrel]`

**The facts.** Three bare Fields — *Jumlah* (`:203`), the "Lainnya" method description (`:248`), *Notes*
(`:294`). Consumed by four screens: `RentalDetailScreen` and `PengembalianScreen` (in scope),
`HutangDetailScreen` and `DetailSewaScreen` (explicitly out). Named in **none** of PRD-8's inventory, the
release plan's D-7 rollout table, AC-7's allow-list, or my own Phase-1 audit surface.

**Why it is not a minor omission: BR-7.** *"The money field is the proof case, not the cleanup item…
**every** rupiah entry point is a field box."* BR-7 enumerates `RupiahInput` and `PengembalianScreen`'s two —
but *Jumlah* is where Mom types a payment amount, from both the return screen and the rental detail screen.
It is plausibly her second-most-frequent money entry in the app.

| | Option | Cost | Consequence |
|---|---|---|---|
| **A** ⭐ | **Fold it in** — a sixth file, 3 inputs boxed | ~30 min, one file, +2 visual-checklist rows | **BR-7 becomes true.** The sheet's appearance changes as seen from the two out-of-scope screens — **but their source does not change by one line** |
| **B** | **Allow-list it** with a written reason | ~0 | A third entry against a gate that calls a third entry *"a scope breach, not a test detail"* — and **a money field stays unmarked** while everything around it gains a box |
| **C** | **Scope AC-7's audit to the five screens** | ~0 | A false pass. The audit stops being durable, which is the entire point of AC-6/AC-7 |

**Recommendation: A `[by-agent]`.**

1. **BR-7 is unsatisfiable without it**, and BR-7 is the requirement PRD-8 elevates to *proof case*.
2. **The fence is not crossed.** Debt #4 and D-6 fence `DetailSewaScreen`'s **own source** — its local
   primitives, its tariff math. `PembayaranSheet` is a **shared** component; boxing its inputs changes zero
   lines in either fenced screen and touches no math. This is structurally identical to `RupiahInput`'s
   border correction, which the release already accepted with its blast radius named — and which C-1 shows
   is *smaller* than I claimed.
3. **It is a shared component, which is where BR-6 wants the convention to live.** Applying the rule to
   screens while skipping the component they share is the exact mechanism that produced ten treatments.
4. **Mom's exposure is the point of the release.** If the one surface she types money into is the one
   unmarked surface, AC-12 fails on an omission rather than on the design.

**If Farrel prefers B**, the honest record is: BR-7 is **partly met**, `PembayaranSheet` joins the
known-exceptions list with its reason, and AC-12's sign-off carries the caveat that Mom's payment-amount
field is deliberately unmarked this release. That is defensible scope discipline — it should be *chosen*,
not defaulted into by four documents having missed the file.

**Either way, Product should record the outcome in PRD-8 alongside A-1** (D-5), so a future reader finds it
in the requirements rather than only in a release report.

> **② does not depend on this.** The characterisation suite pins math and payloads, not appearance, and
> boxing an input does not reorder the render tree. ② can dispatch the moment RG-1 is answered — or
> immediately, if Farrel would rather answer RG-1 while it runs.

### Re-gate resolution — Farrel, 2026-07-25 `[by-Farrel]`

✅ **Option A — fold `PembayaranSheet` in.** *"Agree for option a to fold it in."*

**Five consequences, all recorded rather than left to be rediscovered:**

1. **The release touches six files, not five.** `PembayaranSheet.tsx` joins ④'s brief: its three Fields
   (*Jumlah* `:203`, method description `:248`, *Notes* `:294`) get the box. **BR-7 is now satisfiable** —
   every rupiah entry point in the app wears the convention.
2. **Zero fenced-screen source changes.** `HutangDetailScreen` and `DetailSewaScreen` consume the sheet and
   will *look* different inside it; **neither file is edited.** That distinction is the whole basis of this
   decision and must survive into the record — a future reader must not mistake it for D-6 being reopened.
3. **AC-7's allow-list stays at exactly two.** `PembayaranSheet` moves from *unaccounted-for* to *in scope*,
   which is the only resolution that does not grow the allow-list. The release gate's "any third entry is a
   scope breach" stands untouched and unweakened.
4. **The visual checklist gains rows** for the sheet as opened from `RentalDetailScreen` **and** from
   `HutangDetailScreen` — the second one specifically because it is an out-of-scope screen whose appearance
   changes, which is exactly the kind of thing that gets skipped.
5. **Product records it in PRD-8 alongside A-1** (D-5). Whether it lands as part of A-1 or as its own A-2 is
   Product's call; what matters is that a reader of PRD-8 finds `PembayaranSheet` in the requirements, not
   only in this report. **Its absence from the inventory is the actual defect here** — the file was missed by
   PRD-8, the release plan, AC-7's allow-list, and my own Phase-1 audit surface simultaneously.

**Re-gate closed. Suite shape locked (RG-2). Next action: dispatch ②.**

> ⚠️ **Nothing is dispatched as of this line.** Farrel asked for a handoff document at this point, so the
> session ends here deliberately. See `docs/handoff-prompts/` for the continuation document. The branch is
> clean and the next action is unambiguous: **dispatch ② with the brief in this report, as amended by RG-2
> and RG-3.**

---

## Phase 2 — execution log

Session **S2**, resumed 2026-07-25 from the handoff document. Nothing above this line was re-opened: the
plan is approved, the re-gate is closed, and the baselines were measured once and are not re-derived.
What *is* re-measured below is the branch's own state, because S1 measured `master` and the branch has
moved since.

### State verified on resume — measured, not quoted `[by-agent]`

| Fact | Value | How |
|---|---|---|
| Branch | `v1.0.5-which-fields-you-can-change` | `git branch --show-current` |
| HEAD | `42adf83 docs(v1.0.5): Phase 1 plan approved, discovery returned, re-gate closed` | `git log -1` |
| Working tree | clean apart from **one untracked doc** — the S1 handoff document | `git status --short` |
| `master..HEAD` diff | **exactly two files**: `test/spike.pengembalian.discovery.test.tsx` (+184) and `docs/reports/v1-0-5.md` (+971). **Zero product code.** | `git diff --stat master..HEAD` |
| **Branch test baseline** | **51 suites / 239 tests, all passing** (26.1s), from `apps/lavender-ops-mobile` | `pnpm test` |

**The branch baseline is 51/239, not master's 50/237, and the difference is the spike.** The +1 suite /
+2 tests is `spike.pengembalian.discovery.test.tsx`, which RG-7 requires ② to absorb and **delete**. That
arithmetic is recorded here *before* ② returns so its reported delta can be checked against a number Lead
measured rather than one ② supplied: **② must land at `51 + n − 1` suites and `239 + m − 2` tests.** If the
spike survives into ⑦, RG-7 was missed — and the suite count is where it will show.

### E-1 · ② is dispatched **fresh**, not as a `SendMessage` continuation `[by-agent]`

The Phase-1 plan briefs ② as *"`tester` (continued, via `SendMessage`)"*, on the assumption that ①'s agent
would still be live and holding its own discovery context. **It is not — S1 ended at the re-gate and the
agent went with it.** Continuation is unavailable, so ② was dispatched as a **fresh `tester`** with ①'s
findings carried inline in the brief: the mount recipe and its mock list, the four math locations in
`app/utils/rentalMath.ts`, the `Sisa` inline site, the server-side auto-debt fact, the `closeRental`
signature location, and corrections **C-1 / C-2 / C-3**.

**Nothing about the dispatch's substance changed.** Same role, same `sonnet` pin (D-8 — no substitution),
same RG-2 hybrid shape, same RG-3 / RG-4 / RG-7 amendments, same fences. Recording it because the plan says
one thing and the execution did another, and a future reader comparing the two deserves the reason rather
than a discrepancy. **The general lesson is worth more than this instance:** a plan that routes work through
`SendMessage` has an undeclared dependency on session continuity. Where a dispatch sits behind a gate that
might halt the session — which is every gate this project runs — brief it as though its predecessor's
context is gone, because it may well be.

### ② — dispatched, awaiting return

`tester` · `sonnet` · read-write on `test/` and screen-adjacent test files, **read-only on `app/`**.
Brief as written above, amended by RG-2 (hybrid shape), RG-3 (semantic lookups, no `testID`s added),
RG-4 (fake timers for `returnedAt`) and RG-7 (absorb the spike, then delete it), plus the D-4 requirement
to pin the **connector payload** and to preserve debt #12's two divergences.

Additions Lead made to the brief when reissuing it fresh `[by-agent]`:

1. **The scope of ② stated negatively as well as positively** — ② writes the characterisation suite and
   **not** AC-6/AC-7/AC-8's durability audits, which belong to ⑦. Risk 5 already names those three as the
   easiest thing in this release to fake; the matching risk on this dispatch is a tester who builds them
   early, unbriefed, and half-well.
2. **The `Sisa` shape mismatch pre-empted.** `PengembalianScreen.tsx:242` does not call `rentalMath.sisa()`
   because `pendingPayments` are `Omit<Payment,"id">[]`. ① established this is a shape mismatch, not a
   defect. Briefed explicitly as *not* a defect, because it is precisely the kind of thing a diligent agent
   reports as a finding and a less diligent one silently "fixes".
3. **The scope proof pinned to `git status --short`, never `git diff`** (debt #13) — ② creates untracked
   files by definition, and `git diff` cannot see them.
4. **"What could a component swap break that this suite would not catch?"** asked of ② directly, rather than
   left for ③ to discover. A gap the author names is cheap; the same gap found by a reviewer costs a round
   trip, and found at ⑥ costs the migration.

### ② — returned 2026-07-25. Green, and its gap list changed the plan.

**Lead re-ran everything rather than quoting the agent (D-8's mitigation).** Both numbers reproduce exactly:

| Check | ② reported | Lead measured | Verdict |
|---|---|---|---|
| Suites / tests | 52 / 266 | **52 / 266, all passing** (23.6s) | ✅ matches |
| Suite arithmetic | `51 + 2 − 1` / `239 + 29 − 2` | **exactly the pre-recorded formula** | ✅ the spike really was absorbed and deleted (RG-7 discharged) |
| Real lint errors | 122 | **122**, breakdown unchanged, `no-restricted-imports` still **0** | ✅ **+0**, the tripwire untripped |
| Product code touched | none | `git status --short`: two new test files, spike deleted, **zero `app/` modifications** | ✅ |

**Delivered** — committed as `303ed6d`, the AC-9 "before" anchor:

- `app/utils/rentalMath.test.ts` — 18 tests. `computeFuelAdjustment` in both directions across zero plus
  the equal-case and both stepper extremes, `computeReturnTotal` (including a **negative** extra-fee amount
  and the discount-exceeds-total clamp), `sumPayments`, `hoursLate`. **This module had zero coverage and is
  imported by both rental screens** — the dividend ① predicted, now banked.
- `app/screens/PengembalianScreen.characterization.test.tsx` — 11 tests. `Sisa` including the `Math.max(0,…)`
  clamp, the *Terapkan* interaction end-to-end, and the **full `closeRental` payload** across `sisa=0`,
  `sisa>0` (the auto-debt path), both fuel directions, extra fees, and discount.

**Debt #12's two divergences are pinned as-is**, by name, in tests that assert `subtotalSewa` stays put while
a signed `"Bensin"` line is appended, and that the suggestion row's `backgroundColor` is
`colors.warningContainer` **identically in both directions**. Guard 3 held.

**One trap ② found the hard way, recorded because ⑤ will meet it.** Index-based lookups do **not** generalise
across row types: `addPayment` inserts at the **end** of the tree, but extra-fee and discount rows insert
**mid-tree** (inside the Rincian Biaya card, *before* Catatan), displacing Catatan downward. ② assumed
insertion-at-end, was wrong, and caught it only by writing a throwaway debug spike that dumped placeholders
before and after the press. Also: a single `.parent` hop from `getByText` does **not** reach the host
container, because `@/components/AppText`'s `Text` is a `forwardRef` composite that consumes one hop — a
v1.0.4 artefact with a testing consequence nobody had written down. Both are documented inline at the
lookups. ② reported **no divergences** between the brief and the code; every one of ①'s line numbers held.

> ⚠️ **One claim of ②'s is recorded UNVERIFIED, deliberately.** It states that its test files reference
> `TextInput` / `TouchableOpacity` via `require("react-native")` at call sites rather than a top-level
> `import`, *"matching the existing precedent and avoiding the `no-restricted-imports` violation."* The
> **need** is legitimate — locating host nodes with `UNSAFE_getAllByType` requires the raw RN type, which
> `@/components/AppText`'s clamped wrapper is not. The **gate** is satisfied: Lead measured that rule at
> **0**. What is unverified is *"matching the existing precedent"* — Lead cannot open code to check it, and
> an agent asserting a precedent it did not cite is the exact shape of v1.0.4's C-1. **⑦ is to confirm the
> idiom appears in a pre-existing test file, with `file:line`, or report that ② introduced it.** If it was
> introduced, that is not a defect — but it is a new convention entering the codebase unannounced, and the
> difference between "followed a precedent" and "set one" belongs in the record.

### E-2 · ②b dispatched to close four gaps ② named `[by-agent]`

② answered *"what could a component swap break that this suite would not catch?"* honestly and at length.
Four of its gaps sit **directly under the migrations that come next**, which makes them different in kind
from the rest of the list:

| Gap | Why it is in the blast radius |
|---|---|
| `km`, `tujuan`, `notes` never driven with real content (pinned only at empty defaults) | **⑤ boxes all three.** A field whose only coverage is "left untouched" cannot detect a broken `onChangeText` after re-wrapping |
| Extra-fee / discount row **removal** (only additions were driven) | **⑤ boxes the extra-fee description (D-3) and amount** — the rows are under its cursor |
| The `returnedAt` **picker interaction** | **D-2 requires the row to keep its `inlineEditBtn` with no interaction change.** Nothing currently pins that. This is the direct test of an approved decision |
| `PembayaranSheet` existing-payment **edit/delete** (add *is* covered) | **④ boxes its three inputs (RG-1), and *Jumlah* is BR-7's stated proof case** |

**Excluded, and named so the omission is a choice rather than an oversight:** the `Stepper`'s
disable-at-boundary state (a Control under BR-4, untouched — guard 2), the `closeRental` rejection path,
loading/not-found guards, `photos` non-empty (`PhotoRow` is a Control), and multiple-modifiers-in-one-save
(the composition *is* `computeReturnTotal`, already pinned pure).

**Taken `[by-agent]`, not escalated**, on three grounds: D-4 is already approved and says *"deeper is allowed;
shallower is not"*, so this is depth inside an approved decision rather than new scope; it adds **no product
code** and moves no gate; and ②'s agent is still warm, so a `SendMessage` continuation is a fraction of a
fresh dispatch. **The cost is honest and it is a delay** — ③ waits, and therefore so does ④. Farrel can
overrule and send ③ against the committed suite as it stands; BR-12's floor is already met.

**Sequenced, not parallelised:** ②b **then** ③. A reviewer reading a suite that is still growing reviews a
moving target and has to be paid for twice.

> **AC-9's "before" is now two commits, not one.** `303ed6d` plus ②b's follow-up. Both land before any
> product code moves, so the guarantee is intact — but a future reader looking for a single anchor should
> take **the tree at the last test commit preceding ④**, and ⑦ should quote that SHA rather than `303ed6d`.

### ②b — returned 2026-07-25. Green. **AC-9's "before" state is complete at `91e7b76`.**

Re-run by Lead, not quoted: **52 suites / 280 tests, all passing** (27.1s) · **lint 122, +0**, breakdown
unchanged · `git status --short` shows one modified test file and nothing under `app/` besides it.
`+14 tests, +0 suites` — exactly as reported.

| Area closed | What is now pinned |
|---|---|
| Text entry into ⑤'s boxed fields (8 tests) | `km`, `tujuan`, `notes` driven with real content **through to the payload**, including both trims |
| Extra-fee / discount **removal** (2) | Removing one of two fee rows, and removing the discount — with `Total Tagihan` asserted to move correspondingly |
| The `returnedAt` picker (1) | **D-2's interaction is now pinned**: the inline Edit control opens the picker and a chosen value reaches the payload. The seed at `:175` is untouched (guard 1) |
| `PembayaranSheet` edit/delete (3) | `updatePayment` / `deletePayment` and their effect on `Sisa` and the close payload — including the "Lainnya" description field, ④'s third boxed input |

**Behaviour pinned as found, not as assumed** — worth recording because ⑤ must preserve it: `KM Kembali`
treats **`'0'` as a real value** (the code tests *string* truthiness, not numeric), and it **strips
non-digits inside its own `onChangeText`** before `parseInt` ever runs (`'12a3b4'` → `1234`). Neither is
documented anywhere else.

**②b named five residual gaps** — embedded newlines in multiline `Catatan`; removing a *Terapkan*-created
"Bensin" line (an "undo the suggestion" path); removing fee rows down to **zero**; the picker's
dismiss/cancel branch; the delete-confirmation "Batal" path. **These are handed to ③ with the filter
"which of these could change a number?"** rather than closed reflexively — the suite is an instrument, not
a coverage target, and growing it past the migration's blast radius buys nothing.

### F-7 · A jest-resolution fact that debt #11 does not cover `[by-agent]`

②b found this while pinning D-2's picker interaction, verified it by dumping the resolved module's source,
and it generalises well beyond this release:

**`@react-native-community/datetimepicker` resolves to its *iOS* implementation under this jest config, no
matter what the `Platform` mock says.** The `Platform.ios`→android trick (debt #11) overrides `Platform.OS`'s
**runtime value**; it does nothing to jest's **platform-suffixed file resolution**, which is a separate
mechanism and stays on `.ios.js`. Two consequences, pulling in opposite directions:

- **It is why the interaction is testable at all.** The Android implementation (`datetimepicker.android.js`)
  returns `null` and drives an imperative native dialog with no JS-visible element. The iOS one renders a
  component a test can find.
- **The library's Android code path is therefore never exercised in jest.** `PengembalianScreen`'s own
  `Platform.OS === "android"` JSX guard *is* exercised correctly, and `handlePickerChange` is pinned — but
  the Android **two-dialog date-then-time auto-advance sequencing** is proven only by the screen's own
  handler, not by the library. **Mom's device is Android.**

**Recorded as F-7 and nominated for the debt register at ship `[by-agent]`**, as an extension of #11 rather
than a new item. It changes nothing this release — no styling migration can affect it — but the next agent
who mocks `Platform` and assumes it selected the Android *file* will lose an afternoon to it, and this is
the second time this project has paid for the gap between "what the platform mock does" and "what people
assume it does".

---

## ③ `rental-math-reviewer` — returned 2026-07-25. **The instrument needed repair, and ③ is why we know.**

**Verdict returned:** the suite is fit to serve as AC-9's "before" state, **but** it contains one
tautological test, three fixture collisions that make seeded state unprovable, and one excluded gap where
the harness *cannot currently express the case correctly*. Findings went straight back to ② as **②c**,
per the plan's own rule that a ③ finding returns to ② before ④ starts.

**③ has more than paid for itself.** Adding it was a Lead addition to the release plan, justified on the
grounds that *"AC-9 cannot be satisfied by a suite that pins the wrong thing."* That is exactly what it
found — twice.

### The two findings that justify the round trip on their own

**1 · One of the two debt-#12 tripwires cannot fail.** The amber-unconditional test
(`PengembalianScreen.characterization.test.tsx:576-610`) selects its target node **by** the property it
then asserts — `findAncestorWhere(terapkan, flat => flat.backgroundColor === colors.warningContainer)`,
followed by three `expect`s that the colour is `warningContainer`. **All three are true by construction.**
The only real signal is "the search succeeded", and its failure mode is a *thrown Error*, not an assertion
diff — which reads as a broken locator and invites the next agent to loosen the predicate, destroying the
pin outright. Guard 3's entire exclusion rested on this test. **It was guarding nothing.**

**2 · Three fixture collisions make the four hydration lines (`PengembalianScreen.tsx:221-224`)
unprovable.** `bensinKotak` is `4` in every fixture and `4` is also the `useState` default (`:181`);
`tujuan` and `notes` are `""` on both sides; `tarif` = `totalBill` = `rate24h` = 40000. So nothing proves
the fetched rental was ever read. If ⑤ disturbs that effect:

- **`bensinKotak`** — the screen opens at a hardcoded 4 regardless of what the bike left with. A bike out
  with 8 kotak shows *"Bensin kurang 4 kotak — saran +Rp 20.000"* on open. Mom taps *Terapkan*. **Real
  money, wrong direction.**
- **`tujuan` / `notes`** — existing values render blank, then `handleSave` (`:330-331`) **overwrites them
  with `""`**. Silent destruction of stored data.

**Both invisible behind 25 green tests.** This is precisely risk #2's silent failure mode, found by the
dispatch added to look for exactly it.

### The exclusion I flagged as my least confident — and I was wrong in a specific way

I excluded *"multiple modifiers combined in one save"* on the grounds that the composition **is**
`computeReturnTotal`, already pinned pure. ③'s correction: **that holds for the arithmetic and not for the
construction of the arguments**, which is the half the migration touches.

Worse, it found the harness cannot currently express the case. `addExtraFeeRow` assumes its new pair lands
at `[N-1, N]` — true only while **no Diskon row exists**, a precondition its docstring does not state.
With a discount present the fee inputs insert *before* Diskon, so the helper returns `[Amount, Diskon]`
rather than `[Desc, Amount]`, and a combined test would silently type the description into the amount field
and the amount into Diskon. **The exclusion did not defer a test; it left a landmine in a helper.** ②c is
briefed to **verify this empirically before fixing it** — ③ derived it from JSX and ran nothing, and this
project does not act on unverified readings.

### C-4 · A correction to my own ③ brief `[by-agent]`

I briefed ③ that the `Stepper` was excluded because *"`Stepper` is a Control under BR-4 and is explicitly
not being touched."* **The premise names the wrong component.** `PengembalianScreen` uses its **own local
`Stepper`** (`:105-156`), not `app/components/form/Stepper.tsx`. The *outcome* is unaffected — guard 2
already names the local one, and the 0–8 clamp lives at the **call site** (`:532-533`), not inside the
Stepper, so even swapping in the shared component could not move a number. Recording it because the
reasoning was wrong even though the answer was right, and because a future reader could otherwise "close"
this exclusion on a premise that does not hold. Same failure shape as C-1: a claim about a consumer
relationship asserted without checking which component was actually in play.

### What went back to ② as ②c, and what did not

**Tier 1 (must close before product code moves):** the tautological amber test · the fixture collisions +
assert all four hydration lines · verify-then-fix `addExtraFeeRow`'s ordering precondition · the combined-
modifier full-payload test (which also closes the `alreadyPaid + pendingPaid` hole, currently only ever
exercised with one operand at zero) · **`hargaPerKotak` is never edited in any test** though ⑤ boxes that
field and §6 requires it editable · **no assertion anywhere reads a money field's *displayed* value**, so a
dropped `value={displayRupiah(…)}` binding is invisible — the single most likely casualty of a re-wrap.

**Tier 2 (cheap and real):** reset the module-scope `navigation` mock in `beforeEach` (it accumulates across
all 25 tests, a latent wrong-answer generator) and assert the post-save `navigation.replace` · `Sisa`
banner colours in both states · the `Terlambat` caption, on screen 25 times and asserted zero times ·
removal of a **negative** *Terapkan*-created line · a midnight-rollover flake at `:449-451` · two inert
assertions.

**Refused, and named so the omission is a choice `[by-agent]`:** double-tap Save re-entrancy (real, but the
local bottom bar is out of ⑤'s scope under guard 2) · `photos` wiring beyond its shape pin (`PhotoRow` is a
Control) · row-order assertions (**the visual audit covers appearance; the instrument is not becoming a
layout test**) · `composeTarif` coverage (③ confirmed this screen never calls it — it belongs to
`DetailSewaScreen` and debt #15).

**②c is briefed to push back rather than comply** where a fix is misguided. ③ read carefully but ran
nothing; ② has Bash and wrote the code. An instrument changed on a false premise is worse than one left
alone.

### ③'s observations for Product — NOT actioned this release `[by-agent]`

Guards 3 and 4 forbid touching any of these. Recording them here so Product finds them; **none is a
regression this release could introduce**, and the first is a genuine money defect that predates it.

1. **`Terapkan` is repeatable and the suggestion row never dismisses.** `applyFuelSuggestion` (`:305-312`)
   appends unconditionally, and the row's render condition (`:663`) depends only on `bensinKembali` vs
   `kondisiKeluar.bensinKotak` — never on whether a line was already added. **Two taps append two `Bensin`
   lines and double-charge.** Distinct from debt #12's two items, and it would exist under §6's reading too.
2. **An empty "Tambah Biaya" row is persisted.** `addExtraFee` (`:286-288`) creates
   `{description: "", rawAmount: ""}`, which flows through to the payload as a junk line item if Mom presses
   the button and changes her mind.
3. **Clearing *Harga bensin / kotak* silently reverts to Rp 5.000.** `parseRupiahInput("") || 5000`
   (`:232`) — the field looks empty but still drives a 5.000-per-kotak suggestion.
4. **`parseRupiahInput` accepts `-` (`:46`) but every `onChangeText` on this screen strips to digits.** The
   negative path is reachable only programmatically, so the "−" Mom sees on the Bensin line cannot be typed
   or hand-corrected — her only recourse is the trash icon.

---

## ②c — returned 2026-07-25. **AC-9's "before" state is final at `49fae63`.**

Re-run by Lead, not quoted: **52 suites / 283 tests, all passing** (26.7s) · **lint 122, +0** ·
**`npx tsc --noEmit` exit 0** · `git status --short` clean but for the one test file, and **no debug
artefacts left behind**. `+3 tests, +0 suites` — the rest was repair.

**Every one of ③'s findings survived contact with a run.** ②c verified rather than complied, which is what
it was asked to do.

### ③'s ordering claim: **confirmed empirically, and it was worse than ③ could see**

②c wrote a throwaway debug test, ran it, and deleted it. With a Diskon row present, pressing *Tambah Biaya*
put the new pair at indices **`[4,5]`**, not the `[before-1, before] = [5,6]` the old helper assumed —
exactly as ③ predicted from the JSX, because `extraFees.map` renders before Diskon.

**And the fix exposed a second, independent bug that only a run could have found.** A whole-tree style scan
for the new locator collided with the **Jaminan banner's inline icon row**, which carries a *byte-identical*
style object (`{flexDirection:"row", alignItems:"center", gap:spacing.sm}`) and sits later in the tree — so
"take the last style match" silently grabbed the wrong node. Now filtered by descendant `TextInput` count.

> **This is the vindication of the verify-before-fixing instruction, and it is worth stating plainly.**
> ③ read carefully and reasoned correctly from the source; it still could not have seen the style
> collision, because that fact does not exist in the JSX — it exists in the *rendered tree*. Had ②c
> implemented ③'s suggested fix on trust, it would have shipped a locator pointing at the wrong element and
> the suite would still have been green. **A reviewer who cannot run and an implementer who does not verify
> produce a confident wrong answer between them.**

### What ②c changed

**The two Tier-1 headline fixes:** the amber locator now walks from *Terapkan* to the nearest ancestor
matching the row's **structural** signature (`borderRadius: 12` + `gap` + `padding` + `flexDirection: row`),
correctly skipping `terapkanBtn` (`borderRadius: 8`) — the colour assertions are now genuine diffs. And the
fixtures collide with nothing: `bensinKotak` 4→**6**, `tujuan` ""→**"Kos Barat"**, `notes` ""→**"catatan
awal"**, `totalBill` 40000→**999999**, `rate24h` 40000→**45000**, with `tarif` held at 40000.

**A neat consequence worth recording:** the fourth hydration line needed no new assertion. Making
`totalBill` / `rate24h` distinct from `tarif` **retroactively strengthened the ~20 existing
`subtotalSewa: 40000` assertions** — they would now fail if hydration were mis-wired to either field. A
fixture change that upgrades tests written before it is the cheapest coverage in the release.

**New tests (3):** combined modifiers in one save as a full payload — **with the discount added *before* the
fee, the exact ordering that exposed the helper bug, so it doubles as a regression guard** · `hargaPerKotak`
edited → suggestion recomputed → payload · removal of a **negative** *Terapkan*-created line.

**Strengthened:** `getByDisplayValue` on the money fields **including the U+2212 minus**, closing the
invisible-`value`-binding hole · `navigation` mock reset per test + the post-save `navigation.replace`
assertion · `Sisa` and banner colours in both states · the `Terlambat` caption, computed via the pure
`hoursLate` rather than hardcoded (a hardcoded hour count would be its own timezone flake) · the
midnight-rollover flake · the two inert assertions, replaced with a scoped `getRowValue(…, "Total Tagihan")`.

### ⚠️ OPEN VERIFICATION ITEM — carry this to ⑤ and ⑦ `[by-agent]`

**Nobody has demonstrated that the repaired amber test *can* fail.** Fix #1 existed precisely because the
old test could not fail; the replacement's ability to fail is **reasoned, not measured**. That is the same
class of claim the fix was correcting, one level up.

**Required:** a mutation check — temporarily change the fuel-suggestion row's `backgroundColor` in the
source and confirm the test goes **red**, then revert. It is a two-minute check and it belongs to whoever
next has write access to `app/` (⑤ before it migrates, or ⑦ during its mutation pass, which is already
briefed to mutation-verify AC-6/7/8 and can absorb this one). **Until it is done, guard 3's debt-#12
tripwire is repaired but unproven.**

### A Product nuance ②c flagged, not a defect `[by-agent]`

③'s framing of "green at `Sisa = 0`, amber at `Sisa > 0`" describes the **banner** exactly
(`successContainer` / `warningContainer`), but the **`Sisa` value text itself** uses a different pair —
`colors.onSuccessContainer` / **`colors.error`**, and *error* renders red-toned rather than amber. ②c pinned
both as they actually are rather than conflating them into one story, which is correct instrument
behaviour. Recorded so that if Product ever asks why the `Sisa` figure and the banner beneath it are not
literally the same colour at `Sisa > 0`, the answer is on file: **it is a pre-existing fact about the
screen, untouched by this release.**

---

## ⏸ HALT — Farrel's instruction, 2026-07-25

**Farrel asked to pause once the tester returned.** Stopping here deliberately; **④ is not dispatched.**

**State at the halt:**

| | |
|---|---|
| Branch | `v1.0.5-which-fields-you-can-change` |
| HEAD | **`49fae63`** — **this is AC-9's "before" state. ⑦ must quote this SHA, not `303ed6d`.** |
| Tests | **52 suites / 283 tests**, all passing (Lead-measured) |
| Lint | **122** real errors, **+0** against baseline · `tsc --noEmit` clean |
| Product code changed | **still none** — every commit so far is tests or docs |
| Dispatches complete | ① discovery · ② suite · ②b gaps · ③ review · ②c repair |
| **Next action** | **④ `developer-frontend` #1** — the first dispatch that touches `app/`: footer removal + `RELEASE = "1.0.5"` → `FieldBox` → `UserFormScreen` + `HutangFormScreen` → `PembayaranSheet` (RG-1) |

**Three things a resuming session must carry forward:**

1. **The open verification item above** — mutation-check the repaired amber test.
2. **PRD-8 A-1 is still unwritten** (D-5). It does not block ④; it **does** block ⑦, because a tester
   reading AC-4 as literally written will see the two boxed inputs on `RentalDetailScreen` and correctly
   report a false failure. **A ready-to-run `/product` prompt covering A-1 *and* A-2 (`PembayaranSheet`,
   RG-1 consequence 5) is in `docs/handoff-prompts/AI_Continuation_Document-26Jul2026-1424.md`, Appendix A.**
3. **v1.0.4's overlapping visual rows** (B3/C3/F2, I6/J2/F3, B4/D1/I8, C5/I7) — **see the amended decision
   below. No longer a pre-publish blocker.**

### Two decisions from Farrel at the halt — 2026-07-26 `[by-Farrel]`

**1 · The overlapping v1.0.4 visual rows: *"let it be, and keep flagged."*** This **supersedes** the
Phase-1 approval row that had them walked on Farrel's device **before v1.0.5 publishes**. They are **not** a
pre-publish blocker; the flag **stays standing** and must not be quietly dropped from the register or from
`docs/reports/v1-0-4-visual-audit.md`.

**The consequence is accepted, and it is recorded here so nobody re-derives it as a surprise:** if v1.0.5
publishes before those rows are walked, **v1.0.4's and v1.0.5's visual claims are entangled in the same
pixels** on four screens. A failure found afterwards on `PengembalianScreen`, `UserFormScreen`,
`HutangFormScreen`, `RentalDetailScreen` or `UserDetailScreen` **cannot be cleanly attributed to one
release**. That is a knowingly accepted cost, not an oversight — and v1.0.4's AC-8 remains **PENDING**
either way, since it needs Mom's own words regardless.

**2 · The `Terapkan` double-charge money defect: *"I'll follow your recommendation."*** Farrel adopted
Lead's recommendation rather than issuing a separate ruling, so the recommendation itself is the decision
and is restated here in full:

> **Do not fix it in v1.0.5.** Three reasons: (a) it is a **behaviour change on a fenced rental-math
> screen**, which guards 3 and 4 forbid and which **AC-9 exists specifically to prove did not happen** —
> fixing it would make the release's own safety proof unsatisfiable; (b) it is **pre-existing**, not
> introduced here; (c) v1.0.5 is a presentation-only OTA whose entire safety story is *"nothing but
> appearance moved"*.
>
> **Instead:** Lead records it in `docs/known-technical-debt.md` **at ship**, and Product receives it as
> input for a future release — most naturally **folded into PRD-6** (the dedicated rental edit screen),
> which is already licensed to change this screen's behaviour.

⚠️ **Carry this to whoever fixes it:** the v1.0.5 characterisation suite **does not pin this behaviour**.
③ found it by reading, and ②c was explicitly told not to add it. So the eventual fix has **no "before"
protection** and will need to write its own — the same BR-12 discipline this release just spent five
dispatches establishing. The same applies to ③'s other three observations.

**④'s brief needs no revision.** It is as written above, amended by RG-1 (`PembayaranSheet` folded in) and
C-1 (`RupiahInput`'s only production consumer is `HutangFormScreen`, not the fenced screens).

---

## ▶ S3 — halt lifted, 2026-07-26

Session **S3**, resumed from `docs/handoff-prompts/AI_Continuation_Document-26Jul2026-1424.md`. Nothing
above this line was re-opened. Phase 1, the re-gate, and all five test-phase dispatches stand as recorded.

### State verified on resume — measured, not quoted `[by-agent]`

| Fact | Value | How |
|---|---|---|
| Branch | `v1.0.5-which-fields-you-can-change` | `git rev-parse --abbrev-ref HEAD` |
| HEAD | `18590ab docs(v1.0.5): record Farrel's two halt decisions + S2 handoff package` | `git log -1` |
| Working tree | **clean** | `git status --short` — empty |
| Tests | **52 suites / 283 tests, all passing** (34.7s), from `apps/lavender-ops-mobile` | `npx jest` |
| Lint | **122** real non-`prettier` errors — breakdown byte-identical to S2, `no-restricted-imports` still **0** | the eslint JSON one-liner in § Pre-execution state |
| Types | **exit 0** | `npx tsc --noEmit` |
| Product code changed | **still none** | every commit to date is tests or docs |

**HEAD is one commit past the handoff document's `b25e42d`** — that commit *is* the handoff package. AC-9's
"before" state is unaffected and remains **`49fae63`**.

### Farrel's instruction lifting the halt — 2026-07-26 `[by-Farrel]`

> *"continue where we left of and give me a prompt for product on prd 8 a1. overlapping rows let it be, and
> keep flagged. for the money defect, ill follow your recommendation"*

Three things, all consistent with the two decisions already recorded above and none of them new scope:

1. **Resume execution.** ④ is dispatched. The halt was Farrel's and it is his to lift.
2. **The `/product` prompt (A-1 + A-2) handed over** for a parallel session. It blocks ⑦, not ④.
3. **The two halt decisions re-confirmed** — overlapping v1.0.4 rows stay flagged and unwalked; the
   `Terapkan` double-charge is not fixed this release. Neither is re-litigated here.

### D-9 · Where BR-11's rule doc lives `[by-agent]`

BR-11 requires the convention recorded *"in `docs/` **and** in the primitive's own doc comment"* but does
not say which file. Taking **`docs/field-box-convention.md`**, a new standalone document, rather than a
subsection of `docs/02-demo-development.md`.

**Reason:** `docs/02` is the connector-contract and rental-math document. A UI convention lodged inside it
is discoverable only by someone who already knows it is there — which is the failure mode BR-11 exists to
prevent. Lead wires the `CLAUDE.md` pointer at ship. Recording it because it is a filing decision the PRD
left open, and an unrecorded filing decision is how a rule doc goes missing.

### E-3 · The open verification item goes to ⑤, not ④ `[by-agent]`

Farrel left the placement open (*"④'s or ⑤'s brief"*). **Assigning it to ⑤**, as step 1b of its
before-you-touch-anything protocol, with ⑦ as backstop.

**Reason — and it is about guard integrity, not convenience.** ④'s brief fences it off `PengembalianScreen`
entirely and says so four times. Instructing the same agent to temporarily edit that exact file, in that
exact region, contradicts its own guards — and a brief that contradicts itself is how guard 1 and guard 3
get misread. ⑤ already opens that file and already runs the suite before migrating, so the mutation check
is one extra step inside a protocol it has rather than a licensed exception to a fence.

**Accepted cost, stated so nobody rediscovers it as a surprise:** if the repaired amber test still cannot
fail, we learn it *after* ④ and ④b have landed. That costs **no rework** — neither dispatch touches
`PengembalianScreen` — only wall-clock, and only in the failure case. What it would cost is a tester round
trip whose repair commit lands after `49fae63`; the AC-9 anchor for *`PengembalianScreen.tsx`* stays valid
either way, since no product code will have touched that screen, but the test file would have moved and
⑦ must be told which SHA it is quoting and why.

### ④ — dispatched 2026-07-26, awaiting return

`developer-frontend` #1 · `sonnet` (pinned; no substitution — `docs/agents/lead.md`). Brief as written in
§ "④ `developer-frontend` #1" above, amended by **RG-1** (`PembayaranSheet` folded in) and **C-1**
(`RupiahInput`'s blast radius is `HutangFormScreen` alone).

Additions Lead made when issuing it, carrying **E-1's lesson** — brief every dispatch as though its
predecessor's context is gone, because behind a gate it is `[by-agent]`:

1. **The whole brief written standalone.** ①'s corrections C-1 / C-2 / C-3, D-1's approved fill token, the
   full five-invariant contract, D-7's two lists, and every guard reproduced inline rather than referenced.
   No dependency on session continuity.
2. **The three verification commands given verbatim**, including the eslint JSON one-liner and the
   `pnpm run lint` prohibition, with the instruction to report actual output rather than belief.
3. **`git status --short` before any `git diff`** (debt #13) — ④ creates new files and `git diff` cannot
   see them.
4. **The `no-restricted-imports` tripwire explained rather than asserted** — *why* 0 is more dangerous than
   37, so the agent understands the gate instead of obeying a number.
5. **D-9's doc location decided for it**, so the rule doc cannot land somewhere ⑤ and ⑦ will not find it.
6. **The handoff note for ⑤ specified in four named parts**, with the explicit statement that ⑤ is a fresh
   agent and cannot see ④'s context.

### ④ — returned 2026-07-26. Green. **The first product code of v1.0.5 exists.**

**Lead re-ran everything rather than quoting the agent (D-8's mitigation).** Every number reproduced:

| Check | ④ reported | Lead measured | Verdict |
|---|---|---|---|
| Suites / tests | 52 / 284 | **52 / 284, all passing** (26.8s) | ✅ matches |
| Suite arithmetic | `283 − 2 − 1 + 3 + 1` | **284**, and the suite count holds at 52 (`−1 +1`) | ✅ every test accounted for |
| Real lint errors | 122 | **122**, breakdown byte-identical, `no-restricted-imports` still **0** | ✅ **+0**, tripwire untripped |
| Types | exit 0 | **`npx tsc --noEmit` exit 0** | ✅ |
| Scope | 13 paths | `git status --short` matches ④'s list **exactly** | ✅ |

**Delivered:** `FieldBox` (TDD'd — its 3 tests were confirmed red before the component existed), `RupiahInput`
consuming it, all 6 `UserFormScreen` inputs + `HutangFormScreen`'s Catatan boxed, `PembayaranSheet`'s three
Fields boxed, the diagnostic footer gone, `RELEASE = "1.0.5"`, and `docs/field-box-convention.md` (D-9).

**Fences held, proven by diff:** zero changes under `app/services/` and `supabase/`; `app.json` `version`
still `1.0.0`; `editLogic.ts` untouched; and — the one that mattered most — **zero source lines in
`HutangDetailScreen.tsx` or `DetailSewaScreen.tsx`**, which is the entire basis on which RG-1 was approved.
`SearchField` / `Stepper` / `PhotoRow` / `FieldCard` read but not edited (guard 2 held).

### The finding that matters most from ④ — and it arrived twice, independently `[by-agent]`

**`PembayaranSheet`'s three fields were never "bare treatment #8".** ④ read the source before editing and
found all three already carried a border and a fill — *Jumlah* with `surfaceContainerLow` + `outlineVariant`
+ radius 8 **and a fixed `height: 64`**; the description/Notes pair with `surfaceContainerLowest` (`#ffffff`)
+ `outlineVariant` + radius 8.

**This claim originated in ①'s F-1, was carried into RG-1, into Farrel's approval, into A-2's brief, and into
④'s own brief — five documents deep, never verified.** It changed no action (all three needed migrating
regardless) but it changes the *record*: BR-1's box replaces a wrong-tokened box, not nothing, and the
inventory was under-counted.

**The concurrent `/product` session reached the identical conclusion independently**, from the source,
and corrected PRD-8's inventory **10 → 12 treatments** (treatment #11 = *Jumlah*; #12 = the
description/Notes pair). Two agents, no shared context, same correction. *That* is what makes it safe to
believe. Recorded as the release's fourth instance of the project's most expensive habit — reasoning about
an artefact nobody opened — and its cheapest cure.

### ④'s one decision beyond its literal brief `[by-agent]` — flagged, not waved through

④ **deleted** `app/screens/BerandaScreen.fontScale.acceptance.test.tsx` (1 suite / 1 test). Its brief named
only *"`VersionFooter.tsx` and its two tests"*. ④'s account: the suite existed solely to prove the
diagnostic line rendered inside `BerandaScreen`, its single assertion was
`waitFor(() => getByText("fontScale 1.5 · inset 48"))`, and with the line gone there was no meaningful way
to update it — only delete it.

**That account is plausible and it is UNVERIFIED — Lead cannot open code to check it.** It is the same shape
as ②'s `require("react-native")` precedent claim, and it is recorded the same way rather than accepted.
**The risk if the account is incomplete is specific and live:** that file is a **v1.0.4 PRD-5 acceptance
test**, and v1.0.4 is *still awaiting Mom's sign-off*. Silently reducing v1.0.4's evidence while its own
release is open would be a real cost, paid invisibly.

**Action:** ④b's continuation message asks ④ to reproduce the deleted file's full prior content, state what
each assertion covered, and confirm whether any *other* test covers PRD-5's clamp behaviour on
`BerandaScreen` — with instructions to **restore the file** if its coverage extended past the diagnostic
line. Nothing ④b does depends on the answer, so the question rides along rather than costing a round trip.

### PRD-8 A-1 + A-2 — written, in parallel, by a concurrent `/product` session `[by-Farrel]`

Farrel ran the Appendix A prompt while ④ worked. `docs/prd/PRD-8-editable-vs-readonly.md` now carries both
amendments (+277 / −10), read and confirmed by Lead:

- **A-1** splits AC-4 into **AC-4a** (IN) / **AC-4b** (deferred to PRD-6's release, *"deferred, not
  weakened"*, with the obligation written down and AC-4c's two boxes required to disappear when it lands) /
  **AC-4c** (IN, explicitly a bridge with a defined end). The superseded text is preserved struck-through so
  the change is traceable.
- **A-2** adds `PembayaranSheet`'s three Fields to the inventory, records the four-consumer shared-component
  distinction, corrects the treatment count **10 → 12**, and states explicitly that **AC-7's allow-list
  remains exactly `LoginScreen` + `DetailSewaScreen`** and that the gate's *"any third entry is a scope
  breach"* stands **untouched and unweakened**.

**⑦'s blocker is cleared.** D-5 is discharged.

### ⚠️ F-8 · A one-off flake observed **on the AC-9 instrument** — not reproduced, not dismissed `[by-agent]`

④ reported, unprompted and against its own interest, a single transient timeout under full-suite parallel
load: **`PembayaranSheet — existing-payment edit and delete › deleting an existing payment`**, inside
`PengembalianScreen.characterization.test.tsx`. It passed in isolation and in ④'s two subsequent clean runs.

**Lead probed it before letting any other agent touch the tree** — because a write mid-run would have
contaminated the evidence. **Four consecutive full-suite runs, all `52 suites / 284 tests` passing.** Six
clean runs total across both parties; **the flake did not reproduce once.**

**It is recorded rather than closed, and the reason is specific.** This test lives in the **AC-9
characterisation suite**, and ⑤'s brief says *"any delta = STOP and report, never edit the test."* A test
that fails once in seven runs corrupts that instruction in both directions: it manufactures a false STOP,
and — far worse — it teaches whoever meets it that a red characterisation test can be waved off as flake.
**That is the precise habit AC-9 exists to prevent**, and it would be acquired at the exact moment the
release most needs the opposite reflex.

**Carried to ⑤ and ⑦ as a named, bounded exception:** if this specific test fails, re-run it in isolation
before concluding anything — **and report either outcome to Lead rather than deciding alone.** No other
characterisation failure gets that treatment. If it recurs, it is a suite defect owed to a `tester`, not
something ⑤ may work around.

### The `BerandaScreen.fontScale` deletion — audited, and the deletion stands `[by-agent]`

④ was asked to reproduce the deleted file and account for every assertion, **read-only, with instructions
to stop rather than restore** if it concluded restoration was needed. It pasted the file. The evidence
settles it:

- **The file contained exactly one `describe` with exactly one `it`.** Its sole assertion was
  `getByText("fontScale 1.5 · inset 48")` — the diagnostic footer's exact string. Everything else in the
  file is mocking scaffolding.
- **Its own header comment scoped it explicitly**, and the code matches the comment:
  *"It does NOT prove Beranda 'looks right' at 1.5x, and it does NOT prove Mom's own device reports a
  fontScale near 1.5."*
- **PRD-5's clamp durability lives elsewhere** — `AppText.clampAudit.acceptance.test.ts`, an app-wide
  *source* audit asserting `MAX_FONT_SCALE` is declared exactly once and overridden nowhere under `app/`.
  It covers `BerandaScreen.tsx` by construction and is untouched by this deletion.

**Verdict: the deletion was correct and nothing needs restoring.** One nuance recorded for accuracy rather
than glossed: the assertion string also encoded the **inset** (`inset 48`), so the file did incidentally
prove a safe-area value reached JS on that screen. That proof travelled *through* the diagnostic line, which
D-1b specified as throwaway and which this release removes by design — so the loss is the intended one, not
a side effect. v1.0.4's inset mechanism was separately device-validated at **47.27px**.

**The process point is worth more than the outcome.** ④ went beyond its literal brief, *said so unprompted*,
and its reasoning turned out to be right. It was still audited — because Lead cannot open code, the claim
was unverifiable, and *"plausible and unverifiable"* is exactly the state that has cost this project four
releases running. An audit that confirms the agent was right is not wasted; it is the only thing that makes
"the agent was right" a fact rather than a hope.

---

## ④b — returned 2026-07-26. Green. The strips are in.

**Lead re-ran everything.** `52 suites / 284 tests` passing · lint **122, +0**, breakdown byte-identical ·
`npx tsc --noEmit` **exit 0** · `git status --short` shows `RentalDetailScreen.tsx` as the only new
modification, **net −1 line** — consistent with a subtractive strip plus two additive wraps.

| AC | Outcome |
|---|---|
| **AC-4a** | The two `surfaceContainerLow` single-value blocks removed — *Tujuan* and read-only *Catatan*. The now-unreferenced `insetBlock` style deleted with them, so the strip does not trade a misleading tint for a `no-unused-styles` violation |
| **AC-4c** | `kmEditInput` (`:520`) and `notesInput` (`:827`) boxed — the real JSX sites, per C-3 |
| **AC-5** | `UserDetailScreen` **audited, unchanged.** No `TextInput`, no `FieldBox`, no `surfaceContainerLow`; every value row already the plain pattern. Its one bordered element is the delete **button**, a Control under BR-4. **It already complied** |
| **AC-11** | `canEditKondisiKeluar` / `canEditNotes` consumed unmodified, **zero diff** on the file defining them. No grant, no revoke (BR-9) |

**A judgment call ④b made and justified, which I am endorsing `[by-agent]`:** it did **not** add a
replacement label inside the stripped *Tujuan* / *Catatan* cards. The section already carries a `headlineSm`
header directly above, so BR-2's label-above-value pattern is satisfied by what is already there; a second
label would be a **new treatment**, which BR-2 forbids. That is the correct reading — the strip is supposed
to remove a signal, not substitute a different one.

### C-5 · The AC-11 file path is wrong in three documents `[by-agent]`

**There is no `app/utils/editLogic.ts`.** The permission logic lives at
**`app/screens/RentalDetailScreen.editLogic.ts`**, co-located with its screen and imported at
`RentalDetailScreen.tsx:59`. `canEditKondisiKeluar` (`:11`) and `canEditNotes` (`:16`) are both there, both
consumed by plain import, both untouched.

The wrong path appears in **PRD-8's BR-9 text**, in **this report's ④b brief**, and in the **handoff
document** — propagated through three documents without anyone opening the directory. Found the same way
C-1, C-3 and ④'s `PembayaranSheet` correction were found: an agent went to the source.

**AC-11 is satisfied regardless** — a file with zero diff is unmodified whatever its path — but ⑦ must audit
the *real* path or it will report a false pass on a file that does not exist. **Product should correct BR-9's
path when PRD-8 is next touched.**

> **Five corrections in one release, all the same shape.** C-1 (a consumer relationship inferred from a name
> collision), C-3 (line numbers pointing at style blocks), C-4 (the wrong `Stepper`), ④'s treatment-#8 claim
> (five documents deep), and now C-5 (a path that never existed). **Every one was caught by an agent reading
> the file, and none by a reviewer reading a document.** That is the single most reliable finding this
> project has produced about its own process.

### Commits

| SHA | Contents |
|---|---|
| `c82cc24` | PRD-8 Amendments A-1 + A-2 (Product's, committed by Lead) |
| `79e776b` | **④** — `FieldBox`, `RupiahInput`, both form screens, `PembayaranSheet`, the footer removal, `RELEASE = "1.0.5"`, the rule doc |
| `0eb4150` | **④b** — `RentalDetailScreen` strips + the two boxes |

Kept as three commits so the primitive, the strips and the requirements stay reviewable apart.

---

## ⑤ — returned 2026-07-26. **The migration is done, and the last open assumption is closed.**

Fresh `developer-frontend` #2 on `sonnet`. **Lead re-ran everything:**

| Check | Lead measured |
|---|---|
| **Characterisation suite (AC-9 instrument)** | **2 suites / 46 tests passing** — identical before and after |
| Full suite | **52 suites / 284 tests passing** |
| Lint | **120** real errors — **−2** against 122, comfortably inside the +0 budget |
| Types | `npx tsc --noEmit` **exit 0** |
| Scope | `git status --short`: **`PengembalianScreen.tsx` and nothing else.** 338 insertions / 332 deletions |
| Fences | **Zero diff** under `app/services/`, `supabase/`, and `app.json`. `app.json` `version` still **`1.0.0`** |

### ⚠️ E-3 DISCHARGED — the repaired amber test **can** fail, and now we know rather than believe

**This is the release's sharpest open assumption, closed by measurement.** ⑤ changed
`fuelSuggestionRow.backgroundColor` from `colors.warningContainer` to `colors.errorContainer` and re-ran:

```
✕ the fuel-suggestion row renders amber (colors.warningContainer) UNCONDITIONALLY
  Expected: "#FEF7E0"
  Received: "#ffdad6"
```

**A genuine value mismatch, not a thrown "node not found."** That distinction is the whole point: the
*original* test's only failure mode was a thrown locator error, which reads as a broken test and invites the
next agent to loosen the predicate. This one fails the way an assertion should. Reverted, byte-identical,
suite green again at 46/46.

**Guard 3's debt-#12 tripwire is now repaired *and proven*.** The chain that got here is worth keeping:
③ found a test that could not fail → ②c repaired it → nobody could show the repair worked → ⑤ showed it.
Three dispatches and a Lead decision to close one assumption, on a release whose failure mode is invisible.

> **The line number was wrong again.** The brief said ~`:663`; the JSX is at `:664` and the style
> declaration at `:1235`. ⑤ was told to find it rather than trust it, and did.

### What ⑤ delivered

**Boxed (AC-1):** *Kembali* time — **with its `inlineEditBtn` kept and zero interaction change** (same
`TouchableOpacity`, same `onPress`, same `activeOpacity`, now wrapped) · *Tujuan* · *Harga bensin / kotak* ·
*KM Kembali* · *Subtotal Sewa* · every extra-fee description (D-3: boxed, not underlined) and amount.

**Left plain (BR-2):** *Mulai*, *Durasi*, *Total Tagihan*, *Sudah dibayar*, *Sisa*, both `Saat keluar: …`
captions. No greying, no lock glyph, no new treatment.

**AC-2 closed:** `inputRow`'s `height: 48` and `amountInputRow`'s `height: 40` are **gone** — including the
sub-48dp tap target on the money field. ~~⑤ verified by grep that the only two remaining `height:`
declarations in the file are `backBtn` and the local `stepperBtn`, both Controls, both out of scope.~~

> ❌ **THE STRUCK SENTENCE IS FALSE. Corrected 2026-07-27 from ⑥'s D-1 `[by-agent]`. It must not be
> repeated anywhere in this report as written.** There are **seven** `height:` declarations in the file, not
> two: `addLineBtn` (44) · `backBtn` (40) · `fuelSegment` (12) · `fuelSuggestionIcon` (36) · `paymentIcon`
> (36) · `rowDivider` (1) · `stepperBtn` (40) — plus one inline `height:` and a `shadowOffset.height`.
>
> **AC-2 still holds**, because AC-2 governs *field boxes* and none of the seven is one. But the false claim
> concealed a live defect: **`addLineBtn`'s fixed `height: 44` sits on a text-bearing button — a PRD-5 BR-1
> growth exposure and a sub-48dp tap target** — and ⑤'s sentence would have kept it off Lead's desk
> entirely. **Found twice independently, with no shared context:** ⑥ by hunk arithmetic over ⑤'s committed
> diff, and S4 from the opposite direction via Farrel's device finding 4 (Q3.3). **⑨ item 5 closes it at
> `minHeight: 48` + padding.** This is the release's sixth factual correction, and like all the others it
> was caught by an agent reading the file, not by a reviewer reading a document.

**Guards held, each verified by grep rather than recalled:** `returnedAt`'s `new Date()` seed byte-identical
(guard 1) · local `SectionLabel` / `FuelGauge` / `Stepper` / bottom bar still local and untouched (guard 2) ·
`applyFuelSuggestion()`'s body and the unconditional amber both untouched (guard 3) · no math edited
(guard 4) · `DetailSewaScreen` never opened (guard 6) · none of the four named "do not fix" behaviours
touched.

### ⚠️ OPEN QUESTION FOR FARREL — *Catatan* on `PengembalianScreen` is not boxed `[needs Farrel]`

**⑤ followed AC-1 literally and flagged the conflict rather than deciding it silently. It was right to.**

AC-1 enumerates six things and stops; *Catatan* is in neither AC-1's list nor ⑤'s brief. But **it is a live
editable Field under BR-4** — ②b pinned text entry into it flowing through to the close payload — and the
characterisation suite's own `describe` title reads *"text entry into ⑤'s boxed fields — KM Kembali /
Tujuan / **Catatan**"*. The tester assumed it would be boxed.

**This is the same shape as the AC-4 defect that Amendment A-1 fixed this morning:** a criterion's literal
enumeration contradicting the PRD's own governing principle.

**Lead's recommendation `[by-agent]`: box it.** Three reasons, the third close to decisive:

1. **BR-1/BR-4 govern; AC-1's list enumerates.** An omission from an enumeration is not an exclusion.
2. **Unboxed, it would be the only editable field left plain on that screen** — a sharper inconsistency
   *after* the release than before it, and precisely the defect PRD-8 exists to remove.
3. **It likely breaks AC-7 at ⑦.** The fixed manifest puts `PengembalianScreen`'s **8** `<TextInput` tags in
   the *Boxed* category — *"must have a `FieldBox` ancestor"*. If *Catatan* is one of those 8 and is
   unboxed, **layer 2 of AC-7's audit fails**, and ⑦ would be correct to report it. *(Stated as likely, not
   certain — Lead cannot open code to confirm *Catatan* is one of the 8.)*

**Whose call:** the fix is one `FieldBox` wrap, but the *criterion* is Product's. Precedent from D-5/A-1 is
that the release does not amend a PRD by itself. **Cheapest correct path: Farrel rules; Lead sends the wrap
back to ⑤ (still warm); Product records it in PRD-8 alongside A-1/A-2 at any point before ⑦.**

### Two further judgment calls from ⑤, both endorsed `[by-agent]`

**1 · *Diskon* was boxed although AC-1 does not name it — and not boxing it would have been a regression.**
*Diskon* shares `amountInputRow` / `amountInput` with *Subtotal Sewa* and the extra-fee amounts. Since AC-6
required deleting the field-box style set from those shared styles, leaving *Diskon* behind would have
**silently stripped its border and fill** — a brand-new bare money field, a BR-1 violation and a fresh AC-7
defect. There was no non-regressive alternative. Correct call, and correctly reported as a fact rather than
a question.

**2 · The local `FieldCard` was replaced with `<View style={styles.card}>`, not with the shared
`FieldCard`.** The brief said *"the local `FieldCard` goes"*, which is genuinely ambiguous. ⑤ read it as
"remove the local duplicate declaration" rather than "adopt the shared component", because the shared one
ships different spacing defaults (`marginHorizontal` / `marginBottom`, no `gap`) against a screen whose
`ScrollView` supplies its own `gap`/`padding` — swapping would have needed compensating overrides with real
doubled-spacing risk, for a component whose style set (radius 16, shadow, no border, no `minHeight`) is
**not** the set AC-6 tracks. **Endorsed: the zero-visual-risk reading was right on the release's most
dangerous file.** Consequence for the record: this screen's cards are no longer literally the same component
as the form screens'. That is a **visual-consistency** row for ⑦'s checklist, not a correctness defect.

### F-9 · `react-native/sort-styles` sorts case-**sensitively**, and under-reports `[by-agent]`

⑤ went to the rule's source rather than guessing. Two facts worth the register:

- It compares with plain JS `<` / `>` — **UTF-16, not case-insensitive alphabetical**. So `infoRow` must
  precede `inlineEditBtn`, and `paySummary` must precede `paymentIcon`, because uppercase `S` sorts before
  lowercase `m`. A "naturally" alphabetised block is still a violation.
- **`checkIsSorted` returns on the first violation per pass**, so a single clean-looking eslint run
  *understates* how many pairs are actually out of order.

⑤ took the file's `sort-styles` count from 1 to **0** in passing, which is where the −2 lint movement comes
from (the other is one inline-style object folded into a named key).

> **D-6's ratchet now bites in our favour.** The approved ceiling is *"lowest measured baseline + 10"*.
> With the baseline at **120**, the ceiling moves **132 → 130**. Recorded so it is not silently re-read as
> the old number.

### Commit

| SHA | Contents |
|---|---|
| `99a179b` | **⑤** — the `PengembalianScreen` D-5 migration |

---

## ⏸ HALT — Farrel's instruction, 2026-07-26

**Farrel asked to pause once ⑤ returned. Stopping here; ⑥ is NOT dispatched.**

| | |
|---|---|
| Branch / HEAD | `v1.0.5-which-fields-you-can-change` @ `99a179b`, tree **clean** |
| Tests | **52 suites / 284 tests** · characterisation suite **46/46 identical before and after** |
| Lint | **120** (−2) · `tsc` exit 0 |
| AC-9 "before" | **`49fae63`** — unchanged; ⑤ edited no test file |
| Dispatches complete | ① ② ②b ③ ②c ④ ④b ⑤ |
| **Remaining** | **⑥** `rental-math-reviewer` (AC-9 post-migration + fence check) → **⑦** `tester` (durability layer, acceptance, visual checklist) → publish gate |

**Blocking on Farrel:** the *Catatan* question above. It is cheap now and expensive at ⑦.

**Not blocking, but owed at ship:** debt-register additions (F-7, F-8, F-9, the `Terapkan` double-charge,
③'s three other observations, debt #6's ledger row as repaid, C-5's wrong `editLogic` path) · the
`CLAUDE.md` pointer to `docs/field-box-convention.md` · debt #4 / #15 recorded as **partially** paid, never
struck.

---

## ▶ S4 — resumed 2026-07-26, 21:23. Still halted; ~~⑥ still on hold~~ **⑥ had already run — see the correction at § ⑥.**

Session **S4**, Lead on **Opus 5 xhigh** (sanctioned fallback, flagged again). Resumed from
`docs/handoff-prompts/AI_Continuation_Document-26Jul2026-2123.md`. **Nothing above this line was
re-opened.** Phase 1, the re-gate, and dispatches ① ② ②b ③ ②c ④ ④b ⑤ stand exactly as recorded.

### State verified on resume — measured by Lead running the commands `[by-agent]`

| Fact | Value | How |
|---|---|---|
| Branch | `v1.0.5-which-fields-you-can-change` | `git status --short` |
| HEAD | **`32cfd1a`** `docs(v1.0.5): dispatch 5 recorded…` | `git log --oneline -3` |
| Working tree | **NOT clean** — `M app/screens/PengembalianScreen.tsx` (**+4 / −8**, Farrel's own fix) + 1 untracked handoff doc | `git status --short` · `git diff --stat` |
| **Full suite** | **52 suites / 284 tests — 283 passing, 1 FAILING** | `npx jest --json` |
| **The one failure** | **the AC-9 characterisation instrument** — see F-11 | ibid. |
| **Lint** | **121** real non-`prettier` errors — **+1** against ⑤'s 120 | the eslint JSON one-liner in § Pre-execution state |
| Lint delta location | **`react-native/no-unused-styles` 3 → 4.** Every other rule byte-identical to ⑤: `no-inline-styles` 87 · `no-color-literals` 15 · `sort-styles` 11 · `no-unused-vars` 3 · `import/no-unresolved` 1 · **`no-restricted-imports` still 0** | ibid. |
| Types | **exit 0** | `npx tsc --noEmit` |
| AC-9 "before" | **`49fae63`** — unchanged | — |

> ⚠️ **A tooling note worth the next session's time.** In PowerShell 5.1, `npx jest 2>&1 | …` wraps each
> stderr line in a `NativeCommandError` and the pipeline dies with **empty output and exit 1** — which reads
> exactly like a catastrophic test failure and is nothing of the kind. Jest writes its progress and its
> summary to **stderr**. The reliable measurement is `npx jest --json --outputFile=<path>` and then parsing
> `numTotalTestSuites` / `numPassedTests` / `numFailedTests` out of the JSON. **Never** `2>&1` on a native
> executable here.

### 🔴 F-11 · Farrel's uncommitted fix turns the AC-9 instrument RED `[by-agent]`

The previous session's handoff flagged this as an unverified obligation — *"re-run the characterisation suite
before committing it; nobody has verified it against AC-9's instrument yet."* **Lead ran it. It fails.**

```
FAIL app/screens/PengembalianScreen.characterization.test.tsx
● returnedAt — the Kembali picker interaction (D-2: gets the box, keeps its inlineEditBtn,
  no interaction change) › pressing the inline Edit control opens the Android picker,
  and a chosen value reaches the payload

    Unable to find an element with text: Edit
```

**The rendered tree printed by the failure shows the *Kembali* row with the label outside the box (which is
what Farrel intended) and with no "Edit" text anywhere in the tree.** The `accessible={true}` node — the
`TouchableOpacity` — is still present and still wraps the date/time text, so the row very likely remains
tappable; what has gone is the explicit **`inlineEditBtn`** (the ~16px icon + `labelLg` "Edit").

**Three independent signals agree**, which is why this is recorded as measured rather than suspected:

1. The failing assertion is `getByText("Edit")` — a **value/absence** failure, not a thrown locator error.
2. The printed tree contains no "Edit" node.
3. **Lint moved +1, and the added error is `no-unused-styles`** — an orphaned style declaration is exactly
   the residue of removing a styled element while leaving its `StyleSheet` key behind.

**Why this is not a test problem.** The failing test is not incidental coverage — it exists *because of* a
`[by-Farrel]` decision. **D-2** approved the *Kembali* row getting the box **and keeping its `inlineEditBtn`,
with no interaction change**, for a reason recorded at the time: *"the box says 'changeable'; it does not say
'how'. A date/time is not typed — it opens a picker. The explicit `inlineEditBtn` is the only thing on the
row that currently tells Mom that tapping it does something."* And **debt #16 lives on this exact row** — it
already renders a confident wrong default with no in-app correction path, so *reducing* that row's
affordance while the value defect is unfixed is the one direction D-2 specifically ruled out.

**②b then pinned that interaction as a test**, deliberately, as *"the direct test of an approved decision."*
It is doing precisely the job it was written for.

**Consequences, none of them decided by Lead:**

- **Finding 1's triage in the handoff document is wrong and is corrected here.** It read *"presentation, in
  scope, already done by Farrel — re-run the suite, then commit it."* It **must not be committed as it
  stands**: it would leave the branch red, breach the +0 lint brief, and silently reverse a `[by-Farrel]`
  decision.
- **The label placement itself is almost certainly right** and is not in question. What needs settling is
  whether the `inlineEditBtn`'s removal was **intended** (in which case D-2 needs amending on the record,
  and the characterisation test must be re-pointed by a `tester` — never by a developer) or a **side effect**
  of restructuring the row (in which case the button comes back and the suite goes green on its own).
- **Only Farrel can answer that**, because it is his edit and his D-2. Lead is not guessing at it.

### The read-only investigation dispatched at the halt `[by-agent]`

Per Appendix A step 3 of the handoff document, and **explicitly not a fix dispatch**: one `Explore` agent on
`sonnet`, **read-only by tool set** (it has no `Edit`/`Write`), briefed standalone with the prohibitions
verbatim — including a named ban on `git checkout` / `restore` / `stash` / `reset` / `clean` so it cannot
disturb Farrel's uncommitted file.

Three questions, in priority order:

| Q | Question | Why it is read-only work |
|---|---|---|
| **Q1** | What exactly does the uncommitted diff do — is the `inlineEditBtn` removed, is the row **still tappable**, which style key was orphaned, and does it touch any value/handler/math/`returnedAt` seed? | F-11 needs its mechanism established before Farrel is asked to rule |
| **Q2** | **The gating question.** Is the *Pembayaran* section's non-editability on `PengembalianScreen` a **regression** from ④/⑤ or a **pre-existing** capability gap? Demanded with `file:line` on the current source, `git show 49fae63:…` for the before state, the intermediate SHAs, both `PembayaranSheet` call sites, and an explanation of how the suite's *"existing-payment edit and delete"* tests reach that path at all | A regression **must** be fixed in v1.0.5; a pre-existing gap is a **behaviour change on a fenced rental-math screen** that guards 3 and 4 forbid and that AC-9 exists to disprove — it belongs to **PRD-6**. Opposite answers, so inference is not acceptable |
| **Q3** | Three confirmations the record carries as *"Lead cannot open code to confirm"*: is *Catatan* one of the 8 `<TextInput` tags and is it boxed · is `PembayaranSheet`'s *Tanggal* row an unboxed picker row · how do the *"Tambah Pembayaran"* and *"Tambah Biaya"/"Diskon"* button styles actually differ | Removes three unverified claims from the record and lets the eventual single fix dispatch be briefed on facts. Costs one read-only agent |

**Dispatched while Farrel walks the build** — the halt is for his device, not for Lead, and Q2's answer is
the thing his next decision depends on.

### Farrel's five device findings — carried into the record, with F-11's correction to #1

His words, this session, with Lead's triage. **The list is explicitly *"so far"*, not final** — he is still
walking the build, so **no fix is bundled or dispatched yet.**

| # | Finding | Triage |
|---|---|---|
| **1** | *"kembali field in waktu sewa section. the kembali label should be outside the fieldbox. fixed it myself (uncommitted change)"* | 🔴 **Re-triaged — see F-11.** The label move is right; the fix as it stands **removes the `inlineEditBtn`**, turns the AC-9 instrument red, and reverses D-2. **Farrel's ruling needed.** Not committable as-is |
| **2** | *"why is the pembayaran section cant be edited? it should be editable or removable when selesaikan pengembalian hasnt been clicked yet (like the tambah biaya and diskon fields)"* | 🔴 **GATES EVERYTHING.** Regression → v1.0.5 defect. Pre-existing → PRD-6, because adding it is a behaviour change AC-9 exists to disprove. **Q2 of the read-only dispatch** |
| **3** | *"catatan field should be FieldBox as well (it is not currently)"* | ✅ **In scope. This is the third independent signal** on the open question ⑤ raised and Lead already recommended: box it. Farrel's device is now confirming a Lead recommendation, which settles it in practice; Product records it in PRD-8 alongside A-1/A-2 |
| **4** | *"the 'Tambah Pembayaran' button styling should be like the 'Tambah Biaya' or 'Diskon' button styling"* | ⚠️ **Needs Farrel's scope call.** PRD-8's non-goals assign *"the **placement** and **prominence** of edit **buttons**"* to PRD-6. A button's visual consistency with its siblings is arguably not that — but it is also not *"which fields you can change"*. **Lead's read: one-line style alignment, real user value, near-zero risk — but it should be *chosen* and recorded, not absorbed silently** |
| **5** | *"the tanggal field in PembayaranSheet should be using the FieldBox as well"* | ✅ **Correct, in scope, and it exposes F-10 below** |

### F-10 · AC-7 is blind to date/time-picker Fields **by construction**, and a human found it by eye `[by-agent]`

Recorded here because it belongs in the release record, not only in a handoff document.

`PembayaranSheet`'s *Tanggal* row is a **Field** under BR-4 — a recorded value Mom picks — and **D-2 already
set the precedent** by boxing `PengembalianScreen`'s *Kembali* date/time row. ④ skipped it, and **correctly
by its own brief**: it is a `TouchableOpacity`, not a `TextInput`, and it is absent from ①'s enumeration.

**The generalisable fact is the finding: AC-7 audits `<TextInput` tags, so every date/time-picker Field is
invisible to it by construction.** Exactly two such Fields exist in the app — *Kembali*, caught only because
D-2 named it explicitly, and *Tanggal*, missed simultaneously by PRD-8's inventory, the release plan, ①'s
enumeration, AC-7's manifest, ④'s brief and Lead's own audit surface. **No automated criterion in this
release could catch a third.** Only ⑦'s visual checklist can see them, and that is where the obligation
goes, alongside a debt-register row at ship.

**This is the release's fifth instance of the same lesson**, and the first one found by a human looking at a
screen rather than by an agent reading a file: C-1, C-3, C-4, ④'s treatment-#8 correction, C-5 — and now
F-10. Different discovery mechanism, identical shape: **a claim that survived because the artefact it
described was never opened.**

### ~~⑥ — still ON HOLD~~ ⛔ **THIS SECTION WAS FALSE WHEN WRITTEN. ⑥ RAN.** Corrected 2026-07-27 `[by-agent]`

> **What S4 believed, preserved for the record:** *"hold on rental math reviewer since i found a bug that is
> going to be fixed in another session."* — and S4's reasoning that reviewing a diff about to change twice
> over is paying for the review twice.
>
> **What was actually true:** a **second Lead session was running this release concurrently** (from ~21:17;
> S4 resumed at 21:23) and **had already dispatched ⑥ against ⑤'s committed diff `0eb4150 → 99a179b`.** It
> returned. S4 could not know this — the two sessions were invisible to each other, and Farrel ruled the S4
> line the driver only afterwards `[by-Farrel]`. **The hold Farrel stated was real; the parallel session had
> already acted on a narrower reading of it.**

**S4's reasoning was not wrong, and it is why a second pass is still owed.** ⑥ reviewed ⑤'s **committed
migration only**. It did **not** review Farrel's F-11 edit (which did not exist in its artifacts) and it
cannot speak to anything ⑨ changes. **`v1-0-5-dispatch-6.md` does not discharge the final-diff review**, and
that pass is now scheduled after ⑨ lands — see § S5.

---

## ⑥ `rental-math-reviewer` — returned 2026-07-26. **PASS on the money fence; PASS-WITH-CONCERNS overall.**

> **Merged into this report 2026-07-27 from the sidecar `docs/reports/v1-0-5-dispatch-6.md`**, which was
> written as a separate file only because two Lead sessions were editing this report concurrently and
> appending risked corrupting the record. This section is now authoritative; the sidecar is redundant.

**Dispatched** 2026-07-26 by the 21:17 Lead session. **Agent:** `rental-math-reviewer`, read-only, **no Bash
tool**. **Reviewed:** ⑤'s committed diff `0eb4150 → 99a179b` on `PengembalianScreen.tsx`, plus
non-contaminated reads of `FieldBox.tsx`, `FieldCard.tsx`, `rentalMath.ts`, the characterisation suite,
`docs/02` §6, the release plan and PRD-8.

### Scope, and the technique that made it possible `[by-agent]`

At dispatch time the working-tree `PengembalianScreen.tsx` was **contaminated** by Farrel's uncommitted F-11
edit, which that Lead had not yet identified. Rather than let a reviewer read a moving file, Lead staged
**pristine artifacts out of git**:

```bash
git diff <base> <head> -- <path>  > scratchpad/dispatch5-PengembalianScreen.diff
git show <head>:<path>            > scratchpad/PengembalianScreen.at-99a179b.tsx
```

⑥ confirmed it never opened the working tree; **all its line numbers refer to the `99a179b` snapshot.**

> **Reusable, and worth adding to the Lead playbook.** It gives a reviewer with no Bash tool an
> **immutable** view of exactly what was committed, immune to concurrent edits — and it lets Lead supply
> code to a reviewer **without putting code into Lead's own context**, which `docs/agents/lead.md` forbids.
> Use it again for the second pass (Appendix A step 8).

### Method — hunk arithmetic, not impression

⑥ derived cumulative old→new offsets across all **14 hunks** and computed the **untouched old-line ranges**:
**90–403** · 483–501 · 523–555 · 574–589 · 610–631 · 663–690 · 751–783 · 791–962 · 972–1048.

**Old lines 90–403 — state, hydration effect, computed values, picker handlers, extra-fee mutators,
`applyFuelSuggestion`, `handleSave`: the entire logic body of the screen — appear in no hunk at all.**

It also checked the primitive: `FieldBox.tsx:47-49` is `<View style={[styles.box, style]}>{children}</View>`
— **no `cloneElement`, no prop injection, no handlers** — so it *structurally cannot* alter what it wraps.
**That is what makes "component swap only" a proof rather than an impression.**

### The six fence items — all confirmed unchanged

| # | Item | Evidence |
|---|---|---|
| 1 | **Fuel adjustment** | `applyFuelSuggestion()` inside untouched 90–403. `computeFuelAdjustment` import untouched; `rentalMath.ts` not in the commit |
| 2 | **Return total** | `subtotalSewa`, `discount`, `extraFeesComputed`, `totalTagihan = computeReturnTotal(...)` all untouched; `infoRow`'s `minHeight: 40` byte-identical |
| 3 | **`Sisa`** | `alreadyPaid` / `pendingPaid` / `totalPaid` / `sisa = Math.max(0, …)` untouched; `paySummary` render inside untouched 791–962 |
| 4 | **Auto-debt path** | Jaminan banner, `sisa > 0` branch, *"Hutang otomatis dibuat…"*, CTA label all untouched. Debt creation is server-side via `closeRental`; no client-side hutang code exists or was added |
| 5 | **Close payload** | `handleSave`, the `CloseRentalInput` literal with field order intact, `await closeRental(...)`, `navigation.replace(...)` — all untouched |
| 6 | **`returnedAt` seed (guard 1)** | **Byte-identical.** `useState<Date>(() => new Date())` at `:172`, outside every hunk. **Guard 1 held** |

### ⑤'s claims, independently verified

Guards 1, 2, 3, 4 and 6 **confirmed** — notably, **the diverged `FuelGauge` `max` was NOT migrated**, so
guard 2's specific "behavioural change in a refactor's clothing" risk did not materialise. **AC-2
confirmed**: `inputRow`'s `height: 48`, `amountInputRow`'s `height: 40` and `inlineInput`'s `minHeight: 48`
are gone, replaced by `FieldBox`'s `minHeight: 52`. ⑤'s two judgment calls — boxing *Diskon*, and declining
the shared `FieldCard` — were both **confirmed correct, with the alternatives shown to be regressive**.
**One claim was FALSE — see the correction at § What ⑤ delivered (the seven `height:` declarations).**

### Debt #12 — confirmed **preserved**, both divergences `[by-agent]`

`applyFuelSuggestion` still appends an extra-fee line rather than adjusting Subtotal, and
`fuelSuggestionRow` still renders amber unconditionally. **Both unchanged by the diff. Sign semantics remain
`docs/02` §6-correct underneath.** Guard 3 respected — **this is the finding the brief asked for, not a
defect report. Do not "fix" either.**

### ⛔ ⑥'s D-2 · *Catatan* is a **CONFIRMED** AC-7 scope breach, not a suspicion

⑤ *guessed* this; **⑥ verified it from source.** Snapshot `:993` renders a bare
`<TextInput value={notes} onChangeText={setNotes} multiline>`, and `notes.trim()` reaches the close payload
at `:328`. That makes it a **Field under BR-4**, so BR-1/AC-1's *"every field Mom enters or picks renders in
the field box"* covers it even though AC-1's enumeration stops short. **AC-7 forbids a third unboxed
`<TextInput` outside `LoginScreen` + `DetailSewaScreen`, and the release plan calls a third entry *"a scope
breach, not a test detail."*** `PengembalianScreen`'s *Catatan* **is currently exactly that third entry.**

> **⑨ item 3 is therefore MANDATORY, not optional — ⑦ would be correct to fail the release on it.** It is an
> **omission, not an unauthorised change**, so the money verdict is unaffected.

### ⑥'s D-3 / D-4 / D-5 — three smaller divergences

- **D-3 · guard 1's cited line was a pre-migration number.** The release plan and ⑤'s brief say `~:175`;
  post-migration the seed is at **`:172`**. Same code, byte-identical. The seventh instance of this
  release's line-number drift.
- **D-4 · a stale comment inside the AC-9 instrument.** `characterization.test.tsx:464` still claims
  `amountInputRow` uses `height: 40`. It no longer has `height` at all — the helper works for a **different**
  reason (`FieldBox`'s `minHeight: 52` ≠ 40). Harmless today, and **exactly the kind of stale reasoning that
  gets trusted later.** ⑤ correctly did not edit the suite. **Owed to whoever closes AC-9.**
- **D-5 · two intentional style-value edits beyond key reordering**, both cosmetic, neither on a money path
  — recorded so "swap only" is not **over**stated.

### What ⑥ could not verify, and how it is closed

**⑥ has no Bash tool, so it could not run the suite.** AC-9's "identical before and after" is therefore
**analytically argued by ⑥, not observed by it** — and **it said so plainly rather than issuing a PASS it
could not back.** Correct behaviour, and the same tooling limit v1.0.4's reviewer hit.

**Closed from the other side: Lead observed it.** On a clean tree at `99a179b`, Lead measured **2 suites /
46 tests passing, identical before and after**. ⑥ additionally hand-audited **every locator** in the
characterisation suite against the post-migration tree and found all survive, each for a stated structural
reason. **Analytical and observed agree.**

### One AC-10 flag ⑥ raised for ⑦'s visual checklist

The extra-fee row now stacks **two ≥52pt boxes** (`extraFeeDescBox` `flex: 1` + `amountBox` `minWidth: 140`)
**plus a trash icon on one line.** Horizontal room at `fontScale` 1.5 is a device question, not a source
question. **⑦'s AC-10 pass must look there specifically.**

---

## The read-only investigation — returned 2026-07-26. Every question answered with `file:line`.

Read-only `Explore` on `sonnet`. It read the 1402-line file in full, ran `git show` against four commits,
ran eslint on the single file, and **re-ran the failing test itself rather than taking Lead's word for it**.
Farrel's uncommitted file is untouched — `git status --short` still shows the same `+4 / −8`.

### Q1 — F-11's mechanism, established `[by-agent]`

| Question | Answer, with evidence |
|---|---|
| Is the `inlineEditBtn` removed? | **Yes.** The diff deletes `<View style={styles.inlineEditBtn}>` with its `MaterialIcons name="edit" size={16}` and `<Text …>Edit</Text>` |
| **Is the row still tappable?** | **YES — and this is the answer that de-escalates F-11.** The *same* `TouchableOpacity` survives at `:425` with `onPress={openPicker}` and `activeOpacity={0.7}` untouched; `openPicker` (`:249-253`) is byte-identical. The diff deleted only children. **The capability is fully intact; what was lost is the explicit affordance** |
| Which style orphaned? | **`styles.inlineEditBtn` (`:1242-1246`)** — now referenced only by a comment. Confirmed by `npx eslint app/screens/PengembalianScreen.tsx --format json`: *"Unused style detected: styles.inlineEditBtn", line 1242.* The file's other `no-unused-styles` hit, `lateCaption` (`:1293`), is **pre-existing** and labelled as such in a code comment at `:1291` |
| Any value / handler / math / string / `returnedAt` change? | **None.** Checked per category. `returnedAt`'s `new Date()` seed is at `:172` and is in neither hunk — **guard 1 holds** |
| Anything else in the diff? | **One thing, and it is unrelated to the label.** The second hunk (`:713-717`) is a **cosmetic multi-line reflow of the extra-fee row's `"Rp"` `Text`** — identical string, identical style, prettier-shaped wrap. Harmless, but it is not part of the *Kembali* fix and it lands in the extra-fee amount row that ⑤ migrated |
| **A defect the diff introduced that nothing else would catch** | **The JSX comment at `:419-422` is now stale.** It still reads *"PRD-8 D-2: gets the box AND keeps its `inlineEditBtn` — the box says 'changeable', the Edit control says 'how'"*, and describes the label as being inside. **A comment that documents a decision the code no longer implements is worse than no comment** — it is how the next reader concludes D-2 was honoured |

### F-11's resolution path — and it is not a dilemma `[by-agent]`

**The label move and the `inlineEditBtn` were never in conflict.** Farrel's stated intent — *"the kembali
label should be outside the fieldbox"* — is satisfied by hoisting the label alone. The button lived in a
*sibling* position inside the same `TouchableOpacity`, so it can be restored without putting the label back:

```
<Text …>Kembali</Text>                    ← Farrel's fix, kept
<FieldBox>
  <TouchableOpacity style={styles.timeRow} onPress={openPicker} activeOpacity={0.7}>
    <View style={{ flex: 1 }}>
      <Text …>{formatHeaderDate(returnedAt)} · {formatTime(returnedAt)}</Text>
    </View>
    <View style={styles.inlineEditBtn}>   ← restored
      <MaterialIcons name="edit" size={16} color={colors.primary} />
      <Text …>Edit</Text>
    </View>
  </TouchableOpacity>
</FieldBox>
```

**Recommendation `[by-agent]`: restore the `inlineEditBtn`, keep the label outside, refresh the `:419-422`
comment to describe the new structure.** It honours Farrel's fix *and* D-2, turns the AC-9 instrument green
on its own with **no test edited**, and returns lint to **120 / +0** by re-referencing the orphaned style.
**Farrel still rules** — if the button's removal was *intended*, then D-2 needs amending on the record and a
`tester` (never a developer) re-points the pinned interaction.

### Q2 — 🟢 FINDING 2 SETTLED: **PRE-EXISTING capability gap, not a regression** `[by-agent]`

**And the reason nobody could settle it by inference is that "the Pembayaran section" is two sections.**

| Rendering | Lines | Edit / delete affordance |
|---|---|---|
| `rental.payments.map` — payments **already persisted** on the server | `:822-853` | ✅ **Has one.** `editPayBtn` `TouchableOpacity` at `:842-852` (`setEditingPayment(p)` + `setShowPaySheet(true)`), opening `PembayaranSheet` in edit mode with a *Hapus Pembayaran* button wired to `deletePayment` (`:1052-1062`) |
| `pendingPayments.map` — payments added **this session, before Save** | `:855-879` | ❌ **None whatsoever.** Every element is a plain `View` / `Text`. No `TouchableOpacity`, no icon, no `onPress`, no `onLongPress`, no swipe handler |

**Farrel's complaint is about the second one, and his own analogy proves it:** he compared it to *Tambah
Biaya* and *Diskon* — which are **also** session-scoped, not-yet-saved rows, and which **do** carry trash
icons (`removeExtraFee` at `:729-734`, the discount's at `:763-771`). A payment he enters during the return
flow is the one row of that kind he cannot remove.

**The evidence that settles it, in order of strength:**

1. **The `pendingPayments.map` block is byte-identical to `git show 49fae63:…`** — the fixed reference state
   from before any product code moved in this release.
2. **`git show --stat 79e776b` and `git show --stat 0eb4150` do not list the file at all.** ④ and ④b never
   opened `PengembalianScreen.tsx`.
3. **It traces back a month further.** `git log` on the file plus `git show 3244ab6` (June, *"phase 7 stage C
   — edit/delete pembayaran"*) shows `editPayBtn` was added **only inside the `rental.payments` branch**;
   `pendingPayments.map` appears in that diff as unchanged *context*, already control-less. It has been that
   way ever since.

**Why the earlier inference read "probably pre-existing" for the wrong reason.** The previous session argued
from ②b having pinned *"`PembayaranSheet` existing-payment edit and delete"*. Those tests **do** mount
`PengembalianScreen` and **do** press a real, reachable Edit control — but only on a payment supplied via
`rental.payments` (`characterization.test.tsx:1127`, `:1173`, via the `openExistingPaymentEdit` helper at
`:488-502`). **They never construct a `pendingPayments` entry**, because no control exists on that branch to
press. So the tests and Farrel's device were both right about different halves of the same section, and the
conclusion happened to be correct on reasoning that did not actually support it. **Sixth instance of this
release's one recurring lesson**, and the first where the *conclusion* survived and only the *reasoning* was
wrong — which is the harder version to catch.

### The consequence is sharper than "it belongs to PRD-6" `[by-agent]`

**Adding a remove control to `pendingPayments` is not an affordance change — it is a money-path change.**
`pendingPayments` feeds `totalPaid` → `Sisa` (`:242`, `Math.max(0, totalTagihan − totalPaid)`) → **the
`closeRental` payload and therefore the server-side auto-debt creation**. A row Mom can delete is a row that
changes what gets written at close.

That places it squarely inside **guards 3 and 4** (*"no math moves"*, *"this is a component swap"*) and
inside the thing **AC-9 exists to disprove**. It is the **identical shape** to the `Terapkan` double-charge,
which Farrel already routed to PRD-6 by adopting Lead's recommendation `[by-Farrel]` — and the same
consequence applies: **the characterisation suite does not pin this behaviour**, so whoever builds it will
need to write its own "before" protection first, exactly as BR-12 required here.

**Lead's read `[by-agent]`: this goes to PRD-6, and to the debt register at ship.** It is a real gap and a
real irritation for Mom — a mis-typed payment amount entered during a return currently has no correction
path short of abandoning the screen — but v1.0.5 is a presentation-only OTA whose entire safety story is
*"nothing but appearance moved"*. **Farrel can overrule; the factual question is now closed either way.**

### Q3.1 — *Catatan* confirmed: it is tag **8 of 8**, and it is **not** boxed `[by-agent]`

The report's *"likely fails AC-7 layer 2 — stated as likely, not certain, Lead cannot open code"* is now
**certain**. All eight `<TextInput` tags enumerated with line numbers; seven have a `FieldBox` ancestor,
*Catatan* (`:989`, inside `<View style={styles.card}>` at `:988`) does not. AC-7's fixed manifest puts all
**8** of this file's tags in the *"must have a `FieldBox` ancestor"* category, so **⑦ would correctly report
a layer-2 failure.** ⑤ documented the omission deliberately in a JSX comment at `:981-985` and flagged it —
which is exactly the behaviour that was wanted. **Boxing it closes an audit failure as well as a
presentation inconsistency.** Three independent signals now agree: ⑤'s flag, Lead's recommendation, Farrel's
device.

### F-12 · Finding 5 is bigger than "wrap it" — `PembayaranSheet` carries a **second, rogue field-box style** `[by-agent]`

`PembayaranSheet`'s *Tanggal* row (`:263-289`) is a `TouchableOpacity` date-picker row, as expected. What
was not expected is that **it is not unstyled — it declares its own box**, `dateRow` (`:373-383`):

| Property | `dateRow` (`PembayaranSheet.tsx:373-383`) | `FieldBox` (`FieldBox.tsx:52-61`) | Verdict |
|---|---|---|---|
| Fill | `colors.surfaceContainerLowest` | `colors.surface` (D-1, `#f6faff`) | wrong token |
| Border | **`colors.outlineVariant`** | `colors.outline` | **the exact token BR-3 retired from field borders** |
| Radius | `8` | `borderRadius.default` (12) | inconsistent |
| Height | **`height: 56`** | **`minHeight: 52`** | **a fixed `height` — the thing BR-8 and AC-2 exist to eliminate** |

**Three audits are blind to it, each for a different structural reason:**

- **④ skipped it correctly by its own brief** — not a `TextInput`, absent from ①'s enumeration.
- **AC-7 cannot see it** — it audits `<TextInput` tags. This is **F-10** in the concrete.
- **AC-6 probably cannot see it either** — AC-6 fails on a second declaration of *the field-box style set*,
  and `dateRow` matches that set's *shape* while sharing none of its tokens and using `height` where the set
  uses `minHeight`. **A near-miss duplicate is invisible to an audit that matches on the exact set.**

`FieldBox`'s own doc comment (`:25-28`) states the rule this violates: *"this is the ONE declaration site… a
consumer that needs a variant composes via the style prop — it does not carry its own copy."*

**Two consequences that make this the most valuable of Farrel's five findings:**

1. **It is a live PRD-5 text-scale risk on Mom's device, not just a cosmetic inconsistency.** A fixed
   `height: 56` on a row containing `bodyLg` text is precisely the clipping shape v1.0.4 shipped to fix, and
   `PembayaranSheet` is on the path Mom walks at **every** handover. `56` clears the 48dp tap target, so this
   is a growth problem rather than a touch problem — which is why no v1.0.4 audit row caught it either.
2. **AC-6's mutation test should be re-examined at ⑦.** If a near-miss duplicate passes, AC-6's durability
   claim is narrower than the report currently states. **Recorded as an instruction for ⑦, not as scope.**

### Q3.3 — Finding 4's facts, and a wrinkle the scope call needs `[by-agent]`

All three buttons live in `PengembalianScreen.tsx`. Text style is **identical** in all three
(`textStyles.labelLg` + `colors.primary`); the container is not.

| | *Tambah Biaya* / *Diskon* — `addLineBtn` (`:1082-1092`) | *Tambah Pembayaran* — `addPaymentBtn` (`:1093-1099`) |
|---|---|---|
| Border | `borderWidth: 1`, `colors.outlineVariant` | **none** |
| Fill | `colors.surface` | **none** (transparent) |
| Radius | `10` | none |
| Height | **`height: 44` (fixed)** | none — `paddingVertical: spacing.md`, content-driven |
| Icon | `18` | `20` |
| `activeOpacity` | `0.7` | `0.8` |

Farrel's read is correct: two of the three render as a bordered pill and the third as bare icon+text.

> ⚠️ **The wrinkle, and it inverts the obvious fix.** *"Make `addPaymentBtn` look like `addLineBtn`"* would
> copy **`height: 44`** onto a second button — a **fixed** height (clips at `fontScale` 1.5, the PRD-5
> failure mode) that is also **below the 48dp tap target** v1.0.4 and AC-2 both treat as the floor. Buttons
> are **Controls** under BR-4 and are outside AC-2's remit, so this is not currently anybody's violation —
> but *propagating* it in a release whose sibling criterion bans exactly that shape would be a poor trade.
> **If finding 4 is approved, the alignment should be `minHeight: 48` on both, not `height: 44` on both** —
> which makes it a two-line change touching `addLineBtn` as well, and therefore a slightly larger scope call
> than it first appears. Stating it so the call is made on the real cost.

---

## Farrel's two rulings — 2026-07-26 `[by-Farrel]`

Given while Lead was writing up the investigation, so they answer F-11 and finding 4 on the facts as they
stood *before* the Q1/Q3.3 detail above was available. Both are recorded verbatim.

### 1 · F-11 — **removing the inline "Edit" control is INTENDED. D-2's second clause is superseded.**

> *"removing the inline 'Edit' button is intended since clicking the fieldbox already give interaction on
> editing"*

**What this changes in the record.** D-2 (approved `[by-Farrel]` 2026-07-25) had two clauses. The first —
*"the Kembali row is a Field under BR-4, so it wears the box"* — **stands, unchanged**. The second —
*"keep the `inlineEditBtn`, no interaction change"* — is **superseded as of 2026-07-26 `[by-Farrel]`.**

**His rationale is a claim about PRD-8's own convention, and it is internally consistent:** if boxed means
*"Mom can change this"*, then a row that is boxed does not also need a word telling her so. D-2's original
counter-argument was that a date/time is not *typed*, so the box says "changeable" without saying "how" —
Farrel has weighed that and ruled the box sufficient. **His call, his design, recorded as made.**

**The accepted consequence, stated so nobody rediscovers it as a surprise.** This is the row **debt #16**
lives on — the `returnedAt` default that already renders a confident wrong time and has produced one wrong
persisted record with no in-app correction path. After this change, the box is the **only** signal on that
row that the value can be corrected at all. That makes the *Kembali* row **the single purest test of PRD-8's
central premise anywhere in the app**: every other boxed Field also contains a caret, a placeholder or a
keyboard. This one contains nothing but a formatted date inside a box.

> **Therefore it earns a named row in ⑦'s visual checklist and in Mom's AC-12 walk, phrased as a task rather
> than a look:** *"Ask Mom to change the return time without being shown how."* If the convention alone
> cannot carry that row, PRD-8's premise has a measurable counter-example — and this is the cheapest place in
> the app to discover it. Recorded as the strongest single acceptance question v1.0.5 has.

**Four consequences that must all land, and one of them changes who does the work:**

| # | Consequence | Owner |
|---|---|---|
| 1 | **The pinned characterisation test must be re-pointed** — `getByText("Edit")` (`characterization.test.tsx:1034`) becomes a press on the row's `TouchableOpacity`, which still exists and still calls `openPicker`. The interaction *is* still there; only its handle moved | **A `tester`. NEVER a developer** — the standing rule is absolute, and this is the one case in the release where a characterisation test legitimately changes, so it must change under the right hand |
| 2 | **`styles.inlineEditBtn` (`:1242-1246`) must be deleted** — it is now orphaned, and it is the whole of the `120 → 121` lint movement. Deleting it returns the release to **+0** | `developer-frontend` |
| 3 | **The JSX comment at `:419-422` must be rewritten.** It currently documents D-2's superseded clause as though it were implemented — the most misleading artefact in the diff | `developer-frontend` |
| 4 | **Product records D-2's supersession in PRD-8** (it is OQ-2's resolution) alongside A-1 / A-2 | `/product` |

**Consequence 1 is the structural one: the remaining work is no longer a single `developer-frontend`
dispatch.** It needs a `tester` leg first, because until the instrument is green there is no working "before"
state for the developer to run against — the whole AC-9 protocol depends on it.

### 2 · Finding 4 — **a style alignment is in scope, and recorded rather than absorbed**

> *"right, a style alignment should be recorded"*

**Recorded `[by-Farrel]`.** *Tambah Pembayaran* is aligned with *Tambah Biaya* / *Diskon* in v1.0.5, and the
scope decision is written down here rather than slipped in — which was the whole point of raising it. PRD-8's
non-goal (*"the **placement** and **prominence** of edit **buttons** — that is PRD-6"*) is **not** reopened:
this is three sibling buttons on one screen made to look like each other, not a decision about where edit
buttons go or how prominent they should be. **Product should note it in PRD-8 so a future reader does not
read the non-goal as having been breached.**

**One narrow question remains open, and it is the Q3.3 wrinkle Farrel had not seen when he ruled** — whether
the alignment target is `addLineBtn` **as it is** (`height: 44`, fixed and sub-48dp) or a corrected
`minHeight: 48` applied to **both** styles, which changes *Tambah Biaya* and *Diskon*'s appearance too. Put
to Farrel separately; **the scope decision above is not contingent on it.**

### 3 · Finding 4's alignment target — **`minHeight: 48` on both** `[by-Farrel]`

Farrel took the recommended option over the two smaller-diff alternatives: **the alignment corrects
`addLineBtn`'s fixed sub-48dp `height: 44` rather than propagating it.** So *Tambah Biaya* and *Diskon*
change appearance too — slightly taller at default scale, and **growing rather than clipping at
`fontScale` 1.5**.

**Accepted cost, recorded:** two additional visual-checklist rows, and two controls changing that were not on
the release's original audit surface. **What it buys:** a fixed height on a money-screen control is retired
instead of duplicated, in the release whose sibling criterion (AC-2/BR-8) exists to ban exactly that shape.

**Two refinements Lead is adding inside the approved decision `[by-agent]` — neither changes what Farrel
chose:**

**(a) `minHeight: 48` is added; `paddingVertical` is NOT removed.** The option preview showed
`- paddingVertical: spacing.md`, but `minHeight` alone leaves text touching the border once the content
exceeds 48dp at `fontScale` 1.5 — which is the failure v1.0.4 spent a whole release learning to avoid. The
correct pattern, and the one v1.0.4 established: **`minHeight` + padding, never a fixed `height`.** ⑨ keeps
the padding on `addPaymentBtn` and adds `paddingVertical` to `addLineBtn` if it has none.

**(b) 🔴 The buttons must NOT adopt `FieldBox`'s tokens, and this is a requirement, not a preference.**
`addLineBtn` uses `colors.outlineVariant` + `borderRadius: 10`. `FieldBox` uses `colors.outline` +
`borderRadius.default` (12). **The temptation at ⑨ will be to "unify" them — and doing so would actively
break the release.** A bordered, filled control wearing the field-box border colour and radius **reads as a
field box**, and BR-1's entire proposition is *boxed = a value Mom can change*. Three buttons that look like
field boxes would put the convention's own counter-examples on the screen it is proudest of.

> **The distinction to carry into ⑨'s brief verbatim:** the three buttons are aligned **to each other**, and
> **deliberately kept distinguishable from `FieldBox`**. `outlineVariant` + radius 10 on Controls;
> `outline` + radius 12 + `surface` fill on Fields. **BR-3 retired `outlineVariant` from *field* borders
> only** — on a Control it is not a violation, it is the signal that this is not a Field. Recording it
> because a future agent reading "one border token app-wide" as a tidiness goal would erase the distinction
> and could not be blamed for it: **nothing currently written down says the two must differ.** It belongs in
> `docs/field-box-convention.md` at ship.

---

## ⏸ The remaining plan — **held. Awaiting Farrel's "the list is final".**

**Nothing is dispatched.** Farrel is still walking the dev build and his five findings are explicitly
*"so far"*. Bundling now and re-bundling on a sixth finding costs a dispatch; waiting costs nothing. **⑥
remains on hold independently.**

### ⑧ `tester` — re-point the D-2 pinned interaction — model `sonnet`

**The only legitimate characterisation-test change in this release, and it exists because Farrel superseded
D-2's second clause `[by-Farrel]`.** Brief must state that provenance explicitly, so the agent understands it
is implementing an approved decision rather than accommodating a failure.

1. **Scope: exactly one test.** `PengembalianScreen.characterization.test.tsx:1034` —
   `getAllByText("Edit")[0]` becomes a press on the *Kembali* row's `TouchableOpacity`, which **still exists
   at `PengembalianScreen.tsx:425` with `onPress={openPicker}` unchanged**. The interaction is intact; only
   its handle moved. **The assertions on the chosen value reaching the payload must not weaken** — that is
   the part D-2 was protecting and it is untouched by Farrel's ruling.
2. **The test's `describe` title still says "keeps its inlineEditBtn".** Update it to describe the ruling as
   it now stands, and cite the supersession date so the next reader is not confused by the report's earlier
   D-2 text.
3. **Touch no product code.** The orphaned style and the stale JSX comment are ⑨'s, not yours.
4. **Report the suite at 2 suites / 46 tests green**, and the full suite at 52 / 284.

**Then Lead commits Farrel's edit and ⑧'s re-point as ONE commit** — the branch never carries a red commit,
and a reader sees the product change and its sanctioned test change in a single diff with D-2's supersession
in the message.

### ⑨ `developer-frontend` (fresh) — the device-findings bundle — model `sonnet`

Briefed standalone with the guards verbatim (E-1's lesson), ④'s handoff note, and **the instrument protocol:
run the characterisation suite before and after; any delta = STOP and report; never edit a test.**

| # | Item | Source |
|---|---|---|
| 1 | **Delete `styles.inlineEditBtn`** (`PengembalianScreen.tsx:1242-1246`) — orphaned by Farrel's edit; it is the whole `120 → 121` lint movement | F-11 consequence 2 |
| 2 | **Rewrite the JSX comment at `:419-422`** — it documents D-2's superseded clause as though implemented | F-11 consequence 3 |
| 3 | **Box *Catatan*** (`:989`, tag 8 of 8, confirmed unboxed) — closes an AC-7 layer-2 failure as well as the inconsistency | Finding 3 |
| 4 | **Replace `PembayaranSheet`'s `dateRow` with `FieldBox`** (`:263-289` / `:373-383`) — retires a rogue duplicate box style carrying `outlineVariant` and a **fixed `height: 56`** | Finding 5 / **F-12** |
| 5 | **Align the three buttons at `minHeight: 48` + padding**, keeping Controls visually distinct from Fields | Finding 4 `[by-Farrel]` + refinements (a)/(b) |

**Fences unchanged and reproduced verbatim:** guard 1 (`returnedAt`'s `new Date()` seed at `:172` untouched) ·
guard 2 (local `SectionLabel` / `FuelGauge` / `Stepper` / bottom bar stay put) · guard 3 (debt #12's two
divergences stay pinned) · guard 4 (no math) · guard 6 (`DetailSewaScreen` not opened) · **no `pendingPayments`
edit/delete affordance — that is Q2's pre-existing gap and it is PRD-6's, and adding it would change the
`closeRental` payload** · lint back to **120 / +0** · `app.json` `version` stays `1.0.0` · zero diff under
`app/services/` and `supabase/`.

### Then, in order

**⑥** `rental-math-reviewer` — **only when Farrel lifts the hold**, and only after ⑨ lands, since its job is
to review the final diff. → **⑦** `tester` (durability layer, acceptance, visual checklist — now carrying
the *Kembali* AC-12 task row, F-10's picker-Field rows, F-12's two `PembayaranSheet` rows, the two button
rows, and the instruction to re-examine AC-6's mutation test against a near-miss duplicate). → **publish
gate** → OTA → Mom's combined AC-12 / v1.0.4 AC-8 visit.

### Also owed at ship, now longer by four rows

The existing list (F-7, F-8, F-9, the `Terapkan` double-charge, ③'s three observations, debt #6's ledger row
as repaid, C-5's wrong `editLogic` path, the `CLAUDE.md` pointer, debt #4/#15 as **partially** paid) plus:
**F-10** (AC-7 blind to picker Fields by construction) · **F-11** (D-2 superseded; the *Kembali* row now
carries no explicit affordance, on the row debt #16 lives on) · **F-12** (`dateRow`'s rogue box, and AC-6's
near-miss blind spot) · **the `pendingPayments` edit/delete gap** (pre-existing, PRD-6, money-path, and
**unpinned by the characterisation suite** — its eventual fix needs its own "before").

---

# ⚠️ AMENDMENT — the *waktu kembali* display defect · dispatch ⑧ · **AWAITING APPROVAL**

Raised by Farrel 2026-07-26, during the halt, as **P0**, with the instruction *"put it in v1.0.5"* and
*"talk to PM regarding this."* PM consulted (read-only, `sonnet`); its findings are below and they change
the shape of the problem substantially.

**Nothing in this section is dispatched. ⑥ is still unstarted. This is a Phase-1 gate on new scope.**

## The defect as reported

1. In `PengembalianScreen`, Mom edits **waktu kembali**.
2. She taps **Selesaikan Pengembalian**.
3. She lands on `RentalDetailScreen`.
4. The *waktu kembali* shown is the value from the original **sewa baru** flow (`DetailSewaScreen`), not
   the one she just entered.

## PM's diagnosis — it is a **read-side omission**, and every other hop is correct

PM traced the whole chain against the source rather than the docs. Reproduced here because it is the
finding the entire scoping rests on:

| Hop | Site | Behaviour | Verdict |
|---|---|---|---|
| Screen state → payload | `PengembalianScreen.tsx:172`, `:318` | edited `returnedAt` enters the save payload | ✅ |
| Connector | `app/services/rentals/index.ts:422` | `returnedAt: input.returnedAt.toISOString()` | ✅ |
| RPC write | `…close_rental_exclude_deleted_payments.sql:84` | `returned_at = (payload->>'returnedAt')::timestamptz` — **the client's value, not `now()`** | ✅ |
| Read back | `translators.ts:62` (`rowToRental`) | `returnedAt: row.returned_at ? new Date(…) : null` | ✅ |
| **Display** | **`RentalDetailScreen.tsx:433`** | `{formatHeaderDate(rental.dueAt)} · {formatTime(rental.dueAt)}` under the label **"Kembali"** | ❌ **the defect** |

`dueAt` is set at `DetailSewaScreen.tsx:329` (`dueAt: estimasi`) — **exactly** the sewa-baru value Farrel
sees. Symptom and diagnosis match with no residue.

**The decisive corollary: `rental.returnedAt` is rendered nowhere in `app/`.** Outside its own type,
translator and `PengembalianScreen`'s local state it appears only in test fixtures — all of them
`returnedAt: null`. The screen already holds a full refetched `Rental` (`:117`, via `getRental` at `:142`),
so the correct value is **on the screen object and simply never read**.

### Three corrections to Lead's own framing, and they matter `[by-agent]`

1. **Nothing is lost and nothing is corrupt.** Lead's opening hypothesis — *"the edit is being lost
   downstream of the screen"* — was wrong. The edit is in the database, correct, on **every** rental Mom
   has ever closed. There is **no** historical record to repair, **no** audit in the shape of v1.0.3's
   `deleted_at` audit, **no** hand-correction owed. It also rules out Lead's stale-nav-param
   sub-hypothesis: the screen refetches on mount.
2. **It is a missing requirement, not a typo — and the one-line fix is the wrong fix.**
   `dueAt` under "Kembali" is **correct for an ACTIVE rental** (it is when the vehicle is due back; it
   legitimately drives the `error` colour at `:429` and the Terlambat banner at `:446-452`, which *is*
   correctly ACTIVE-gated). The row was simply never made status-aware. So
   `rental.dueAt → rental.returnedAt ?? rental.dueAt` **must not be the fix**: it makes one label mean two
   different things by status — the exact defect class PRD-8 exists to eliminate — and it destroys the
   planned-vs-actual comparison that is the only lateness record on the permanent screen.
3. **The hole is bigger than one field.** On a COMPLETED rental the *entire* Waktu Sewa card is planned
   data: Kembali = planned `dueAt` (`:433`), Durasi = `formatPaket(paketHari, paketJam)` (`:441`) = the
   **booked package**, not what happened. There is no actual return time, no actual duration and no actual
   lateness anywhere on the permanent record. `formatActualDuration` exists only as a **local** function at
   `PengembalianScreen.tsx:60`, and its output is transient. **Named here, then deliberately declined** —
   see the cut line and PRD-9.

### Not a v1.0.5 regression `[by-agent]`

④b touched *Tujuan*, read-only *Catatan*, `kmEditInput`, `notesInput`; ⑤ touched `PengembalianScreen`
only. **Neither went near the Waktu Sewa card at `:410-454`** — PM confirmed this independently against
the source. **AC-9 did not fail and ⑤ broke nothing.** This is pre-existing, almost certainly v1.0-era.

> **Lead did not verify this by reading code, deliberately** (`docs/agents/lead.md` forbids it). It rests
> on ④b's recorded diff plus PM's independent source read — two sources that agree. **⑧ confirms it with
> `git log -L` as its first act**, and the report is corrected if it turns out otherwise.

### Severity — PM argues **P1/high**, not P0, and the scheduling does not change `[needs Farrel]`

By this project's own precedent P0 has meant *blocks the business day* (v1.0.4's inset) or *moves money*
(v1.0.3's `deleted_at`). This does neither: the handover completes, the totals are right, the hutang is
right, and the true time is in the database. **No money recomputation** — confirmed: `isLate` / `jamLambat`
are `PengembalianScreen`-local and feed only the banner; late fees are hand-entered `extraFees`; and
`RentalDetailScreen`'s Terlambat banner is ACTIVE-gated so it does not lie on a completed rental.

**But the real harm is behavioural, and it is worse than debt #16's `[by-agent]` —** this is the sentence
worth keeping:

> Debt #16 is the app guessing and Mom not noticing. **This is Mom correcting the guess and the app
> appearing to discard it.** If she concludes that editing that field does nothing, she stops editing it —
> and *then* #16 starts producing wrong persisted records at scale. **This defect is the mechanism by
> which #16 gets worse.**

**Lead recommends accepting P1/high** — not to slow anything (it still lands in v1.0.5 before publish) but
so that *"it's P0"* cannot later be the argument that buys a guard-5 waiver or a halt this defect does not
warrant. **Farrel's label to set.**

## The plan — **⑥ → ⑧ → ⑦.** Fold in; do not hotfix `[by-agent]`, PM concurs and ranked it first

```
  ⑥  rental-math-reviewer   AC-9 post-migration + fence check on ⑤'s diff.  UNCHANGED, FIRST.
        │                   Runs against a final, clean PengembalianScreen. ⑧ touches nothing it reads.
        ▼
  ⑧  developer-frontend #3  The RentalDetailScreen returnedAt display fix. Own cut line, own commit.
        │                   Fresh agent — ④/④b's context is spent and ⑤'s is the wrong screen.
        ▼
  ⑦  tester (fresh)         Durability layer + acceptance, NOW ALSO COVERING ⑧ (AC-13).
        ▼
     ⏸  PUBLISH GATE — plus Farrel's own-device round trip (below), blocking.
```

**Why fold rather than hotfix — four reasons, the last one decisive:**

1. **The reason this release plan kept things out does not apply.** Every fence in v1.0.5 is about the
   *delivery model* (no migration, no server surface, stay OTA-revertible). This fix is OTA either way. It
   is not a guard-5 question and must not be argued as one.
2. **This is the cheapest moment it will ever exist.** ⑦ is unstarted; fold now and the fix rides the same
   tester pass. After ⑦ you pay for a second one for identical output.
3. **The marginal review surface is genuinely small**, and unlike `PengembalianScreen` this screen **has**
   coverage: `RentalDetailScreen.test.tsx` is 409 lines and already parameterises `"COMPLETED"` at
   `:133-134`. The fixture pattern exists.
4. **It fixes the build Mom validates.** Under the hotfix option she signs off on a build that is not the
   one she keeps, and the release recorded ✅ is not the one on her phone — in a project whose entire
   discipline is that the ledger is honest. Folding makes OQ-5's visit land on a build that tells the truth.

**Why ⑧ after ⑥ and not before:** ⑥ is the gate protecting the release's one genuinely dangerous change,
and its value depends on certifying a final, clean `PengembalianScreen`. ⑧ touches nothing ⑥ looks at, so
there is **zero** coupling to gain by reordering and a muddied certification to lose.

**Rejected: halt v1.0.5 and ship the fix first.** It strands 8 green committed dispatches on a branch, and
a paused branch carrying a fenced rental-math migration *decays* — `master` moves and the AC-9
characterisation baseline was written against a specific `master` state. Spending the release's most
expensive asset to buy nothing, for a bug corrupting no data. (Checked: `master` is at `22b13d5`, **0
commits ahead** of the branch point. The baseline is still valid *today*. That is an argument for moving
now, not for pausing.)

## ⑧'s cut line — written before dispatch, on purpose

**IN.** On `RentalDetailScreen`, a **COMPLETED** rental's Waktu Sewa card surfaces the **actual** return
time from `rental.returnedAt`, labelled so it cannot be confused with the planned one. **ACTIVE behaviour
is byte-for-byte unchanged** — `dueAt`, the `error` colour at `:429`, the Terlambat banner at `:446-452`.
Plus the three tests below.

**OUT — every one of these will look like a twenty-minute win:**

- **Do not extract or reuse `formatActualDuration`** (`PengembalianScreen.tsx:60`). Extracting it is debt
  #4 and it re-opens the fenced screen ⑤ just migrated and ⑥ has not yet certified. **Hard no.**
- **Do not change the Durasi row** (`:441`). It shows the booked package. Making it show actual duration is
  a new requirement adjacent to rental math.
- **Do not add an "actually returned N hours late" derivation.** Real gap; not this fix. → PRD-9.
- **Do not touch `PengembalianScreen`** at all — not its `new Date()` seed, not its picker, not
  `returnedAt`'s value. **Guard 1 still binds and now binds harder**: the screen is post-⑤, pre-⑥.
- **Do not box the new or changed row.** It is read-only. AC-4a / AC-4c already settled what gets a box on
  this screen, and A-1 defined AC-4c as a bridge with an end.
- **No server surface. No backfill of existing rows.** Nothing is corrupt; a backfill would be a migration
  built on a guess.
- **AC-7's allow-list stays exactly `LoginScreen` + `DetailSewaScreen`.** This fix adds no `TextInput`, so
  it structurally cannot breach that — stated so nobody wonders.
- Same lint discipline (**+0** against the new baseline **120**), same standing instruction, no
  `eslint --fix` / `prettier --write` (guard 8).

### The tripwire that makes folding safe — Lead asks Farrel to **pre-authorise** it `[needs Farrel]`

> **If the fix requires anything beyond `RentalDetailScreen`'s render plus its tests** — a connector
> change, a type change, a shared-helper extraction, a server surface, or **one line inside
> `PengembalianScreen`** — **it stops being a ride-along and drops out to a v1.0.6 hotfix immediately,
> without renegotiation.**

PM's verification says it needs none of those. The rule is what keeps *"fold it in"* from becoming the
thing everyone in this project recognises. Pre-authorising it means ⑧ can be stopped by a rule rather than
by a round trip.

## Requirements home — **not debt #16, and not a PRD-8 amendment** `[by-agent]`, PM's recommendation

**Not #16.** Different failure mode: #16 is a **write**-side wrong value; this is a **read**-side omission.
Folding them makes #16 unfalsifiable — you would "fix #16" while the guessed default remained entirely
untouched. **Cross-reference both ways instead**, and record the relationship, which is the genuinely
interesting finding:

> **This defect has been *masking* debt #16 from the app's own surface.** #16 was found by inspecting the
> database while verifying PRD-6 — *not by using the app* — because the app never displays `returned_at`.
> **Fixing the display makes #16's confident guesses visible for the first time, on Mom's screen, on every
> rental.** That is an argument for raising #16's priority at the next scoping round, and a consequence to
> record rather than discover.

**Not PRD-8.** PRD-8 is a presentation convention about *which fields are editable*; this is about *which
value a read-only field shows*. Don't pay the coherence cost. **The release title does not change.**

**So, two artifacts:**

| Artifact | Contents | When |
|---|---|---|
| **Debt #17** — opened and immediately **struck as fixed-in-v1.0.5** | The read-side omission, with the #16 cross-references both ways | at ship |
| **`Also rides along:` line + criterion AC-13** on `docs/releases/v1-0-5.md` | Makes the fix a named, verifiable release line that cannot be silently forgotten | **before ⑧ dispatches** |
| **PRD-9** (Product's number) — *"a completed rental's record tells the truth about what actually happened"* | The whole family: the planned-only Waktu Sewa card, actual duration with no home, actual lateness with no home, **debt #16** (finally where it belongs), and PRD-1's permission matrix not listing return time at all. **This is where the RPC and the matrix extension get scoped properly, once** — instead of leaking in as a P0 waiver | **after** the OQ-5 visit `[needs Farrel]` |

**AC-13 as Lead proposes to word it `[by-agent]`:** *On `RentalDetailScreen`, a COMPLETED rental displays
the actual return time from `rental.returnedAt`, distinguishable from the planned `dueAt`; an ACTIVE
rental's Waktu Sewa card is unchanged in output. Verified by jest (both statuses) and by one round trip on
a real Android device.* It attaches to the **publish gate**, **not to AC-9** — AC-13 has no math surface and
diluting AC-9 is how a money gate stops meaning something.

## Testing obligation — **F-7 does not apply, and that is the good news** `[by-agent]`

F-7 is about the **input** path (`datetimepicker` resolving to iOS under jest, on `PengembalianScreen`).
⑧ is a **read-side render of an already-populated field** on a different screen. No picker, no
`Platform.select`, nothing structurally untestable. Three checks, all required:

1. **COMPLETED fixture with `returnedAt` deliberately ≠ `dueAt`** — the actual value renders and the
   planned value does **not** appear where the actual belongs.
2. **ACTIVE negative assertion** — still `dueAt`, still `error` colour when overdue, still the Terlambat
   banner. **This is the one that matters most**: it is what stops the naive fix from breaking the case
   that was always correct.
3. **Farrel's own Android device, full round trip** — edit the return time → *Selesaikan Pengembalian* →
   the detail screen shows the edited value. **The only check that crosses the screen boundary**, and the
   only one that would have caught this bug. Lead recommends it as a **blocking pre-publish gate**
   `[needs Farrel]`.

**Explicitly NOT sufficient, and ⑦ must be told so:**
`PengembalianScreen.characterization.test.tsx:1087` —
`expect(payload.returnedAt).toEqual(new Date(2026, 6, 10, 14, 30, 0, 0))`.
It proves the **payload** carries the edit. It is green. **It was green while this bug was live.** Anyone
glancing at it will think it covers the fix. It does not.

**Amendment to ⑦'s brief:** `RentalDetailScreen.acceptance.test.tsx:87` also carries `returnedAt: null`.
At least one acceptance fixture must get a real value, or the acceptance suite keeps sailing over the fixed
path with a null. Plus a COMPLETED-rental row on the visual checklist.

## F-10 · The structural finding, and it outlives this bug `[by-agent]`

> **Every test in this project stops at a screen boundary.** No test follows a value from one screen,
> through the connector, to its display on another.

Eight green dispatches, 52 suites, 284 tests, and a characterisation suite written *specifically* to pin
this screen's behaviour — and none of them caught this, **because none of them could.** ②'s suite proved
the payload was right; nothing asked what the next screen did with it. This is not a criticism of ⑦, ② or
③: it is a gap in the test *architecture*, and this bug is its proof case.

**Owed at ship: debt #18 — no cross-screen integration coverage.** Recorded regardless of what happens to
this fix. It is the most valuable thing this bug bought.

## Open items — the amendment is blocked on these `[needs Farrel]`

| # | Item | Lead recommends |
|---|---|---|
| **A-3a** | Fold the fix in as **⑧, between ⑥ and ⑦**, under the cut line above; recorded as debt **#17** + `Also rides along:` + **AC-13** | **Yes.** Cheapest moment it will ever exist, and it fixes the build Mom validates |
| **A-3b** | **Pre-authorise the tripwire** — anything beyond `RentalDetailScreen`'s render + tests drops out to v1.0.6 automatically | **Yes.** It is what makes folding safe rather than hopeful |
| **A-3c** | **Row shape + Indonesian copy on COMPLETED** — two rows or one? Which labels? **A developer must not invent this** | **Two rows.** Planned relabelled **"Estimasi Kembali"** → `dueAt`; new **"Dikembalikan"** → `returnedAt`. Preserves the only planned-vs-actual lateness record on the permanent screen. Both changes confined to the COMPLETED branch so ACTIVE's output is untouched. Cost: one extra row, which **must grow not clip** at `fontScale` 1.5 (AC-10 / PRD-5) |
| **A-3d** | `NULL` `returned_at` on a legacy COMPLETED row | **Omit the "Dikembalikan" row entirely**; the planned row is already present and correctly labelled. Never a blank, never "Invalid Date". **No backfill migration, ever** |
| **A-3e** | Severity label: **P1/high** rather than P0 | **P1/high.** Changes no scheduling; prevents the label buying a waiver later |
| **A-3f** | **PRD-9** opened now, or after the OQ-5 visit? | **After the visit.** Record the obligation + the #16 masking insight now; opening a PRD mid-release competes with two dispatches and a publish gate, and nothing decays by waiting |
| **A-3g** | Farrel's own-device round trip as a **blocking** pre-publish gate | **Yes.** Five minutes, and the only check that crosses the screen boundary |
| **A-3h** | **Mom's visit does NOT grow.** AC-12 asks her a *presentation* question; adding "is this the right return time?" mixes two mental models and risks contaminating the one verdict this release needs | **Hold at three verdicts** (PRD-4 AC-8, PRD-5 AC-8, v1.0.5 AC-12). AC-13 goes on Farrel's device pass instead |

### Still unanswered from before this amendment — both cheap now, expensive at ⑦

| # | Item | Status |
|---|---|---|
| **⑤'s *Catatan* question** | Is `PengembalianScreen`'s *Catatan* boxed? Lead recommends **yes** (BR-1/BR-4 govern; AC-1's list enumerates, and an omission from an enumeration is not an exclusion). **Likely fails AC-7 layer 2 at ⑦ if left unboxed** | **BLOCKING ⑧'s sibling work — unanswered since ⑤ returned** |
| **v1.0.4's overlapping visual rows** | B3/C3/F2, I6/J2/F3, B4/D1/I8, C5/I7 walked on Farrel's device **before** v1.0.5 publishes. Approved 2026-07-25 `[by-Farrel]` | **Outstanding, not started.** Can run in parallel with ⑥ |
| **Uncommitted change on `PengembalianScreen.tsx`** | `4 +` / `8 −`, unexplained by this record, on the fenced file, after ⑤'s commit `99a179b` (which the report certified as a clean tree). **Lead cannot open code to identify it** | **RESOLVED — a concurrent session, see A-4c** |

---

## Farrel's rulings on the amendment — 2026-07-26 `[by-Farrel]`

| # | Decision | Recorded |
|---|---|---|
| **A-3a** | **Fold the fix in as ⑧, between ⑥ and ⑦**, under the cut line above | ✅ `[by-Farrel]` |
| **A-3b** | **Tripwire pre-authorised** — anything beyond `RentalDetailScreen`'s render + tests drops out to a v1.0.6 hotfix automatically, no renegotiation | ✅ `[by-Farrel]` |
| **A-3c** | Row shape + Indonesian copy on COMPLETED | ⏸ **no option selected — still open.** Superseded by A-4a below |
| **⑤'s *Catatan*** | **Leave it unboxed in this session** — *"it's being worked on in another session"* | ✅ `[by-Farrel]` — see **A-4c** |

### A-4a · Farrel's added requirement — **the Durasi row must reflect `returned_at`** `[by-Farrel]`

Farrel's note, verbatim: *"and change the duration to fit the returned_at, not the due_at."*

**This expands ⑧'s cut line, which explicitly declined the Durasi row, and it rests on a premise that is
factually wrong. Correcting the premise before scoping the request `[by-agent]`:**

**Durasi is not derived from `dueAt` today.** `RentalDetailScreen.tsx:441` renders
`formatPaket(paketHari, paketJam)` — **the booked package**, i.e. what the customer paid for and the basis
of the tariff. It is not an interval between two timestamps. So the request is not "swap one operand for
another"; it is **"replace a paid-for value with a measured one"**, which is a different and larger change:

- **It removes the tariff basis from the permanent record.** *Total Tagihan* on that screen was computed
  from the booked package. Replacing the only on-screen statement of that package with an actual elapsed
  duration leaves the total unreconcilable against anything visible.
- **The measurement function is local to the fenced screen.** `formatActualDuration` lives at
  `PengembalianScreen.tsx:60`. Reaching it means either **extracting it** — touching `PengembalianScreen`
  post-⑤ / pre-⑥, which **trips the tripwire A-3b just pre-authorised** — or **re-implementing duration
  math in a second site**, which is exactly how debt #4's two `FuelGauge` copies diverged on `max`.

**Possible clean third path, being checked, not assumed:** ① established that this screen's math was
*already* extracted to `app/utils/rentalMath.ts`. If a shared duration helper already exists there,
`RentalDetailScreen` can consume it with **zero** lines in `PengembalianScreen` and the request fits ⑧
untouched. **PM re-consulted on exactly this** — it is the fact that decides whether A-4a is in ⑧ or drops
to v1.0.6 under the tripwire.

**Lead's position `[by-agent]`:** the *intent* is right and worth delivering — a completed rental should say
how long the vehicle was actually out. The question is only whether it is **additive** (a new row, booked
package retained) or **substitutive** (the package replaced). Lead expects to recommend **additive**, for
the reconciliation reason above. A-3c's row shape is therefore folded into A-4a and re-presented as one
decision once PM reports.

### A-4c · A concurrent session is editing `PengembalianScreen.tsx` — this has sequencing consequences `[by-agent]`

Farrel's *Catatan* answer accounts for the uncommitted `4 +` / `8 −`: **another session is working that
file right now.** It is no longer an unexplained diff — but it is now a **scheduling constraint**, and a
sharper one:

1. **⑥'s job is certifying `PengembalianScreen`'s diff.** Run against a working tree a second session is
   actively editing, it certifies a moving target and its PASS means nothing.
2. **Resolution `[by-agent]`: ⑥ reviews ⑤'s *committed* diff — `0eb4150..99a179b` — which is immutable in
   git**, and is told explicitly that the working tree is dirty from a concurrent session whose changes are
   **not** in its scope. That unblocks ⑥ today without it certifying anything it cannot see the end of.
3. **Whatever the other session lands owes its own review pass**, and must keep the AC-9 characterisation
   suite green at 46/46. That obligation belongs to that session; recorded here so it is not lost between
   the two.
4. **⑧ must not touch `PengembalianScreen` at all** — already in its cut line, and now doubly true: a
   concurrent editor makes any edit there a merge conflict as well as a tripwire breach.

> ⚠️ **THE SECTION ABOVE (A-4a, A-4c) IS SUPERSEDED — corrected 2026-07-27 `[by-agent]`.** It shows Lead
> mid-scoping, *"expecting to recommend additive"*, and the numbering it uses for the display fix (**⑧**)
> collides with S4's ⑧. **Farrel has since ruled: substitutive, and deferred to v1.0.6 (A-4b / A-4a).** The
> dispatch was **renumbered ⑩** and **shipped as `f0a965e`.** A-4c's "concurrent session" is now identified:
> it was the *other Lead session*, which has stopped. **Read § S5 below for what is actually true.**

---

# ▶ S5 — resumed 2026-07-27, 09:06. **Holds lifted. Report ownership settled. Finishing the sequence.**

Session **S5**, Lead on **Opus 5 xhigh** — the *sanctioned fallback*, not the primary. Fable-high is the
recommended model for this role; flagged to Farrel on resume, as S4 and S3 also flagged it. Resumed from
`docs/handoff-prompts/AI_Continuation_Document-27Jul2026-0906.md`, having read **both untracked sidecars in
full** before touching anything.

**Nothing above this line was re-opened**, except the three factual corrections marked as such. Phase 1, the
re-gate, and dispatches ① ② ②b ③ ②c ④ ④b ⑤ ⑥ ⑩ stand exactly as recorded.

### Farrel's instruction lifting both holds — 2026-07-27 `[by-Farrel]`

> *"the bug is fixed, can we continue on fixing the problems i stated before and just finishing the
> sequence? there are new changes on the docs/reports/v1-0-5.md from the other lead session. we might want
> to redo the rental math reviewer step."*

Three instructions, and Lead reads them as `[by-Farrel]`:

1. **Both holds are over.** The `rental-math-reviewer` hold (2026-07-26) and the device-verification halt
   are lifted. Resume dispatching.
2. **The merge pass is authorised and expected.** *"new changes … from the other lead session"* — absorb the
   sidecars, correct the stale lines, renumber the collisions.
3. **A second `rental-math-reviewer` pass is owed.** Farrel independently reached the same conclusion ⑥'s
   own sidecar states: **`v1-0-5-dispatch-6.md` does not discharge the final-diff review.** It runs after ⑨.

### State verified on resume — every number measured by Lead running the command `[by-agent]`

**None of these is quoted from an agent or from either prior session's handoff.**

| Fact | Value | How |
|---|---|---|
| Branch | `v1.0.5-which-fields-you-can-change` | `git status --short` |
| HEAD | **`f0a965e`** `fix(v1.0.5): RentalDetailScreen shows the actual Kembali/returnedAt…` — ⑩ | `git log --oneline -3` |
| Working tree | **NOT clean** — `M PengembalianScreen.tsx` **+4 / −8**, **Farrel's own approved fix** · `M docs/reports/v1-0-5.md` · 4 untracked docs | `git status --short` · `git diff --numstat` |
| **Full suite** | **52 suites / 288 tests — 287 passing, 1 FAILING** (51 suites pass, 1 fails) | `npx jest --json --outputFile=…`, JSON parsed |
| **The one failure** | `PengembalianScreen.characterization.test.tsx` › *"pressing the inline Edit control opens the Android picker, and a chosen value reaches the payload"* — **F-11, expected, owned by ⑧** | ibid. |
| **Lint** | **121** real non-`prettier` errors | the eslint JSON one-liner in § Pre-execution state |
| Lint breakdown | `no-inline-styles` **87** · `no-color-literals` **15** · `sort-styles` **11** · **`no-unused-styles` 4** (was 3 — the orphaned `inlineEditBtn`) · `no-unused-vars` 3 · `import/no-unresolved` 1 · **`no-restricted-imports` 0** | ibid. |
| Types | **exit 0** | `npx tsc --noEmit` |
| AC-9 "before" | **`49fae63`** — quote this SHA, **not** `303ed6d` | — |

**Every figure reproduces S4's and the 21:17 session's independently. Three sessions, same numbers.** The
`+4` test delta against S4's 284 is ⑩'s four new tests, exactly as its record claims.

### The one red test is expected, owned, and must not be "fixed" by a developer `[by-Farrel]`

Farrel's edit removed the *Kembali* row's inline "Edit" control; he **ruled the removal intentional**, on the
grounds that *"clicking the fieldbox already give interaction on editing"* — **which is PRD-8's own
proposition, applied to the release's own screen.** **D-2's first clause stands** (the *Kembali* row wears
the box); **D-2's second clause — "keeps its `inlineEditBtn`, no interaction change" — is superseded as of
2026-07-26 `[by-Farrel]`.**

**The interaction itself is intact.** The same `TouchableOpacity` survives at `PengembalianScreen.tsx:425`
with `onPress={openPicker}` and `activeOpacity={0.7}` untouched; `openPicker` is byte-identical; no value,
handler, math, string or the `returnedAt` seed moved. **Only the test's handle moved.** ⑧ — a **`tester`,
never a developer** — re-points it.

> **The accepted consequence earns a named acceptance row, and it is the sharpest thing in this release.**
> This is the row **debt #16** lives on: `returnedAt` defaults to `new Date()` at screen-open, has already
> produced one wrong persisted record, and has **no in-app correction path**. After this change **the box is
> the only signal on that row that the value can be corrected** — every other boxed Field carries a caret,
> placeholder or keyboard; this one holds nothing but a formatted date inside a border. **It is therefore
> the purest test of PRD-8's premise anywhere in the app.** ⑦'s checklist and Mom's AC-12 walk carry it as a
> **task, not a look**: *"Ask Mom to change the return time without showing her how."*

### Merge obligations — status `[by-agent]`

| # | Obligation | Status |
|---|---|---|
| 1 | Correct the stale *"⑥ still on hold"* lines and state ⑥'s scope precisely | ✅ **Done** — heading + § ⑥ rewritten; scope stated as *⑤'s committed migration only* |
| 2 | Merge `v1-0-5-dispatch-6.md` into this report, then delete the sidecar | ✅ **Merged** (§ ⑥ above). Sidecar deleted at the ship commit, not before |
| 3 | Merge the 21:17 session's decision log, **renumbering its colliding findings** | ✅ **Done below** — its F-10 → **F-13**, its F-11 → **F-14** |
| 4 | Record ⑩ in the main report | ✅ **Done below** |
| 5 | The tail's stale A-4a scoping (*"expects to recommend additive"*) | ✅ **Superseded-notice added** directly above this section |
| 6 | ⑤'s false *"only two `height:` declarations"* sentence, wherever it appears | ✅ **Struck and corrected** at § What ⑤ delivered — one occurrence, line ~1717 |

### 🔢 Finding numbers — the collision is resolved. **This table is canonical.**

The 21:17 session numbered its own findings F-10 and F-11; **this report had already used both numbers for
different things.** This report is the authoritative record, so **its numbering wins and the sidecar's
findings are renumbered:**

| # | Finding | Origin |
|---|---|---|
| **F-10** | **AC-7 is blind to date/time-picker Fields by construction** | S4 |
| **F-11** | **Farrel's edit turns the AC-9 instrument red** (ruled intentional) | S4 |
| **F-12** | **`PembayaranSheet`'s `dateRow` is a rogue second field-box style** | S4 |
| **F-13** | **No test in this project crosses a screen boundary** — *was the 21:17 session's "F-10"* | 21:17 |
| **F-14** | **The `durationToPaket` reuse trap** — *was the 21:17 session's "F-11"* | 21:17 |

### ⑩ `developer-frontend` — returned green 2026-07-26, **Lead-verified**. Commit `f0a965e`.

**The defect:** Mom edits *waktu kembali* on `PengembalianScreen` → *Selesaikan Pengembalian* →
`RentalDetailScreen` shows the **planned** value from the original *sewa baru* flow instead.

**PM traced it to a pure read-side omission — nothing lost, nothing corrupt.** Screen state → payload ✅ ·
connector ✅ · **the RPC writes the client's value, not `now()`** ✅ · read-back translator ✅ ·
**display ❌** — `RentalDetailScreen` rendered `rental.dueAt` under the label *"Kembali"*. `rental.returnedAt`
was rendered **nowhere in `app/`**. **Every rental Mom has closed has the correct return time in the
database. No audit, no backfill, no migration. Guard 5 / BR-9 / AC-11 untouched, and the fully-OTA-revertible
rollback story survives.** **Not a v1.0.5 regression** — neither ④b nor ⑤ went near the Waktu Sewa card;
**AC-9 did not fail and ⑤ broke nothing.**

**What it built** — three derived constants beside the file's other derived render values:

```ts
const showActualReturn = rental.status === "COMPLETED" && rental.returnedAt != null
const kembaliLabel = showActualReturn ? "Dikembalikan" : "Kembali"
const kembaliDate  = showActualReturn ? (rental.returnedAt as Date) : rental.dueAt
```

The `overdue && rental.status === "ACTIVE"` colour expression was left **completely untouched** — already
ACTIVE-gated, so COMPLETED rows were never eligible for `colors.error`. **No other line in the file changed.**

**Lead re-ran everything rather than quoting the agent:**

| Check | ⑩ reported | Lead measured | Verdict |
|---|---|---|---|
| Commit scope | 3 files | **`f0a965e`, exactly 3** — screen (+12 net), `.test.tsx` (+57), `.acceptance.test.tsx` (+27) | ✅ |
| Suites / tests | 52 / 288, 1 failing | **52 / 288, 287 passing, 1 failing** | ✅ `+4`, all ⑩'s |
| The 1 failure | pre-existing, not its own | **Confirmed** — the F-11 `"Edit"` locator | ✅ **⑧ owns it** |
| Types / Lint | exit 0 / 121 | **exit 0 / 121**, `no-unused-styles` 4 corroborating the cause | ✅ **+0** |

> **⑩ reported an honest asymmetry rather than inflating its own evidence, and that is worth recording.**
> Only the two *"actual value"* assertions were red→green. The ACTIVE-negative and null-fallback tests
> **pass both before and after by design** — they encode *"must not change"*. **⑩ said so itself rather than
> presenting four green tests as four proofs.**

**Tripwire (A-3b): did not come near firing.** No connector change, no type change, no shared or new helper,
**zero lines in `PengembalianScreen.tsx`** — which is also why **the branch carries no red commit**: the
suite failure comes from Farrel's *uncommitted* edit, not from `f0a965e`.

**Still owed for ⑩:** **AC-13** on `docs/releases/v1-0-5.md`, and **debt #17** (the read-side `returnedAt`
omission) **opened and immediately struck** as fixed-in-v1.0.5, cross-referenced to #16 both ways.

### The 21:17 session's decisions, absorbed `[by-Farrel]` unless marked

| # | Decision | Note |
|---|---|---|
| **A-3a** | **Fold the `returnedAt` fix into v1.0.5** rather than hotfix a v1.0.6 | It fixes **the build Mom validates** — under the hotfix option she signs off on a build that is not the one she keeps |
| **A-3b** | **Tripwire pre-authorised** — anything beyond `RentalDetailScreen`'s render + tests drops to v1.0.6 automatically, **no renegotiation** | **Fired the same day it was authorised, and was allowed to** |
| **A-4a** | **The Durasi change defers to v1.0.6.** ⑩ shipped the return-time row only | Verbatim: *"Split but make sure it is extracted into a shared util, not as a new helper"* |
| **A-4a′** | When A-4a ships it must **extract `formatActualDuration` into a shared util**, never define a parallel helper | **Farrel's constraint is stronger than either Lead's or PM's proposal** — the two-copies divergence risk never comes into existence rather than being deferred |
| **A-4b** | **Substitutive.** On a COMPLETED rental the row shows the **actual** return only — no planned value, no muted secondary line, no second row | Chosen with all three ⚠ costs displayed |
| **Labels** | **`Dikembalikan`** — *"let it be, no need to ratify"* | `Estimasi` remains available if a planned line ever returns |
| **Severity** | **P1/high, not P0** | Farrel reported it as P0. Recorded as P1 **so that *"it's P0"* cannot later buy a guard-5 waiver or a halt.** Scheduling unchanged either way |
| **PRD-9** | Opened **after** the OQ-5 visit, not now | *"a completed rental's record tells the truth about what actually happened"* — the planned-only Waktu Sewa card, actual duration and lateness with no home, **debt #16**, and PRD-1's matrix not listing return time. **Where the RPC + matrix extension get scoped properly, once** |
| **AC-12** | **Does not grow.** Stays three verdicts | AC-12 asks a *presentation* question; adding *"is this the right return time?"* mixes two mental models. **AC-13 goes on Farrel's device pass instead** |

**AC-13, proposed wording:** *On `RentalDetailScreen`, a COMPLETED rental displays the actual return time
from `rental.returnedAt`, distinguishable from the planned `dueAt`; an ACTIVE rental's Waktu Sewa card is
unchanged in output. Verified by jest (both statuses) and by one round trip on a real Android device.*
**Attach it to the publish gate, NOT to AC-9** — it has no math surface, and **diluting AC-9 is how a money
gate stops meaning something.**

### F-13 · Every test in this project stops at a screen boundary `[by-agent]`

**No test follows a value from one screen, through the connector, to its display on another.** Eight green
dispatches, 52 suites, a characterisation suite written *specifically* to pin `PengembalianScreen` — and
**none caught ⑩'s bug, because none could.** `characterization.test.tsx:1087` asserts
`expect(payload.returnedAt).toEqual(new Date(2026, 6, 10, 14, 30, 0, 0))`. **It proves the payload was
right. It is green. It was green the entire time the bug was live**, and anyone glancing at it will believe
it covers the fixed path. → **debt #18**, and **the most valuable thing this bug bought.**

### F-14 · The helper reuse that would have recreated debt #16 `[by-agent]`

`formatPaket(...durationToPaket(startAt, returnedAt))` composes **two existing shared exports** into an
actual duration with zero new arithmetic. **It looks free. It is wrong.** `durationToPaket`
(`rentalMath.ts:33-40`) **snaps** — it is a **billing-package inference** function whose job is rounding an
interval **up to a saleable tier**. A vehicle gone 1 day 4 hours returns `{hari:1, jam:6}` →
**"1 Hari 6 Jam"** — **overstating reality by up to 5 hours, in the vocabulary of a charge, on the one row
whose entire purpose is truthfulness.** `formatPaket`'s signature forbids it anyway (`jam: 0 | 6 | 12`), and
widening it is a rental-math edit. **`rentalMath.ts` contains duration arithmetic but not *this* duration
arithmetic. Recorded because the next person wanting an elapsed duration will find the same two exports and
the same apparent freebie.**

### One cost Farrel accepted that does not bite until v1.0.6 `[by-agent]`

He chose **substitutive** knowing lateness becomes unreconstructable in-app. **In v1.0.5 it does not** —
Durasi still shows the booked package, and `dueAt` *was* computed from it, so the planned return stays
**derivable from Mulai + Durasi** for one release. **It becomes genuinely unrecoverable the moment A-4a
flips Durasi to actual elapsed. Record as an explicit v1.0.6 gate: v1.0.6 must re-confirm the trade rather
than inherit it silently.**

### Process finding · two Lead sessions on one release `[by-agent]`

Both sessions wrote to this report, **assigned ⑧ to different dispatches**, and held **contradictory beliefs
about ⑥'s state**. **No file was clobbered and no work was lost — but that was luck, not design:** the two
sessions happened to touch disjoint product files. The 21:17 session **twice saw "file modified on disk"
warnings and rationalised them as its own edits.**

> **The rule this earns:** the release report is a **single-writer artifact**, and an unexplained
> modification to it is a **stop-and-check, not a race to append.** The cheapest detector was already in
> hand — `git status --short` showed an untracked *second* handoff document, which is another session's
> fingerprint, and it was visible well before the collision was understood. **Debt entry + a Lead playbook
> line at ship.**

---

## ⏸ S5 EXECUTION PLAN — **awaiting Farrel's approval. Nothing is dispatched.**

**One question gates the plan, and only ⑨ depends on it** — see § Open item below.

### ⑧ `tester` — re-point the D-2 pinned interaction — model `sonnet`

**The only legitimate characterisation-test change in this release**, and it exists because Farrel superseded
D-2's second clause `[by-Farrel]`. **The brief states that provenance explicitly**, so the agent understands
it is implementing an approved decision rather than accommodating a failure.

1. **Scope: exactly one test.** `characterization.test.tsx:1034`'s `getAllByText("Edit")[0]` becomes a press
   on the *Kembali* row's `TouchableOpacity` — **still at `PengembalianScreen.tsx:425`, still
   `onPress={openPicker}`.** The interaction is intact; only its handle moved.
2. **The payload assertions must NOT weaken.** That is what D-2 was protecting, and Farrel's ruling does not
   touch it.
3. **Update the `describe` title**, which still reads *"keeps its inlineEditBtn"*, and cite the supersession
   date so the next reader is not confused by this report's earlier D-2 text.
4. **Touch no product code.** The orphaned style and the stale JSX comment are **⑨'s**.
5. Report the instrument at **2 suites / 46 tests green** and the full suite at **52 / 288, 288 passing**.

**Then Lead commits Farrel's edit + ⑧'s re-point as ONE commit** — the branch never carries a red commit, and
a reader sees the product change and its sanctioned test change in one diff with D-2's supersession in the
message.

### ⑨ `developer-frontend` (fresh) — the device-findings bundle — model `sonnet`

Briefed **standalone** with the guards verbatim (E-1's lesson) and **the instrument protocol: run the
characterisation suite before and after; any delta = STOP and report; never edit a test.**

| # | Item | Source |
|---|---|---|
| 1 | **Delete `styles.inlineEditBtn`** (`:1242-1246`) — orphaned by Farrel's edit; the whole `120 → 121` lint movement | F-11 |
| 2 | **Rewrite the stale JSX comment at `:419-422`** — it documents D-2's superseded clause as though implemented | F-11 |
| 3 | **Box *Catatan*** (`:989`) — **MANDATORY, not optional.** ⑥ verified it is a live AC-7 scope breach | Finding 3 / ⑥'s D-2 |
| 4 | **Replace `PembayaranSheet`'s `dateRow` with `FieldBox`** — retires a rogue duplicate box style carrying `outlineVariant` and a **fixed `height: 56`** | Finding 5 / F-12 |
| 5 | **Align the three buttons at `minHeight: 48` + padding**, keeping Controls visually distinct from Fields | Finding 4 `[by-Farrel]` + refinements (a)/(b) |

**Two constraints carried into the brief verbatim, because a reasonable agent would otherwise get them
wrong:**

- **(a) `minHeight` is ADDED; `paddingVertical` is NOT removed.** `minHeight` alone lets text touch the
  border once content exceeds 48dp at `fontScale` 1.5 — **the exact failure v1.0.4 spent a release learning
  to avoid.** The pattern is **`minHeight` + padding, never a fixed `height`.**
- **(b) 🔴 The buttons must NOT adopt `FieldBox`'s tokens.** Controls keep `outlineVariant` + radius 10;
  Fields use `outline` + radius 12 + `surface`. **Unifying them would break the release** — a bordered,
  filled control wearing the field-box border colour and radius **reads as a field box**, and BR-1's whole
  proposition is *boxed = a value Mom can change*. **BR-3 retired `outlineVariant` from *field* borders
  only.** Nothing currently written down says the two must differ, so a future agent "tidying" them would be
  doing something reasonable-looking and destructive.

**Fences reproduced verbatim:** guard 1 (`returnedAt`'s seed at `:172` untouched) · guard 2 (local
`SectionLabel` / `FuelGauge` / `Stepper` / bottom bar stay put) · guard 3 (debt #12's two divergences stay
pinned) · guard 4 (no math) · guard 6 (`DetailSewaScreen` not opened) · **no `pendingPayments` edit/delete
affordance — pre-existing, PRD-6's, and adding it changes the `closeRental` payload** · **do not implement
the Durasi/`formatActualDuration` change (A-4a defers it to v1.0.6)** · lint back to **120 / +0** ·
`app.json` `version` stays `1.0.0` · zero diff under `app/services/` and `supabase/`.

### Then, in order

| Step | What | Why it is where it is |
|---|---|---|
| **2nd `rental-math-reviewer`** | **On the FINAL diff**, using the pristine-artifact technique | ⑥ reviewed ⑤'s committed migration **only**. `v1-0-5-dispatch-6.md` **explicitly does not discharge this.** Farrel asked for it independently |
| **⑦ `tester` (fresh)** | Durability layer (AC-6/7/8, **mutation-verified, including a near-miss duplicate like F-12's `dateRow`**), acceptance ACs, full suite, lint re-count, visual checklist | Carries the *Kembali* **AC-12 task row**, F-10's picker-Field rows, F-12's four `PembayaranSheet` screens, the two button rows, and ⑥'s AC-10 extra-fee-row-width flag |
| **Report merge pass** | Final sweep + debt-register additions | Merge obligations 1–6 are already done; the ship-time rows are not |
| **Publish gate → OTA** | Then Mom's combined AC-12 + v1.0.4 AC-8 visit, and Farrel's AC-13 round trip | Mom's own phone is the only thing that can close AC-12 |

### Model routing for the remainder

| Agent | Model | Status |
|---|---|---|
| Lead (S5) | Fable high → **Opus 5 xhigh** | **on the sanctioned fallback**, flagged to Farrel |
| ⑧ `tester` | `sonnet` | pinned |
| ⑨ `developer-frontend` | `sonnet` | pinned |
| 2nd `rental-math-reviewer` | inherits | — |
| ⑦ `tester` | `sonnet` | pinned |

**If a developer-tier Sonnet is unavailable, Lead stops and hands to Farrel. No larger model is substituted
for a developer.**

### ❓ Open item — the plan is blocked on exactly one thing `[needs Farrel]`

**Is the device-findings list closed?** Findings 1–5 were given as *"so far"*, and that qualifier was never
formally retracted. Farrel has since said the bug is fixed and asked to finish the sequence, **which reads
as closure but has not been stated.** It matters because **⑨ is a bundle: a sixth finding arriving after it
is dispatched costs an entire extra dispatch**, and re-opening `PengembalianScreen` a third time is exactly
what the fences exist to limit.

**⑧ does not depend on this** — it is a single-test re-point of an already-ruled decision, and it can be
dispatched the moment the plan is approved.

### ✅ RESOLVED — Farrel, 2026-07-27 `[by-Farrel]`

| Question | Ruling |
|---|---|
| **Is the device-findings list closed?** | **CLOSED. The list is final.** *"so far"* is formally retracted. **⑨ bundles all five items in one dispatch.** A sixth finding after this point is **v1.0.6 scope**, not a re-bundle |
| **Is the S5 plan approved?** | **APPROVED — begin dispatching.** ⑧ → commit → ⑨ → 2nd `rental-math-reviewer` on the final diff → ⑦ → publish gate → OTA |

**Phase 2 resumes. The gate is closed and ⑧ is dispatched.**

### E-4 · ⑧ picks up ⑥'s D-4 stale comment — a scope addition Lead is taking `[by-agent]`

⑥'s **D-4** found a stale comment at `characterization.test.tsx:464` claiming `amountInputRow` uses
`height: 40`. It no longer has `height` at all; the helper still works, for a **different** reason
(`FieldBox`'s `minHeight: 52` ≠ 40). ⑥ filed it as *"owed to whoever closes AC-9."*

**Lead is folding it into ⑧ rather than leaving it, and the reasoning is about roles, not convenience.**
⑧ is **the one sanctioned opening of the AC-9 instrument in this release**, by **the one role permitted to
edit it**. Leaving D-4 means the next person to touch that comment has no legitimate reason to be in the
file at all. **Constraint carried into the brief: comment text only — zero change to any assertion, helper,
locator or import.** The suite must stay at **46 tests**.

---

## ⑧ `tester` — returned 2026-07-27. **GREEN. The suite is whole again, and no assertion weakened.**

**Lead re-ran every number rather than quoting the agent.** All four measurements reproduce ⑧'s report
exactly:

| Check | ⑧ reported | **Lead measured** | Verdict |
|---|---|---|---|
| Characterisation instrument | 2 suites / 46 tests, all passing | **2 / 46, all passing** | ✅ **AC-9's instrument is whole — 46, not 45 or 47** |
| Full suite | 52 / 288, **288 passing** | **52 suites / 288 tests / 288 passing / 0 failing**, `success: true` | ✅ **287 → 288. The release is GREEN** |
| Types | exit 0 | **`npx tsc --noEmit` exit 0** | ✅ |
| Lint | unchanged | **121**, breakdown byte-identical (`no-unused-styles` still **4**) | ✅ **+0 — correct: ⑧ touched only a test file; the orphaned style is ⑨ item 1** |
| Scope | 1 file | **`+49 / −9` on `characterization.test.tsx` only.** Farrel's `PengembalianScreen.tsx` still **`+4 / −8`, untouched** | ✅ |

### The locator ⑧ chose, and why it is better than the one it replaced `[by-agent]`

```ts
UNSAFE_getAllByType(TouchableOpacity).filter(node => {
  const flat = StyleSheet.flatten(node.props.style) ?? {}
  return flat.alignItems === "flex-start" && flat.flexDirection === "row" && flat.gap !== undefined
})
```

**It reused the file's own idiom** (the same `UNSAFE_getAllByType` + `StyleSheet.flatten` filter as
`allExtraFeeRows`) rather than inventing a new one. `styles.timeRow` is shared by three rows — *Mulai*,
*Kembali*, *Durasi* — **but only *Kembali*'s is a `TouchableOpacity`**; the other two are plain `View`s. ⑧
then **enumerated every other `TouchableOpacity` on the screen** (back button, both Stepper buttons, *Tambah
Biaya* / *Diskon*, both trash icons, a payment row's own Edit, *Tambah Pembayaran*, *Selesaikan*, and all of
`PembayaranSheet`) and established that **every one uses `alignItems: "center"`, never `"flex-start"`.**

> **The helper throws if the match count is ever ≠ 1.** So a future accidental collision **fails loudly
> instead of silently pressing the wrong control** — which is strictly stronger than the
> `getAllByText("Edit")[0]` it replaced, where an index into an array of matches would have happily pressed
> a different element. **The re-point left the instrument sharper than it found it.**

### The assertions — verified unweakened, which was the whole risk `[by-agent]`

**Only the interaction-opening line changed.** Every downstream assertion is byte-identical: the `date`-mode
step, the `time`-mode step, the rendered `"Jumat, 10 Juli 2026 · 14:30"`, the *Terlambat* caption, and —
**decisively** — `expect(payload.returnedAt).toEqual(new Date(2026, 6, 10, 14, 30, 0, 0))`. **That payload
assertion is what D-2 existed to protect and it is untouched.**

### The new `describe` title carries its own provenance

> *returnedAt — the Kembali picker interaction (D-2's first clause stands: gets the box. D-2's second clause
> — "keeps its inlineEditBtn, no interaction change" — SUPERSEDED 2026-07-26 by Farrel: inline Edit control
> removed by hand, ruled intentional since the FieldBox itself already gives the interaction)*

**A reader hitting this test now learns why it looks the way it does without needing this report.** That is
the standard the rest of the suite should be held to.

### ⑧ found a second stale comment and reported it instead of silently fixing it `[by-agent]`

`openExistingPaymentEdit`'s docstring (`:485-486`) still claimed `within(row)` *"disambiguates from the
Kembali row's OWN 'Edit' control, which uses the identical string."* **That control no longer exists** — so
the comment is *more* stale than ⑥'s D-4 was, and it describes a deleted control inside the AC-9 instrument.

**⑧ correctly left it alone as outside its three-item brief and flagged it.** Lead sent it back as a
comment-only follow-up under the same E-4 reasoning: **this dispatch is the one sanctioned opening of this
file in the release, by the one role permitted to edit it.** Left uncorrected, the next person to touch that
comment would have no legitimate reason to be in the file at all.

**The follow-up returned green and Lead re-verified it**: comment lines only, `within(row).getByText("Edit")`
and `openExistingPaymentEdit`'s body untouched, **52 / 288 all passing, tsc exit 0**, final scope
`+56 / −10` on the one file. The corrected comment now **dates the removal (2026-07-26)** and re-grounds why
`within(row)` is still necessary on its own terms — **a rental can carry several persisted payment rows,
each rendering the identical "Edit" string.** The scoping was over-attributed to the *Kembali* collision; it
was always doing a second, still-live job.

### Commit — `ad34bbb` `[by-agent]`

**Farrel's product edit and ⑧'s sanctioned test change are ONE commit**, so the branch never carries a red
commit and a reader sees the change and its authorised test adjustment in a single diff, with D-2's
supersession and Farrel's verbatim ruling in the message.

```
ad34bbb  fix(v1.0.5): the Kembali row drops its inline Edit control — D-2's second clause superseded
f0a965e  fix(v1.0.5): RentalDetailScreen shows the actual Kembali/returnedAt, not the planned dueAt
```

**2 files changed, 60 insertions, 18 deletions.** Working tree now carries **no product changes** — only this
report and four untracked docs. **Lead confirmed the content of Farrel's diff before committing** rather than
trusting the line count: the *Kembali* `<Text>` label moves above `<FieldBox>`, the
`<View style={styles.inlineEditBtn}>` block with its `MaterialIcons` and `<Text>Edit</Text>` is deleted, the
`TouchableOpacity` with `onPress={openPicker}` and `activeOpacity={0.7}` survives **byte-identical**, and the
second hunk is a cosmetic line-wrap of the extra-fee row's `"Rp"` `Text`. **No value, handler, math, string,
or the `returnedAt` seed moved.**

**Verified at the commit — measured by Lead, not quoted:** 52 suites / 288 tests, **288 passing, 0 failing**
· AC-9 instrument **2 / 46 green** · `npx tsc --noEmit` **exit 0** · lint **121** (**+0**; the orphaned
`styles.inlineEditBtn` is ⑨ item 1 and `no-unused-styles` stays at 4 until then).

---

## ⑨ `developer-frontend` — dispatched 2026-07-27, awaiting return

Dispatched **fresh and standalone** on `sonnet` with all five items, every fence reproduced verbatim, and
**the instrument protocol stated as the first thing it reads after the item list**: run the characterisation
suite before and after each item; **46/46 or STOP**; **never edit a test**; a red instrument after a
developer's change is a **finding**, not a locator to fix.

**Two brief decisions worth recording `[by-agent]`:**

1. **Item 3 (*Catatan*) is flagged in the brief as the item most likely to disturb a locator**, because it
   inserts a `FieldBox` ancestor into the rendered tree of the very screen the instrument pins. ⑨ is told to
   re-run immediately after that item specifically, rather than batching all five and discovering a delta it
   cannot attribute.
2. **Refinement (b) — "do not unify the button tokens with `FieldBox`" — is given as a requirement with its
   reasoning**, not as a bare prohibition. A developer told only *"don't change these two values"* will
   reasonably assume it is fussiness and may tidy them anyway; a developer told *"a filled, bordered control
   wearing the field-box border colour and radius reads as a field box, and this release's whole proposition
   is boxed = changeable"* has the model needed to defend the distinction on its own.

---

## ⑨ — returned 2026-07-27. **GREEN. All five items, every fence held, lint back to baseline.**

**The instrument never went red once** — ⑨ reported 46/46 at all six checkpoints (before, after 1+2, after 3,
after 4, after 5, final), which is the protocol working as designed rather than a formality.

### Lead's verification — measured, and the fences checked by pattern rather than by trusting the report

| Check | ⑨ reported | **Lead measured** | Verdict |
|---|---|---|---|
| Full suite | 55 / 291, 0 failing | **55 suites / 291 tests / 291 passed / 0 failed**, `success: true` | ✅ **+3 suites / +3 tests, all ⑨'s new files** |
| AC-9 instrument | 2 / 46, unmodified | **Absent from `git status --short`** — untouched **by ⑨** (see the precision note below) | ✅ |
| Lint | 120 | **120**, `no-unused-styles` **4 → 3**, every other rule byte-identical | ✅ **back to ⑤'s baseline, +0** |
| Types | exit 0 | **`npx tsc --noEmit` exit 0** | ✅ |
| Scope | 2 modified + 3 new | **exactly that** — `PembayaranSheet.tsx`, `PengembalianScreen.tsx`, 3 new test files | ✅ |

**The fences were verified by `git diff -G<pattern> --name-only`, which lists files whose diff hunks contain
a pattern without printing any code** — so Lead could prove the guards held **without reading source**, which
`docs/agents/lead.md` forbids:

- **Guard 1** — `git diff -G"returnedAt"` matches **no product file**. The `new Date()` seed is untouched.
- **Guard 4** — `git diff -G"computeReturnTotal|computeFuelAdjustment|closeRental|applyFuelSuggestion|totalTagihan|sisa|subtotalSewa|pendingPayments|formatActualDuration|durationToPaket"` matches **no product file**. **No math, no payload, no deferred-Durasi work, no `pendingPayments` affordance.**
- **`app/services/` · `supabase/` · `app.json` · `DetailSewaScreen.tsx` · the instrument · `rentalMath.ts`** — all report **clean** under `git status --short`.

> **This technique is worth keeping.** `-G` answers *"did any hunk touch this concept?"* with a filename list
> instead of a diff. It gives Lead a real verification instrument that respects the no-code-reading
> constraint, rather than forcing a choice between trusting the agent and breaking the rule.

### Commit — `24a3df4` `[by-agent]`

**5 files changed, 457 insertions, 41 deletions.**

```
24a3df4  feat(v1.0.5): the device-findings bundle — Catatan boxed, dateRow retired, buttons aligned
ad34bbb  fix(v1.0.5): the Kembali row drops its inline Edit control — D-2's second clause superseded
f0a965e  fix(v1.0.5): RentalDetailScreen shows the actual Kembali/returnedAt, not the planned dueAt
```

### What ⑨ delivered, and the two places it went beyond a literal reading — both correctly `[by-agent]`

**Item 3 (*Catatan*)** — wrapped in `FieldBox`; `notesInput` gained `padding: 0` so the input's own padding
does not stack on the box's, **matching the established `tujuanInput` / `RupiahInput.field` convention**. Its
`minHeight: 96` (the ~4-line minimum) is unchanged, and **both heights are minimums**, so a long note grows
rather than clips. ⑨ also removed an earlier dispatch's comment that explicitly declined to box *Catatan* —
**correct, since this brief supersedes that decision**, and leaving it would have been a fresh stale-comment
defect of exactly the kind ⑥'s D-4 filed.

**Item 4 (`dateRow`)** — ⑨ dropped `paddingHorizontal: 16` **in addition to** the border/fill/radius/height
the brief named. **This was the right call and it was reasoned, not assumed:** keeping it would have stacked
on `FieldBox`'s own `paddingHorizontal: spacing.md` (12) and **doubled the inset**. Every other
`FieldBox`-wrapped row in the codebase follows the same *"let the box own the padding"* convention.
⑨ verified all **four** consumer screens use `PembayaranSheet` through its unchanged public prop API only —
**and checked `DetailSewaScreen` by grepping its import line rather than opening the file, correctly
respecting guard 5** while still discharging the verification.

### ⚠️ ⑨'s one flagged judgment call — accepted, and routed to ⑦ rather than absorbed `[by-agent]`

**`addLineBtn`'s new `paddingVertical` is `spacing.sm` (8), and the brief did not specify a value.** ⑨ chose
it over matching `addPaymentBtn`'s `spacing.md` (12), reasoning that at default scale `minHeight: 48`
dominates either way, so the choice only bites at large `fontScale`, and 8 keeps the button closer to its
original 44px design intent while still separating text from the border if it wraps.

**Lead accepts it and is NOT overriding it — but it does not close silently.** It is a defensible-but-
arbitrary number **on the exact axis v1.0.4 spent an entire release fixing** (text touching a border at
`fontScale` 1.5). **⑦ carries it as a named device-checklist row**, not as a look-and-see. ⑨ raising it
itself, rather than picking a number and moving on, is the behaviour the briefs keep asking for.

### F-15 · The `findStyledAncestor` trap — a reusable testing finding `[by-agent]`

⑨ found that the project's existing height-based ancestor-walk test helper **does not work for *Catatan***,
because `notesInput` **itself** declares `minHeight: 96` one level *below* `FieldBox`'s `minHeight: 52` — so
the walk stops at the wrong ancestor. ⑨ wrote a local `borderColor`-based predicate instead, mirroring the
characterisation suite's own idiom.

> **Generalised: any `FieldBox` test on a field whose own inner style also declares `height` or `minHeight`
> will silently match the wrong ancestor.** It fails by finding *something* rather than by finding nothing,
> which is the dangerous kind. **⑦ must know this before writing AC-6's mutation tests** — a durability test
> that walks to the wrong node can pass while proving nothing. → debt register at ship.

### ⑨'s "noticed but not acted on" — all four carried, none actioned this release `[by-agent]`

1. **Three pre-existing `no-unused-styles` hits** outside its scope — `PengembalianScreen`'s `lateCaption`,
   `RentalDetailScreen`'s `paketChip` and `timeRow`. **These are the whole remaining count of that rule.**
2. The `addLineBtn` padding value — above, routed to ⑦.
3. **`PembayaranSheet` carries other inline styles and a colour literal** (`rgba(0,0,0,0.4)` backdrop) —
   pre-existing debt, unrelated to the *Tanggal* box.
4. **`HutangDetailScreen` has an unused `handleAddPayment`** — one of the three standing
   `@typescript-eslint/no-unused-vars`. Noticed while checking `PembayaranSheet`'s consumers.

**None is in scope and none was touched.** Debt register at ship.

---

## ⑪ `rental-math-reviewer` — the FINAL-diff pass. **PASS on the money fence.**

**This is the review ⑥ explicitly did not discharge.** ⑥ covered `0eb4150 → 99a179b` (⑤'s migration commit
only). ⑪ covers **`49fae63 → 24a3df4`** — the entire release, including Farrel's F-11 edit, ⑧'s re-point and
all five of ⑨'s items. Farrel asked for it independently on resume; it was already owed.

**Supplied via the pristine-artifact technique**, so Lead handed a reviewer the code without reading any
itself: `git diff <base> <head> -- <path>` and `git show <head>:<path>` redirected to scratchpad files, via
**Git Bash rather than PowerShell** — PowerShell's `>` writes a **BOM** into the file, which would land at
the top of every artifact the reviewer reads.

### Verdict

> **PASS on the money fence.** *"Every calculation path on the return flow is either provably absent from
> the diff, or is a pure `<FieldBox>` wrap with byte-identical `value` / `onChangeText` / `onPress` props.
> No arithmetic expression, sign, operand, clamp, connector call, payload field, or money-gating conditional
> changed."*

### Method — and it validated its own arithmetic, which is the part worth copying

⑪ reconstructed cumulative old→new offsets across all **20 hunks**, then **checked the chain at both ends**:
the final hunk terminates at new line **1414**, which **matches the committed artifact's length exactly** —
so an error anywhere in the offset chain would have desynchronised that endpoint. It repeated the check on
`PembayaranSheet` (ends at new **476** = artifact length) and spot-checked an interior boundary by naming the
exact JSX line the offset predicts and confirming it in the file.

> **That is a self-verifying proof rather than a careful assertion**, and it is a materially better technique
> than ⑥'s otherwise-identical method, which had no endpoint check.

**Regions appearing in NO hunk at all** — old → new:

| Old | New | Contents |
|---|---|---|
| **90–403** | **87–400** | The entire component body: state, hydration effect, **all computed values**, `applyFuelSuggestion`, `handleSave` **and the close payload** |
| 663–690 | 668–695 | Fuel-suggestion row incl. *Terapkan* |
| 791–962 | 807–978 | Pembayaran section, **Sisa summary**, Status Jaminan banner |
| 983–1048 | 1005–1070 | Sticky CTA (**the save-button label branch**) + `PembayaranSheet` wiring |
| *(sheet)* 20–190 | 21–191 | All sheet logic incl. `handleSubmit` |

**The primitive is structurally inert**, verified from source not assumed: `FieldBox.tsx:47-49` is
`<View style={[styles.box, style]}>{children}</View>` — no `cloneElement`, no prop injection, no context
provider. **"Component swap only" is therefore structural, not a judgment call.**

**`rentalMath.ts` zero-diff, independently corroborated** — ⑪ checked the 25-file stat and confirmed it is
absent, as are `app/services/rentals/**` (the connector and the `rpc_close_rental` call site) and
`app/utils/format.ts` (`formatRupiah` / `parseRupiahInput`).

### The six fence items — all confirmed, each with `file:line`

1. **Fuel adjustment — sign semantics intact.** `rentalMath.ts:83-85` computes
   `selisih = bensinKembali − bensinKeluar` and `direction = selisih > 0 ? "subtract" : selisih < 0 ? "add" : "none"`, matching `docs/02` §6:125-129. `PengembalianScreen.tsx:302-309` writes
   `signed = direction === "add" ? deltaRupiah : −deltaRupiah`. **More fuel returned → negative line → the
   bill goes down. Less → up.** Call-site argument order (departure, return, price) unchanged at `:244-246`.
2. **Return total.** `:228-235` untouched → `computeReturnTotal` = `Math.max(0, subtotal + extras − discount)`
   (`rentalMath.ts:94-95`). **All five money inputs' `value`/`onChangeText` pairs survive the wrap verbatim**,
   each cited by line.
3. **`Sisa`.** `:236-239` untouched — `sisa = Math.max(0, totalTagihan − totalPaid)`, matching §6:138, and
   **the summed set is unchanged** (persisted `rental.payments` + local `pendingPayments`).
4. **Auto-debt.** **No client-side debt mutation exists anywhere** — no `createHutang`/`createDebt` call; the
   trigger is server-side. `closeRental` is called **exactly once app-wide**, from `:331`. All four `sisa > 0`
   branches sit in untouched regions, including the CTA label at `:1024`.
5. **Close payload.** `:317-330`, **inside the untouched 87–400 block, so byte-identical to `49fae63`.**
   Field order preserved; the `km` null-guard unchanged.
6. **`returnedAt` seed.** `:172` `useState<Date>(() => new Date())`, inside the untouched block. **Guard 1
   held.**

### Debt #12's two divergences — preserved, and ⑪ caught a trap in the diff `[by-agent]`

Both confirmed unchanged. **Worth recording: the unconditional-amber block *looks* rewritten in the diff**
because the `sort-styles` alphabetiser paired unrelated lines together. ⑪ **read the final file rather than
trusting the diff's shape** and confirmed all six properties are identical to the pre-release set.
**A reviewer working from the diff alone would plausibly have reported a false positive here.**

### ⚠️ ⑪ corrected the Lead's own record — and it was right `[by-agent]`

⑪ flagged that *"the AC-9 instrument is byte-unmodified"* **cannot be carried forward unqualified**, because
`characterization.test.tsx` shows **`66 +-`** across the release range.

**⑪ is correct and Lead's wording was over-broad.** The precise statement: **⑨ left the instrument untouched;
⑧ made the one sanctioned change to it earlier in the release** — a `tester`, deliberately not a developer,
re-pointing a single interaction after Farrel's ruling. So the file **is** modified across `49fae63..24a3df4`
and only unmodified **relative to ⑨**. The ⑨ verification table above is corrected accordingly.

**⑪ independently assessed the replacement as *stronger*, not weaker** — a structural locator that throws
unless it matches exactly one node, versus a text lookup indexing into an array — with all payload, sign,
colour and CTA assertions intact. **That is a third party reaching ⑧'s conclusion without being told it.**

> **This is the release's seventh factual correction, and the first one aimed at a Lead statement rather than
> an agent's.** The pattern holds exactly: caught by an agent reading the artefact, not by a reviewer reading
> a document.

### ⑪'s other observations — none is a money defect

1. **Stale line references in the instrument's comments** (`:242` for `sisa`, actually `:239`; `:181` for the
   bensin `useState`, actually `:178`; three JSX line cites off by 4–14). **Comments only — every assertion
   is located structurally, not by line number.** Joins ⑥'s D-4 on the register.
2. **The fixed-`height:` audit, re-enumerated: SIX remain**, down from the seven ⑥ found. **`addLineBtn` is
   gone from the list — independent confirmation that ⑨ item 5 landed.** The remaining six are icon buttons,
   gauge segments and a 1px divider; **⑪ states explicitly that none wraps a rupiah value or any text that
   could clip a number.**
3. `styles.inlineEditBtn` removed with no dead style left behind (⑨ item 1 confirmed from the other side).
   `lateCaption` is dead but **was dead pre-release**.
4. `RupiahInput` is a controlled pass-through, so `HutangFormScreen`'s `parseRupiahInput` is unaffected by
   its `FieldBox` adoption.

### ⚠️ The gap ⑪ refused to close by assertion — and it was Lead's staging error `[by-agent]`

**⑪ declined to certify `RentalDetailScreen.tsx`** (116 lines changed in range) because **Lead did not stage
a diff for it.** It said so plainly instead of extending its PASS, and named the exact falsifiable check that
would close it.

**That is the correct behaviour and the failure was Lead's**, not the reviewer's: the artifact set was built
around the two files Lead was thinking about, and a third money-adjacent screen — the one ⑩ changed — was
left out. **A reviewer that had quietly generalised its PASS would have handed the release a hole.**

**Closed by re-dispatch:** `final-RentalDetailScreen.diff` (203 lines) and `RentalDetailScreen.at-24a3df4.tsx`
(1219 lines) staged, and ⑪ resumed on that single question with its earlier PASS explicitly **not** reopened.
A Lead-side probe — `git diff -G"sumPayments|totalBill|Math\.max" --name-only 49fae63 24a3df4` scoped to that
file — returns **no match**, and ⑪ was told to **trust its own reconstruction over that probe if the two
disagree.**

### ⑪b — the gap CLOSED. **`RentalDetailScreen.tsx`: PASS, display-only.**

⑪ resumed on that single question with its earlier PASS explicitly not reopened. **11 hunks**: one import,
one derived-display block, two render sites for the *Kembali* row, two `FieldBox` wraps, two `insetBlock`
removals, three style edits.

> **Every money expression, every payment-mutation call site, and every rupiah render in the file falls
> outside all 11 hunks.**

**Offset chain anchored at the terminus again:** cumulative delta `+8`; old EOF 1211 + 8 = **1219**, and the
committed artifact's last line is 1219. **No intermediate offset can be wrong.**

**1 · The money math is provably absent.** `:298-299` — `totalPaid = sumPayments(rental.payments)` and
`sisa = Math.max(0, rental.totalBill − totalPaid)` — sit in untouched region **new 21–300**, and the nearest
hunk begins at new **301**, two lines below. **Not context, not changed: outside.** §6:138 upheld.

> **⑪ agreed with Lead's `-G` probe but for a stronger reason, and the distinction is worth keeping:**
> *"a `-G` no-match is also what you would get from a mis-scoped path or a pattern that misses the real
> change; the reconstruction shows positively **which** untouched region those lines occupy."* **A negative
> search and a positive localisation are not the same evidence.** Lead's probe is a good cheap screen; it is
> not a proof.

**2 · All three payment mutations and every rupiah render are outside every hunk** — `updatePayment` `:926`,
`addPayment` `:927`, `deletePayment` `:940`, the Tarif → Add-on → Diskon → Total rows in §6:120 order
(`:662`, `:679`, `:689`, `:698`), the payment row amount `:728`, and *Sudah dibayar* / *Sisa* `:775`/`:793`.
**No call moved, was added, removed, or reordered.**

**3 · ⑩'s display fix is read-side only, with no arithmetic.** The three derived constants at `:308-310`
contain **no `+ − * /`, no `Math.*`, no `getTime()`, no `new Date(...)`** — a predicate and two ternary
*selections*. `kembaliDate` **selects** between two existing `Date`s; it never computes one.

> **The check that matters most: the fix did not leak into lateness.** ⑪ grepped the final file and
> confirmed `showActualReturn` / `kembaliLabel` / `kembaliDate` appear **only** at their definitions and two
> render sites, reaching **neither `hoursLate`, `isOverdue`, `sisa`, `totalBill`, nor any close/payment
> path.** The overdue colour conditional (`:438`) and the *Terlambat* caption (`:459`) are both in untouched
> regions, and the caption **still measures against `rental.dueAt`, not `kembaliDate`** — which is correct,
> because `docs/02` §6:131 makes lateness the only signal Mom charges from. **Substituting the actual return
> there would have changed money.** It did not.

**Also verified:** both `insetBlock` removals wrap *Tujuan* and read-only *Catatan* — **no amount was ever
inside them** — and the style was deleted with its last usage. Both `FieldBox` wraps preserve `testID`,
`value` and `onChangeText` verbatim, and their consumers (`parseKmInput`, `updateRental`) are in untouched
regions. `notesInput` retains `minHeight: 80` over the box's 52 floor — **both minimums, so neither can
clip.**

### 🔒 THE MONEY GATE IS CLOSED `[by-agent]`

**AC-9's proposition — that the rental calculations came through this release identical — now rests on
three independent legs:**

| Leg | Evidence |
|---|---|
| **Analytical, migration** | ⑥'s hunk arithmetic over ⑤'s commit |
| **Analytical, full release** | ⑪ + ⑪b's offset reconstruction over `49fae63..24a3df4`, **self-validated at both termini**, across all three money-bearing screens |
| **Observed** | Lead's measured runs: the instrument **46/46 green** before and after every dispatch, and the full suite green at each commit |

**⑪'s one standing limitation, stated by it and not papered over:** it has no Bash tool, so it never executed
anything. **Its analysis is static and would not catch a runtime-only regression** — though, as it notes, the
untouched-range proof means such a regression would have to originate outside the three files reviewed.
**That is exactly the gap ⑦'s execution closes**, which is why the two passes are sequenced and not merged.

---

## ⑦ `tester` — dispatched 2026-07-27, awaiting return

**The final gate.** Briefed fresh with the durability layer (AC-6/7/8, **each mutation-verified**), the
acceptance criteria per screen, the fence diffs, the suite and lint deltas, and the visual checklist.

**Four things were carried into the brief that no earlier ⑦ draft contained `[by-agent]`:**

1. **F-15, the ancestor-walk trap**, stated before it writes a single assertion — because a durability test
   that walks to the wrong node **passes while proving nothing**, and that is the one failure mode a test
   author cannot detect from a green run.
2. **AC-6 must be proven against a NEAR-MISS duplicate**, not an exact one, with the instruction to *assume
   it is currently blind and prove otherwise*. F-12's `dateRow` differed on **every** value, which is
   precisely what an exact-match audit misses.
3. **The *Kembali* row is a TASK row, worded verbatim** — *"Ask Mom to change the return time without showing
   her how."* Left as "confirm the box is visible", it would test the checklist author's eyesight rather than
   PRD-8's premise.
4. **PENDING may never be rounded up to PASS.** AC-12 is structurally pending until Mom has the update on her
   own phone, and saying so is the deliverable.
